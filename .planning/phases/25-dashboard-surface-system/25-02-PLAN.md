---
phase: 25-dashboard-surface-system
plan: "02"
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/features/ui/toast-provider.tsx
  - frontend/src/features/ui/confirmation-dialog.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/pages/auth/login-page.tsx
  - frontend/src/pages/auth/register-page.tsx
  - frontend/src/__tests__/auth-routes-guard.test.tsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04]
must_haves:
  truths:
    - User can read login and registration forms comfortably because the layout, spacing, and field hierarchy match the new surface system.
    - User sees confirmation dialogs and toast notifications use the same shadcn-aligned light/dark surface language instead of bespoke overlays or raw div stacks.
    - User sees interactive auth controls keep the milestone rounded-lg styling consistently.
  artifacts:
    - path: frontend/src/pages/auth/login-page.tsx
      provides: Shadcn-based login form surface
      contains: '@/components/ui'
    - path: frontend/src/pages/auth/register-page.tsx
      provides: Shadcn-based registration form surface
      contains: '@/components/ui'
    - path: frontend/src/features/ui/toast-provider.tsx
      provides: Shared toast orchestration on shadcn-aligned presentation
      contains: toast
    - path: frontend/src/features/ui/confirmation-dialog.tsx
      provides: Shared confirmation surface matching the milestone visual system
      contains: '@/components/ui'
  key_links:
    - from: frontend/src/app/providers.tsx
      to: frontend/src/features/ui/toast-provider.tsx
      via: app-level toast wiring
      pattern: ToastProvider
    - from: frontend/src/features/ui/toast-provider.tsx
      to: frontend/src/components/ui/sonner.tsx
      via: shadcn-aligned toast rendering
      pattern: sonner|Toaster|toast
---

<objective>
Move auth forms and shared feedback surfaces onto the Phase 25 shadcn language.

Purpose: Users should experience the same readable, high-contrast surface system on entry flows and global feedback as they do on the dashboard shell.
Output: Auth pages, dialogs, and toast presentation updated to official `@/components/ui` primitives and milestone light/dark tokens.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-surface-system/25-CONTEXT.md
@frontend/src/app/providers.tsx
@frontend/src/pages/auth/login-page.tsx
@frontend/src/pages/auth/register-page.tsx
@frontend/src/features/ui/toast-provider.tsx
@frontend/src/features/ui/confirmation-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate auth entry forms to official shadcn form primitives</name>
  <files>frontend/src/pages/auth/login-page.tsx, frontend/src/pages/auth/register-page.tsx, frontend/src/__tests__/auth-routes-guard.test.tsx</files>
  <action>Replace the auth pages' raw Tailwind labels, inputs, buttons, and alert boxes with official `@/components/ui` primitives created in Plan 25-01. Keep the existing auth behavior, cooldown handling, inline validation semantics, and return-to routing intact, but remove the current bespoke radial-gradient cards in favor of the milestone shell surface language with clearer spacing and hierarchy. Use `rounded-xl` for the outer auth surface and `rounded-lg` for controls exactly as locked in context.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/auth-routes-guard.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Login and registration are rendered with official shadcn form controls, remain behaviorally unchanged, and feel visually consistent with the new dashboard shell.</done>
</task>

<task type="auto">
  <name>Task 2: Move dialogs and toasts onto shadcn-aligned feedback surfaces</name>
  <files>frontend/src/components/ui/sonner.tsx, frontend/src/features/ui/toast-provider.tsx, frontend/src/features/ui/confirmation-dialog.tsx, frontend/src/app/providers.tsx</files>
  <action>Refactor shared feedback surfaces so toast presentation and confirmation dialogs match the milestone surface system. Keep the existing `useToast` API, dedupe timing, and safety-toast contract, but render notifications through a shadcn-aligned toast surface that respects the Phase 24 theme provider instead of `next-themes`. Update the shared confirmation dialog to use official button/card/input surface language from `@/components/ui`, preserve current portal behavior and semantics, and keep dark-mode separation border-first rather than shadow-first.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/auth-routes-guard.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Global toasts and shared confirmation dialogs inherit the same light/dark surface tokens as the rest of the app without changing the existing event/safety APIs.</done>
</task>

</tasks>

<verification>
Verify auth tests still pass and the shared feedback stack now renders from shadcn-aligned surfaces wired at the provider boundary.
</verification>

<success_criteria>
Auth entry, dialogs, and toasts satisfy the readable spacing and control consistency expectations for VIS-03 and VIS-04 while honoring the locked shadcn rollout decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-02-SUMMARY.md`
</output>
