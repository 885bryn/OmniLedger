---
phase: 25-dashboard-surface-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/features/dashboard/data-card.tsx
  - frontend/src/app/shell/app-shell.tsx
  - frontend/src/pages/dashboard/dashboard-page.tsx
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, IMPL-03]
must_haves:
  truths:
    - User sees the authenticated shell and dashboard use the milestone light tokens with crisp borders and restrained elevation.
    - User sees the same shell and dashboard swap to deep-black dark surfaces that separate panels with borders instead of heavy shadows.
    - User sees at least one reusable data-card pattern built from official `@/components/ui` primitives on the dashboard.
  artifacts:
    - path: frontend/src/index.css
      provides: Phase 25 surface, border, text, and radius token tuning
      contains: --background
    - path: frontend/src/features/dashboard/data-card.tsx
      provides: Reusable shadcn-based data card pattern
      exports: [DataCard]
    - path: frontend/src/pages/dashboard/dashboard-page.tsx
      provides: Dashboard metrics and sections rendered with shared surface primitives
      contains: DataCard
  key_links:
    - from: frontend/src/pages/dashboard/dashboard-page.tsx
      to: frontend/src/features/dashboard/data-card.tsx
      via: shared metric and section rendering
      pattern: DataCard
    - from: frontend/src/app/shell/app-shell.tsx
      to: frontend/src/index.css
      via: background, border, and card token classes
      pattern: bg-background|bg-card|border-border
---

<objective>
Roll the shell and dashboard onto the Phase 25 shadcn surface system.

Purpose: Establish the shared light/dark surface language and ship the first reusable data-card exemplar before form and toast rollout.
Output: Tuned global tokens, any missing official shadcn input primitives needed for later plans, and a dashboard shell/data-card implementation that demonstrates the milestone visual system.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dashboard-surface-system/25-CONTEXT.md
@.planning/phases/24-theme-foundation/24-01-SUMMARY.md
@frontend/src/index.css
@frontend/src/app/shell/app-shell.tsx
@frontend/src/pages/dashboard/dashboard-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Phase 25 surface tokens and official primitive foundation</name>
  <files>frontend/src/index.css, frontend/src/components/ui/select.tsx, frontend/src/components/ui/textarea.tsx</files>
  <action>Retune the existing CSS variable layer in `frontend/src/index.css` so the light theme lands on the roadmap neutral canvas/surface/border/text contract and the dark theme lands on the deep-black border-first contract from the phase context. Keep light mode as the visual baseline, preserve the Phase 24 manual-toggle behavior, and do not introduce `prefers-color-scheme` logic. If dashboard rollout needs additional official primitives, add them with `npx shadcn@latest add ...` and commit the generated `@/components/ui` wrappers instead of hand-rolling bespoke form controls; ensure interactive controls continue to read as `rounded-lg` while cards read as `rounded-xl`.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>`frontend/src/index.css` exposes the milestone surface tokens for both themes, dark mode relies on borders rather than card shadows, and any new form primitives needed by later plans exist under `frontend/src/components/ui`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the new shell surface language and reusable dashboard data card</name>
  <files>frontend/src/features/dashboard/data-card.tsx, frontend/src/app/shell/app-shell.tsx, frontend/src/pages/dashboard/dashboard-page.tsx, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Create a reusable dashboard data-card component built from official `@/components/ui` primitives and Phase 25 tokens, then migrate the authenticated shell and dashboard page to it. Update shell chrome, dashboard metric cards, empty states, and grouped due-date sections so they use consistent `rounded-xl` cards, generous spacing, crisp borders, and restrained light-only elevation. Preserve current data/query behavior and keep existing animation classes only where they do not change the Phase 26 motion scope.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>The dashboard shell reads as one cohesive high-contrast surface system in both themes, at least one reusable data-card exemplar is in the codebase, and dashboard regressions still pass.</done>
</task>

</tasks>

<verification>
Run the dashboard regression suite and confirm the shell/dashboard classes now rely on shared background/card/border/text tokens rather than page-specific gradients and ad hoc panel styles.
</verification>

<success_criteria>
The authenticated shell and dashboard satisfy VIS-01/VIS-02/VIS-03, and the reusable data-card exemplar satisfies IMPL-03 for later rollout plans.
</success_criteria>

<output>
After completion, create `.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-01-SUMMARY.md`
</output>
