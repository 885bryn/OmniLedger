---
phase: 01-domain-model-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/db/index.js
  - src/db/models/index.js
  - src/config/database.js
  - src/scripts/verify-domain-models.js
  - test/db/domain-runtime-smoke.test.js
autonomous: true
requirements:
  - DEPL-02
must_haves:
  truths:
    - "API runtime can initialize Sequelize with PostgreSQL and register Users, Items, Events, and AuditLog models."
    - "All model associations are wired so parent-child items and item-events relations are queryable."
    - "A repeatable automated smoke check confirms model registration and DB connectivity."
  artifacts:
    - path: "src/db/index.js"
      provides: "Sequelize initialization and model bootstrap for runtime use"
      exports:
        - "sequelize"
        - "models"
    - path: "src/db/models/index.js"
      provides: "Single registry that loads and associates User, Item, Event, and AuditLog"
      contains: "associate"
    - path: "src/scripts/verify-domain-models.js"
      provides: "CLI smoke verification for DB connection and model availability"
      contains: "authenticate"
  key_links:
    - from: "src/db/models/index.js"
      to: "src/db/models/*.model.js"
      via: "model loader and associate calls"
      pattern: "User|Item|Event|AuditLog"
    - from: "src/scripts/verify-domain-models.js"
      to: "src/db/index.js"
      via: "runtime bootstrap import"
      pattern: "require\('../db'\)"
---

<objective>
Finalize the domain runtime wiring so Sequelize can boot against PostgreSQL with all four required models available to the API layer.

Purpose: Satisfy the deployment/runtime requirement early by proving model registration and connectivity in an executable smoke flow.
Output: Database bootstrap modules, model registry wiring, and an automated runtime smoke check.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-domain-model-foundation/01-CONTEXT.md
@.planning/phases/01-domain-model-foundation/01-RESEARCH.md
@.planning/phases/01-domain-model-foundation/01-01-SUMMARY.md
@.planning/phases/01-domain-model-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Sequelize runtime bootstrap and model registry</name>
  <files>src/db/index.js, src/db/models/index.js, src/config/database.js</files>
  <action>Implement centralized Sequelize PostgreSQL initialization and model registration for `User`, `Item`, `Event`, and `AuditLog`. Ensure all model association methods execute after registration, and expose a runtime-safe `sequelize` instance plus `models` map for API usage. Keep migration-based schema ownership (do not use `sync({ alter: true })`).</action>
  <verify>
    <automated>node src/scripts/verify-domain-models.js</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Runtime bootstrap initializes without missing model/association errors and exposes all required models from a single import path.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable domain runtime verification script</name>
  <files>src/scripts/verify-domain-models.js</files>
  <action>Create a CLI verification script that authenticates to PostgreSQL, verifies model names include `User`, `Item`, `Event`, and `AuditLog`, and performs a minimal non-destructive query against each table to confirm mappings are usable. Exit non-zero on missing model registration or connectivity/association failure.</action>
  <verify>
    <automated>node src/scripts/verify-domain-models.js</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The script fails fast on runtime wiring problems and passes only when model boot + DB connectivity are valid.</done>
</task>

<task type="auto">
  <name>Task 3: Add smoke test for runtime model availability</name>
  <files>test/db/domain-runtime-smoke.test.js</files>
  <action>Create an automated smoke test that boots the DB module, checks all four required models are present, validates key associations (`Item` self-link, `Event` to `Item`, `AuditLog` to `User`), and confirms a clean connect/disconnect lifecycle for local PostgreSQL runtime.</action>
  <verify>
    <automated>npm test -- test/db/domain-runtime-smoke.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Automated smoke test proves DEPL-02 readiness for API runtime use of Sequelize models against PostgreSQL.</done>
</task>

</tasks>

<verification>
Run `node src/scripts/verify-domain-models.js` and `npm test -- test/db/domain-runtime-smoke.test.js --runInBand`; both pass on a migrated PostgreSQL instance.</verification>

<success_criteria>
The project can boot Sequelize against PostgreSQL and expose Users, Items, Events, and AuditLog models with valid associations for downstream API phases.</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-foundation/01-03-SUMMARY.md`
</output>
