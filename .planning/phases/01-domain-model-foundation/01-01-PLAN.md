---
phase: 01-domain-model-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/XXXXXXXXXXXX-create-users-and-items.js
  - src/db/models/user.model.js
  - src/db/models/item.model.js
  - src/domain/items/minimum-attribute-keys.js
  - test/db/user-item-domain.test.js
autonomous: true
requirements:
  - ACCT-01
  - ITEM-01
  - ITEM-02
  - ITEM-03
must_haves:
  truths:
    - "User account records persist with UUID id, username, email, and password hash."
    - "Items persist only with allowed item types and JSONB attributes payloads."
    - "FinancialCommitment items cannot persist without a valid parent item link."
    - "Parent items cannot be deleted while linked commitments still exist."
  artifacts:
    - path: "src/db/migrations/XXXXXXXXXXXX-create-users-and-items.js"
      provides: "Users and Items schema contracts including UUID keys, JSONB attributes, type enum, and parent FK restriction"
      contains: "createTable('Users'"
    - path: "src/db/models/user.model.js"
      provides: "User model validations and case-insensitive unique identity handling"
      exports:
        - "User"
    - path: "src/db/models/item.model.js"
      provides: "Item model enum/jsonb validations and self-referential parent-child association"
      exports:
        - "Item"
  key_links:
    - from: "src/db/models/item.model.js"
      to: "src/domain/items/minimum-attribute-keys.js"
      via: "model-level custom validator"
      pattern: "minimumAttributeKeys"
    - from: "src/db/models/item.model.js"
      to: "Items.parent_item_id"
      via: "belongsTo/hasMany self-association"
      pattern: "onDelete:\s*'RESTRICT'"
---

<objective>
Create the persistence contract for accounts and items so the ledger stores valid identity and item linkage data from day one.

Purpose: Lock in non-negotiable storage guarantees (UUID identity, item types, JSONB attributes, and commitment-parent integrity) before API endpoints are built.
Output: Users/Items migration, Sequelize models with validations, and automated domain tests for account/item invariants.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-domain-model-foundation/01-CONTEXT.md
@.planning/phases/01-domain-model-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration contract for users and items</name>
  <files>src/db/migrations/XXXXXXXXXXXX-create-users-and-items.js</files>
  <action>Implement migration-first schema contracts for Users and Items using PostgreSQL + Sequelize conventions: UUID primary keys, allowed item type ENUM values (`RealEstate`, `Vehicle`, `FinancialCommitment`, `Subscription`), JSONB `attributes`, self-referencing `parent_item_id` with `ON DELETE RESTRICT`, and a deterministic case-insensitive uniqueness strategy for `username` and `email` (CITEXT or normalized unique index). Reject dangling parent links at DB level and add parent lookup index.</action>
  <verify>
    <automated>npx sequelize-cli db:migrate && npx sequelize-cli db:migrate:status</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Migration creates Users and Items with enforced identity uniqueness, strict item-type constraints, JSONB attributes, and restricted parent-child referential integrity.</done>
</task>

<task type="auto">
  <name>Task 2: Implement User and Item Sequelize models with domain invariants</name>
  <files>src/db/models/user.model.js, src/db/models/item.model.js, src/domain/items/minimum-attribute-keys.js</files>
  <action>Define User and Item model attributes and validations to match locked decisions: balanced required-field posture, case-insensitive identity handling, minimum attribute keys per item type with allowance for extra keys, invalid-value rejection (invalid dates/negative numeric domain values), and type-conditional parent requirement for `FinancialCommitment`. Set self-association for parent/child commitments and keep `Subscription` parent link optional by default.</action>
  <verify>
    <automated>npm test -- test/db/user-item-domain.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>User and Item models enforce all phase-locked semantics and association wiring without controller-only safeguards.</done>
</task>

<task type="auto">
  <name>Task 3: Add executable tests for account and item persistence rules</name>
  <files>test/db/user-item-domain.test.js</files>
  <action>Create focused persistence tests that exercise ACCT-01 and ITEM-01/02/03 behavior against a test PostgreSQL database: user insert success, case-insensitive duplicate rejection for identity fields, item-type enum rejection for unknown values, required minimum JSONB keys by type, failure when commitment parent is missing/nonexistent, and parent delete blocked while linked commitments exist.</action>
  <verify>
    <automated>npm test -- test/db/user-item-domain.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Test suite fails when any core account/item invariant is broken and passes with the intended schema/model implementation.</done>
</task>

</tasks>

<verification>
Run `npx sequelize-cli db:migrate` on a clean database, then run `npm test -- test/db/user-item-domain.test.js --runInBand`; both commands pass and demonstrate database-level plus model-level enforcement.</verification>

<success_criteria>
Accounts and items can be persisted only when they satisfy UUID identity, allowed type, JSONB minimum-key, and parent-child integrity semantics required for downstream API phases.</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-foundation/01-01-SUMMARY.md`
</output>
