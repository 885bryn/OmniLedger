---
phase: 01-domain-model-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/db/migrations/XXXXXXXXXXXX-create-events-and-audit-log.js
  - src/db/models/event.model.js
  - src/db/models/audit-log.model.js
  - src/domain/events/status-and-completion-rules.js
  - test/db/event-audit-domain.test.js
autonomous: true
requirements:
  - EVNT-01
must_haves:
  truths:
    - "Users can persist timeline events with type, due date, amount, status, and recurrence flag."
    - "Stored events only use canonical statuses `Pending` and `Completed`."
    - "Completed events require an explicit completion timestamp in persisted data."
    - "Audit entries can persist user, action, entity, and timestamp fields for traceability groundwork."
  artifacts:
    - path: "src/db/migrations/XXXXXXXXXXXX-create-events-and-audit-log.js"
      provides: "Events and AuditLog schema contracts with UUID keys and item/user foreign keys"
      contains: "createTable('Events'"
    - path: "src/db/models/event.model.js"
      provides: "Event model with status, due-date, amount, recurrence, and completion invariants"
      exports:
        - "Event"
    - path: "src/db/models/audit-log.model.js"
      provides: "AuditLog model with required audit metadata fields"
      exports:
        - "AuditLog"
  key_links:
    - from: "src/db/models/event.model.js"
      to: "src/domain/events/status-and-completion-rules.js"
      via: "model-level validators"
      pattern: "completed_at"
    - from: "src/db/models/event.model.js"
      to: "Items.id"
      via: "belongsTo association and FK"
      pattern: "foreignKey:\s*'item_id'"
---

<objective>
Add event and audit persistence structures so timeline behavior can be stored consistently and audited in later endpoint phases.

Purpose: Establish immutable domain semantics for event lifecycle records before implementing event completion APIs.
Output: Events/AuditLog migration, Sequelize models enforcing event/audit invariants, and event/audit persistence tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-domain-model-foundation/01-CONTEXT.md
@.planning/phases/01-domain-model-foundation/01-RESEARCH.md
@.planning/phases/01-domain-model-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration contract for events and audit logs</name>
  <files>src/db/migrations/XXXXXXXXXXXX-create-events-and-audit-log.js</files>
  <action>Create migration schema for `Events` and `AuditLog` with UUID ids, FK relationships to `Items` and `Users`, enum-constrained event statuses (`Pending`, `Completed`), required event timeline columns (`event_type`, `due_date`, `amount`, `status`, `is_recurring`), and audit fields (`user_id`, `action`, `entity`, `timestamp`). Add integrity-friendly indexes for item timeline queries and audit lookups.</action>
  <verify>
    <automated>npx sequelize-cli db:migrate && npx sequelize-cli db:migrate:status</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Events and AuditLog tables exist with required columns, FK links, status guardrails, and query indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Event and AuditLog models with validation rules</name>
  <files>src/db/models/event.model.js, src/db/models/audit-log.model.js, src/domain/events/status-and-completion-rules.js</files>
  <action>Define Sequelize models and validators for event/audit semantics from locked decisions: allowed event status set, invalid due date rejection, disallowed negative amount rejection, and required completion timestamp when status is `Completed`. Enforce consistent verb-style audit action naming and required `user`, `action`, `entity`, `timestamp` fields for audit rows.</action>
  <verify>
    <automated>npm test -- test/db/event-audit-domain.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Event and AuditLog models enforce domain invariants with no reliance on downstream controller checks.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence tests for event and audit domain behavior</name>
  <files>test/db/event-audit-domain.test.js</files>
  <action>Create tests validating EVNT-01 and locked event/audit language: event rows persist required timeline fields, only canonical statuses are accepted, completed events without completion timestamp fail validation, negative amounts fail validation, and audit rows require all minimum metadata fields with verb-style action values.</action>
  <verify>
    <automated>npm test -- test/db/event-audit-domain.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Automated tests prove event and audit persistence behavior is deterministic and aligned with phase semantics.</done>
</task>

</tasks>

<verification>
Run migrations and execute `npm test -- test/db/event-audit-domain.test.js --runInBand`; results show event rows and audit rows enforce the required domain field contracts.</verification>

<success_criteria>
Event timeline data and audit metadata persist with strict status and validation semantics needed for later completion/audit workflow phases.</success_criteria>

<output>
After completion, create `.planning/phases/01-domain-model-foundation/01-02-SUMMARY.md`
</output>
