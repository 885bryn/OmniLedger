---
phase: 13-admin-scope-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/auth/scope-context.js
  - src/api/routes/items.routes.js
  - src/domain/items/get-item-net-status.js
  - src/domain/items/update-item.js
  - src/domain/items/soft-delete-item.js
  - src/domain/items/restore-item.js
  - src/domain/events/complete-event.js
  - src/domain/events/update-event.js
autonomous: true
requirements:
  - AUTH-06
  - TIME-04
must_haves:
  truths:
    - "Admin in all-data mode can mutate existing cross-owner item/event records without actor-only false denials."
    - "Admin drill-through to /items/:id/net-status honors active scope semantics (all mode allows any owner; owner mode follows lens owner)."
    - "Ownership denials remain 404/not_found for unauthorized scope paths, preserving non-leaky public contracts."
  artifacts:
    - path: "src/api/auth/scope-context.js"
      provides: "Shared scope-to-owner resolution helpers for mutation and detail authorization"
      contains: "resolveOwnerFilter"
    - path: "src/domain/items/get-item-net-status.js"
      provides: "Scope-aware net-status ownership checks replacing actor-only enforcement"
      contains: "scope"
    - path: "src/domain/events/complete-event.js"
      provides: "Scope-aware event complete/undo ownership resolution for persisted and projected targets"
      contains: "resolveTargetEvent"
    - path: "test/api/admin-scope-lens.test.js"
      provides: "Admin mutation and drill-through regressions across all-mode and owner-lens modes"
      contains: "net-status"
  key_links:
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/get-item-net-status.js"
      via: "Pass req.scope to net-status service instead of req.actor.userId"
      pattern: "scope: req.scope"
    - from: "src/domain/events/complete-event.js"
      to: "src/api/auth/scope-context.js"
      via: "Mutation owner resolution uses shared scope helper before ownership checks"
      pattern: "resolve"
    - from: "src/domain/items/update-item.js"
      to: "test/api/items-list-and-mutate.test.js"
      via: "Admin cross-owner update/delete/restore behavior regression-locked"
      pattern: "admin"
---

<objective>
Harden admin mutation and net-status domain scope enforcement to eliminate actor-only drift.

Purpose: Close AUTH-06 and backend TIME-04 continuity gaps by making mutation and detail ownership checks resolve from effective admin scope semantics instead of actor identity.
Output: Shared scope resolution contract adoption across item/event mutation and net-status pathways, with behavior verification against existing integration suites.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-scope-integration-hardening/13-RESEARCH.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-03-SUMMARY.md
@src/api/auth/scope-context.js
@src/api/routes/items.routes.js
@src/domain/items/get-item-net-status.js
@src/domain/events/complete-event.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish shared scope-resolution contract for mutation/detail authorization</name>
  <files>src/api/auth/scope-context.js, src/api/routes/items.routes.js, src/domain/items/get-item-net-status.js, src/domain/items/update-item.js, src/domain/items/soft-delete-item.js, src/domain/items/restore-item.js</files>
  <action>Extend `src/api/auth/scope-context.js` with a single mutation/detail-safe owner-resolution contract that handles admin all-mode, admin owner-lens mode, and standard owner mode. Migrate item net-status and item mutation entry points to consume this shared contract instead of actor-bound owner checks. Update `GET /items/:id/net-status` route wiring to pass `req.scope` into domain logic. In admin all-mode, preserve entity-first authorization (load target then authorize) to avoid false denials on cross-owner records. Keep existing 404/not_found denial envelope and error codes unchanged.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js test/api/items-list-and-mutate.test.js -i</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Item net-status and item mutation paths no longer depend on actor-only owner derivation, and admin all/lens scope enforcement is centralized in shared scope resolution helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Apply scope-resolution contract to event complete/update mutation flows</name>
  <files>src/domain/events/complete-event.js, src/domain/events/update-event.js</files>
  <action>Migrate event completion/undo and event update ownership checks to the same shared scope-resolution contract introduced for item paths. Ensure projected and persisted event targets both resolve authorization via effective request scope (admin all, admin lens, standard owner), not actor identity. Preserve existing domain return contracts, not_found mapping, and completion semantics while replacing only authorization owner resolution.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js test/api/events-update.test.js -i</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Event complete/undo/update authorization paths use shared scope semantics and no longer false-deny eligible admin cross-owner actions in all/lens modes.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/items-net-status.test.js test/api/items-list-and-mutate.test.js test/api/events-complete.test.js test/api/events-update.test.js -i` and confirm admin all/lens mutation + net-status continuity scenarios pass.
</verification>

<success_criteria>
Admin scope semantics are uniform across item/event mutation and net-status drill-through paths, with regression coverage locking all-mode, owner-lens, and not_found-denial contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-scope-integration-hardening/13-01-SUMMARY.md`
</output>
