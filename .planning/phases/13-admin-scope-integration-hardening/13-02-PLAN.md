---
phase: 13-admin-scope-integration-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/src/lib/query-keys.ts
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/__tests__/item-detail-ledger.test.tsx
  - frontend/src/__tests__/admin-lens-hard-reset.test.tsx
autonomous: true
requirements:
  - TIME-04
  - AUTH-06
must_haves:
  truths:
    - "Admin lens transitions do not reuse stale item-detail or net-status cache entries from a previous scope."
    - "Dashboard/timeline drill-through into item detail preserves active admin all/lens context without manual refresh."
    - "Frontend regression tests fail if detail query keys or detail fetch params omit scope dimensions."
  artifacts:
    - path: "frontend/src/lib/query-keys.ts"
      provides: "Lens-scoped key contracts for item detail and related detail lookup queries"
      contains: "detail"
    - path: "frontend/src/pages/items/item-detail-page.tsx"
      provides: "Scope-aware item detail, net-status, and related lookup query wiring"
      contains: "useAdminScope"
    - path: "frontend/src/__tests__/item-detail-ledger.test.tsx"
      provides: "Detail-page regression coverage for scope-aware query execution and drill-through continuity"
      contains: "scope_mode"
  key_links:
    - from: "frontend/src/pages/items/item-detail-page.tsx"
      to: "frontend/src/lib/query-keys.ts"
      via: "Detail and lookup queries include mode/lens dimensions in query keys"
      pattern: "queryKeys.items.detail"
    - from: "frontend/src/pages/items/item-detail-page.tsx"
      to: "/items/:id/net-status"
      via: "Detail fetch executes under current admin scope and refetches on lens changes"
      pattern: "apiRequest<NetStatusResponse>"
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/pages/items/item-detail-page.tsx"
      via: "Lens transition cache reset plus scope-aware keys prevents stale drill-through detail state"
      pattern: "actorSensitiveQueryRoots"
---

<objective>
Harden frontend drill-through continuity so admin detail flows always match active all/lens scope.

Purpose: Close TIME-04 continuity gaps by making item-detail query partitioning explicitly scope-aware and regression-locked against stale cache bleed across lens switches.
Output: Lens-aware item detail query keys/fetch wiring and focused frontend regressions for admin drill-through transitions.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-scope-integration-hardening/13-RESEARCH.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-06-SUMMARY.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-04-SUMMARY.md
@frontend/src/lib/query-keys.ts
@frontend/src/pages/items/item-detail-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lens-aware detail query-key partitioning for item detail and lookup reads</name>
  <files>frontend/src/lib/query-keys.ts, frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Extend query key contracts so item detail and detail-adjacent lookup reads include admin scope dimensions (`mode`, `lensUserId`) similar to existing dashboard/events/items list keying. Wire item detail page queries (`/items/:id/net-status`, detail lookup `/items?filter=all...`, and ledger fetch params) to the active `useAdminScope` state so lens switches trigger deterministic refetches for the same route id. Preserve existing route paths, payload shape, and UX sections; this hardening changes cache partitioning and scope continuity only.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/admin-lens-hard-reset.test.tsx src/__tests__/item-detail-ledger.test.tsx</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Item detail queries are keyed by active admin scope, eliminating stale detail reuse across all/lens transitions for identical item routes.</done>
</task>

<task type="auto">
  <name>Task 2: Expand frontend regressions to lock admin drill-through scope continuity</name>
  <files>frontend/src/__tests__/item-detail-ledger.test.tsx, frontend/src/__tests__/admin-lens-hard-reset.test.tsx</files>
  <action>Add or extend tests to assert that switching admin scope and opening/remaining on item detail invokes scope-aware fetches and returns lens-consistent data (including same item id under different lens context). Include negative expectations proving old detail keys are not reused after lens transition. Keep assertions at behavior level (request params/query key usage and rendered owner-correct content) and avoid brittle implementation-only snapshots.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/item-detail-ledger.test.tsx src/__tests__/admin-lens-hard-reset.test.tsx && npm --prefix frontend build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Regression coverage reliably catches stale-lens drill-through regressions in item detail and validates continuity under admin all/lens switching.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend test -- src/__tests__/item-detail-ledger.test.tsx src/__tests__/admin-lens-hard-reset.test.tsx && npm --prefix frontend build` and confirm item detail drill-through behavior remains scope-consistent across lens changes.
</verification>

<success_criteria>
Admin drill-through into item detail and related ledger/net-status views remains consistent with current all/lens scope, with no stale cache bleed and explicit regression locks.
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-scope-integration-hardening/13-02-SUMMARY.md`
</output>
