---
phase: 22-operator-deployment-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Operator has one README runbook that starts with pinned-tag image publish and then proceeds to Portainer deployment."
    - "Operator can distinguish first-time deployment vs update/redeploy and execute both without guessing missing steps."
    - "Operator can copy one canonical Portainer environment block and identify required, optional, and derived values with placeholders only."
    - "Operator understands DATABASE_URL encoding rules, including ! -> %21, before entering stack variables."
    - "Operator can trigger rollback to a previous known-good image tag using the documented procedure."
  artifacts:
    - path: "README.md"
      provides: "Single operator-facing production deployment runbook with environment contract and deterministic deployment flows"
      contains: "## Production Deployment (Ugreen NAS + Portainer)"
  key_links:
    - from: "README.md"
      to: ".github/workflows/publish-prod-images.yml"
      via: "runbook publish-first step references workflow dispatch and image_tag input"
      pattern: "Publish Production Images|image_tag"
    - from: "README.md"
      to: "docker-compose.prod.yml"
      via: "Portainer stack deployment steps reference canonical compose file"
      pattern: "docker-compose\.prod\.yml"
    - from: "README.md"
      to: ".env.example"
      via: "environment variable list mirrors stack variable contract and placeholders"
      pattern: "GHCR_OWNER|IMAGE_TAG|NAS_STATIC_IP|DB_PASSWORD|JWT_SECRET|SESSION_SECRET|HACT_ADMIN_EMAIL"
---

<objective>
Create the core production operator runbook in `README.md` with deterministic publish/deploy/update/rollback flows and an explicit Portainer environment variable contract.

Purpose: Satisfy DOCS-01 by making one canonical deployment path that operators can execute end-to-end without missing required stack configuration.
Output: Updated `README.md` containing production deployment scope, required variables, and first-time/update/rollback procedures aligned to existing GHCR + Portainer contracts.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-operator-deployment-documentation/22-CONTEXT.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-01-SUMMARY.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-02-SUMMARY.md
@README.md
@.env.example
@docker-compose.prod.yml
@.github/workflows/publish-prod-images.yml
@scripts/deploy/publish-ghcr-images.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace README top-level path with production operator deployment scope</name>
  <files>README.md</files>
  <action>Restructure `README.md` around a new primary section `## Production Deployment (Ugreen NAS + Portainer)` and make this the canonical path for operators. Explicitly state pinned image tags are required (no floating `latest` for deployment/recovery) and place GHCR publish guidance before any Portainer deployment steps. Keep language operational and deterministic; avoid introducing alternate deployment tracks that bypass the pinned-tag flow.</action>
  <verify>
    <automated>rg -n "Production Deployment \(Ugreen NAS \+ Portainer\)|pinned|latest" README.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`README.md` clearly establishes one production runbook path with publish-first and pinned-tag policy visible near the top of the deployment section.</done>
</task>

<task type="auto">
  <name>Task 2: Document exact Portainer environment variables in tiered groups and canonical block</name>
  <files>README.md</files>
  <action>Add an environment contract section with three explicit groups: required core, optional tuning, and derived values. Include one canonical copy-paste block that uses placeholders only (no live secrets) for Portainer stack input and document `DATABASE_URL` encoding rules with a concrete `!` to `%21` example. Ensure required variables at minimum include `GHCR_OWNER`, `IMAGE_TAG`, `NAS_STATIC_IP`, `DB_PASSWORD`, `JWT_SECRET`, `SESSION_SECRET`, and `HACT_ADMIN_EMAIL`.</action>
  <verify>
    <automated>rg -n "Required core|Optional tuning|Derived values|GHCR_OWNER|IMAGE_TAG|NAS_STATIC_IP|DB_PASSWORD|JWT_SECRET|SESSION_SECRET|HACT_ADMIN_EMAIL|DATABASE_URL|%21" README.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>README provides an explicit, copy-ready variable contract where operators can identify every required Portainer value without guessing.</done>
</task>

<task type="auto">
  <name>Task 3: Add first-time deploy, update/redeploy, and rollback procedures with explicit commands</name>
  <files>README.md</files>
  <action>Write three separate procedures: first-time deployment, update/redeploy, and rollback to previous known-good tag. Put GitHub Actions publish/verification commands before Portainer stack actions, then provide Portainer Web Editor update steps tied to `docker-compose.prod.yml` and stack env variables. Include deterministic rollback flow that reuses previously published tags and does not use runtime hotfix shortcuts.</action>
  <verify>
    <automated>rg -n "First-time deployment|Update/redeploy|Rollback|Publish Production Images|docker-compose\.prod\.yml|Portainer" README.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>README contains complete and distinct procedures for initial deployment, standard updates, and rollback recovery using explicit image tags.</done>
</task>

</tasks>

<verification>
Run `rg -n "GHCR_OWNER|IMAGE_TAG|NAS_STATIC_IP|DB_PASSWORD|JWT_SECRET|SESSION_SECRET|HACT_ADMIN_EMAIL|First-time deployment|Update/redeploy|Rollback" README.md` and confirm all required operators inputs and flows are present in one document.
</verification>

<success_criteria>
- `README.md` presents one explicit production deployment runbook for NAS + Portainer.
- Required Portainer variables are listed in a deterministic section and provided as one placeholder-only copy block.
- First-time, update/redeploy, and rollback procedures are all documented with pinned-tag behavior.
- Documentation language and order align with Phase 22 locked decisions from `22-CONTEXT.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/22-operator-deployment-documentation/22-01-SUMMARY.md`
</output>
