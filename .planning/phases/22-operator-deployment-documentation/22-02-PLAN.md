---
phase: 22-operator-deployment-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Operator can diagnose documented deployment incidents by symptom and run exact checks with expected signatures."
    - "Operator has permanent-fix steps for session drop, /items route-not-found, Portainer deploy 500, and image-tag drift mismatch."
    - "Operator can execute gate-based deployment verification and interpret failures using expected status/body outcomes."
    - "Operator knows when to stop rollout and trigger rollback based on explicit criteria."
  artifacts:
    - path: "README.md"
      provides: "Symptom-driven troubleshooting and gate-based validation checklist for post-deploy operator checks"
      contains: "## Troubleshooting (Symptom -> Checks -> Fixes)"
  key_links:
    - from: "README.md"
      to: "SESSION_COOKIE_SECURE"
      via: "session-drop troubleshooting maps symptom to cookie-security root cause and permanent fix path"
      pattern: "SESSION_COOKIE_SECURE"
    - from: "README.md"
      to: "src/domain/items/financial-metrics.js"
      via: "route-not-found troubleshooting identifies missing backend module in image"
      pattern: "financial-metrics\.js|Route not found"
    - from: "README.md"
      to: "docker-compose.prod.yml"
      via: "verification gates and rollback criteria anchor to stack runtime behavior"
      pattern: "health|/api/auth/login|/api/items|postgres"
---

<objective>
Finish the operator runbook by adding incident-focused troubleshooting and a gate-based verification matrix with stop and rollback triggers.

Purpose: Complete DOCS-01 with operational clarity so deployment can be completed from README alone, including failure diagnosis and deterministic recovery decisions.
Output: `README.md` troubleshooting and verification sections that encode known incidents, expected signatures, and required deployment acceptance gates.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-operator-deployment-documentation/22-CONTEXT.md
@.planning/phases/22-operator-deployment-documentation/22-01-SUMMARY.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-VERIFICATION.md
@README.md
@docker-compose.prod.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add symptom-first troubleshooting catalog with exact diagnostics and permanent fixes</name>
  <files>README.md</files>
  <action>Create troubleshooting entries organized as Symptom -> Cause checks -> Exact fix commands/outcomes. Cover all required incidents from context: (1) login succeeds but session drops / protected route returns 401, (2) `/items` returns route-not-found due to missing backend module, (3) Portainer deploy returns `500`, and (4) image-tag drift between frontend/backend. For each entry, include concrete diagnostic commands and expected output signatures that let operators confirm root cause quickly. Keep fixes permanent and deployment-safe; exclude temporary hotfix procedures.</action>
  <verify>
    <automated>rg -n "session|401|/items|Route not found|Portainer|500|image-tag drift|SESSION_COOKIE_SECURE|financial-metrics\.js" README.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>README contains all four required troubleshooting incidents with executable diagnostics, expected signatures, and permanent remediation paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add gate-based deployment verification table with stop conditions and rollback triggers</name>
  <files>README.md</files>
  <action>Add a verification section that uses a request/response table for mandatory gates: backend health, auth/session persistence behavior, protected-route expectation, item-create functional check, and persistence restart check. Each gate must define request, expected status/body signature, and failure interpretation. End the section with explicit stop conditions that block go-live and clear rollback trigger criteria tied to previous known-good tag redeploy.</action>
  <verify>
    <automated>rg -n "Verification gates|Request|Expected|Failure interpretation|/health|/api/auth/login|/api/items|persistence|Stop conditions|Rollback trigger" README.md</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Operators can decide pass/fail deployment status from README alone using mandatory gate checks and explicit rollback criteria.</done>
</task>

</tasks>

<verification>
Run `rg -n "Portainer deploy 500|Route not found|SESSION_COOKIE_SECURE|Verification gates|Stop conditions|Rollback trigger" README.md` and confirm every required incident and gate appears with expected signatures and interpretation guidance.
</verification>

<success_criteria>
- Troubleshooting is symptom-first and includes all four locked incidents with exact diagnostic commands.
- Verification is represented as gate-style request/response checks with failure interpretation.
- README explicitly defines stop conditions and rollback triggers for deterministic operator decisions.
- Documentation excludes deferred/private-secret cheat-sheet content and keeps placeholders in shared docs.
</success_criteria>

<output>
After completion, create `.planning/phases/22-operator-deployment-documentation/22-02-SUMMARY.md`
</output>
