---
phase: 21-portainer-stack-deployment-and-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish-prod-images.yml
  - scripts/deploy/publish-ghcr-images.sh
  - docker-compose.prod.yml
  - .env.example
  - .planning/phases/21-portainer-stack-deployment-and-persistence/21-VERIFICATION.md
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
gap_closure: true
must_haves:
  truths:
    - "Operator can publish backend and frontend production images to GHCR for a deployable release tag."
    - "Operator can deploy via Portainer Web Editor using `docker-compose.prod.yml` image tags only, without local `build:` directives."
    - "Pulled GHCR images start successfully, backend `/health` and frontend HTTP checks pass, and postgres sentinel data survives restart through `/volume1/docker/house-erp/db-data`."
  artifacts:
    - path: ".github/workflows/publish-prod-images.yml"
      provides: "CI path for GHCR build-and-push of backend/frontend production images"
    - path: "scripts/deploy/publish-ghcr-images.sh"
      provides: "CLI path for deterministic GHCR build-and-push workflow"
    - path: "docker-compose.prod.yml"
      provides: "Portainer-ready image-based deployment contract for frontend/backend/postgres"
    - path: ".planning/phases/21-portainer-stack-deployment-and-persistence/21-VERIFICATION.md"
      provides: "Runtime evidence from pull-based deployment and restart persistence probe"
  key_links:
    - from: ".github/workflows/publish-prod-images.yml"
      to: "ghcr.io"
      via: "docker/login-action and docker/build-push-action publish backend/frontend tags"
      pattern: "ghcr\.io/.*/house-erp-(backend|frontend)"
    - from: "scripts/deploy/publish-ghcr-images.sh"
      to: "ghcr.io"
      via: "docker login + docker buildx build --push for backend/frontend"
      pattern: "docker buildx build.*--push"
    - from: "docker-compose.prod.yml"
      to: "ghcr.io/${GHCR_OWNER}/house-erp-(frontend|backend):${IMAGE_TAG}"
      via: "Portainer stack deploy pulls remote images"
      pattern: "image: ghcr\.io/\$\{GHCR_OWNER\}/house-erp"
    - from: "docker-compose.prod.yml"
      to: "/volume1/docker/house-erp/db-data"
      via: "postgres bind mount exercised by restart probe"
      pattern: "/volume1/docker/house-erp/db-data:/var/lib/postgresql/data"
---

<objective>
Close the Phase 21 deployment gap by implementing a GHCR-first image publishing flow and switching the Portainer compose contract to pull-only image tags, then verify restart persistence and health checks against pulled images.

Purpose: Honor the locked gap-closure decision to deploy through GHCR images in Portainer Web Editor (not local builds) while preserving deterministic NAS persistence guarantees.
Output: GHCR publish automation (`CI` + `CLI`), image-based `docker-compose.prod.yml` contract, and updated `21-VERIFICATION.md` with pull-based deployment/persistence evidence.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-CONTEXT.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-VERIFICATION.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-01-SUMMARY.md
@docker-compose.prod.yml
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GHCR-first production image publishing workflow (CI and CLI)</name>
  <files>.github/workflows/publish-prod-images.yml, scripts/deploy/publish-ghcr-images.sh</files>
  <action>Create `.github/workflows/publish-prod-images.yml` to build and push `backend` (`Dockerfile.prod`, repo root context) and `frontend` (`frontend/Dockerfile.prod`, frontend context) production images to `ghcr.io/${GHCR_OWNER}` with explicit tag input (`IMAGE_TAG`) and optional `latest` tagging policy. Add `scripts/deploy/publish-ghcr-images.sh` as a CLI equivalent that logs into GHCR, builds both images with the same naming/tag contract, and pushes them. Use GHCR-first flow per locked user decision for this gap closure (do not introduce Docker Hub/ECR alternatives).</action>
  <verify>
    <automated>GHCR_OWNER=example-owner IMAGE_TAG=phase21-gap GHCR_TOKEN=dummy-token bash scripts/deploy/publish-ghcr-images.sh --dry-run</automated>
    <sampling_rate>run once after workflow/script creation and before compose contract changes</sampling_rate>
  </verify>
  <done>Repo has one CI workflow and one CLI script that both target `ghcr.io/${GHCR_OWNER}/house-erp-backend:${IMAGE_TAG}` and `ghcr.io/${GHCR_OWNER}/house-erp-frontend:${IMAGE_TAG}` with deterministic build contexts and tags.</done>
</task>

<task type="auto">
  <name>Task 2: Switch Portainer production compose contract from local build to remote images</name>
  <files>docker-compose.prod.yml, .env.example</files>
  <action>Replace `frontend` and `backend` `build:` blocks in `docker-compose.prod.yml` with required `image:` references to GHCR tags (`ghcr.io/${GHCR_OWNER}/house-erp-frontend:${IMAGE_TAG}` and `ghcr.io/${GHCR_OWNER}/house-erp-backend:${IMAGE_TAG}`). Keep existing service names, health checks, env interpolation fail-fast behavior, API_URL contract, and postgres persistence mount unchanged. Update `.env.example` with required GHCR deployment variables (`GHCR_OWNER`, `IMAGE_TAG`) for Portainer Web Editor stack env input so deployment no longer requires local Dockerfile build capability.</action>
  <verify>
    <automated>GHCR_OWNER=example-owner IMAGE_TAG=phase21-gap NAS_STATIC_IP=192.168.1.50 FRONTEND_PORT=8085 BACKEND_PORT=8080 DB_NAME=hact_prod DB_USER=postgres DB_PASSWORD=testpass JWT_SECRET=test_jwt SESSION_SECRET=test_session HACT_ADMIN_EMAIL=admin@example.com docker compose -f docker-compose.prod.yml config</automated>
    <sampling_rate>run after compose/env updates</sampling_rate>
  </verify>
  <done>Rendered compose config resolves image tags for frontend/backend with no `build` requirement, and required env checklist includes GHCR tag owner/version inputs needed by Portainer operators.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pull-based deployment, restart persistence, and finalize Phase 21 verification</name>
  <files>.planning/phases/21-portainer-stack-deployment-and-persistence/21-VERIFICATION.md</files>
  <action>On a Docker-enabled NAS/host, run pull-first deployment using the updated compose contract (`docker compose pull` then `docker compose up -d` without `--build`), insert sentinel data in postgres, restart postgres (or full stack), and verify sentinel data survives restart. In the same run, verify backend `/health` returns 200 and frontend root is reachable. Capture concrete command output and update `21-VERIFICATION.md` to terminal status (`verified` if all checks pass, `diagnosed` if any fail) with explicit requirement traceability for `DEPL-01`, `DEPL-02`, and `DEPL-03` tied to GHCR-image deployment evidence.</action>
  <verify>
    <automated>GHCR_OWNER=example-owner IMAGE_TAG=phase21-gap NAS_STATIC_IP=192.168.1.50 FRONTEND_PORT=8085 BACKEND_PORT=8080 DB_NAME=hact_prod DB_USER=postgres DB_PASSWORD=testpass JWT_SECRET=test_jwt SESSION_SECRET=test_session HACT_ADMIN_EMAIL=admin@example.com docker compose -f docker-compose.prod.yml pull && docker compose -f docker-compose.prod.yml up -d && docker compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d hact_prod -c "create table if not exists stack_persistence_probe(value text); insert into stack_persistence_probe(value) values ('persist-ok-ghcr');" && docker compose -f docker-compose.prod.yml restart postgres && docker compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d hact_prod -c "select value from stack_persistence_probe order by ctid desc limit 1;" && docker compose -f docker-compose.prod.yml exec -T backend node -e "fetch('http://127.0.0.1:8080/health').then((r)=>{if(r.status!==200)process.exit(1);}).catch(()=>process.exit(1));" && docker compose -f docker-compose.prod.yml exec -T frontend wget -qO- http://127.0.0.1/ >/dev/null && node "C:/Users/bryan/.config/opencode/get-shit-done/bin/gsd-tools.cjs" verify phase .planning/phases/21-portainer-stack-deployment-and-persistence --json</automated>
    <sampling_rate>run once after task edits; rerun only if any command fails</sampling_rate>
  </verify>
  <done>`21-VERIFICATION.md` includes pull-based GHCR deployment evidence, restart continuity proof for `/volume1/docker/house-erp/db-data`, and a non-uncertain terminal status with explicit DEPL-01/02/03 coverage.</done>
</task>

</tasks>

<verification>
The gap is closed when operators can publish release-tagged frontend/backend images to GHCR, deploy through Portainer Web Editor using `image:` tags only, and persistence/health evidence is recorded from pulled-image runtime.
</verification>

<success_criteria>
- GHCR publish path exists in both CI and CLI forms and uses one canonical tag contract for frontend/backend production images.
- `docker-compose.prod.yml` supports Portainer Web Editor deployment without local `build:` directives for frontend/backend services.
- Pull-based deployment verification proves backend/frontend health and postgres restart persistence; `21-VERIFICATION.md` ends in terminal status with DEPL-01/02/03 traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/21-portainer-stack-deployment-and-persistence/21-02-SUMMARY.md`
</output>
