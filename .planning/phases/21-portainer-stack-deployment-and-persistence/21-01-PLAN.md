---
phase: 21-portainer-stack-deployment-and-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - .env.example
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03
must_haves:
  truths:
    - "Operator can deploy frontend, backend, and postgres together from docker-compose.prod.yml in Portainer."
    - "Deployed stack is managed under one canonical long-lived stack identity (`house-erp-prod`) for in-place update/redeploy workflows."
    - "Portainer-provided stack environment variables are consumed by compose service definitions, including API_URL=http://${NAS_STATIC_IP}:8085/api."
    - "Postgres data remains available after container and stack restarts because storage is bound to /volume1/docker/house-erp/db-data."
    - "Initial deployment has explicit frontend reachability and backend health checks after DB readiness and migrations."
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Three-service production stack contract with env mapping and NAS persistence"
    - path: ".env.example"
      provides: "Canonical required variable checklist for Portainer stack environment values"
  key_links:
    - from: "docker-compose.prod.yml"
      to: "Dockerfile.prod"
      via: "backend service build uses production backend image contract"
      pattern: "backend:|dockerfile: Dockerfile.prod"
    - from: "docker-compose.prod.yml"
      to: "frontend/Dockerfile.prod"
      via: "frontend service build uses production frontend image contract"
      pattern: "frontend:|dockerfile: frontend/Dockerfile.prod"
    - from: "docker-compose.prod.yml"
      to: "API_URL=http://${NAS_STATIC_IP}:8085/api"
      via: "compose environment interpolation from Portainer stack vars"
      pattern: "API_URL=http://\$\{NAS_STATIC_IP\}:8085/api"
    - from: "docker-compose.prod.yml"
      to: "/volume1/docker/house-erp/db-data"
      via: "postgres host bind mount for persistent storage"
      pattern: "/volume1/docker/house-erp/db-data:/var/lib/postgresql/data"
---

<objective>
Create a Portainer-ready production compose stack with deterministic environment mapping and NAS-persistent Postgres data.

Purpose: Fulfill DEPL-01, DEPL-02, and DEPL-03 by making production deployment a single-stack operation that is environment-driven and restart-safe on Ugreen NAS.
Output: New `docker-compose.prod.yml` plus aligned env template checklist proving deployability, env injection behavior, and persistence continuity.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-portainer-stack-deployment-and-persistence/21-CONTEXT.md
@.planning/phases/19-environment-driven-production-configuration/19-01-SUMMARY.md
@.planning/phases/19-environment-driven-production-configuration/19-02-SUMMARY.md
@.planning/phases/20-production-container-build-and-gateway-routing/20-01-SUMMARY.md
@.planning/phases/20-production-container-build-and-gateway-routing/20-02-SUMMARY.md
@Dockerfile.prod
@frontend/Dockerfile.prod
@frontend/docker-entrypoint.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Portainer-ready three-service production compose stack</name>
  <files>docker-compose.prod.yml</files>
  <action>Create root `docker-compose.prod.yml` with exactly three services (`frontend`, `backend`, `postgres`) and production-safe defaults for Portainer deployment. Set the compose project identity to canonical stack name `house-erp-prod` so Portainer updates run as in-place redeploys on one long-lived stack. Build frontend/backend from existing production Dockerfiles, wire backend startup through the established preflight image contract, and configure dependency gating so backend waits for postgres health. Publish frontend on `${FRONTEND_PORT:-8085}` and backend on `${BACKEND_PORT:-8080}` to satisfy NAS host reachability and existing frontend gateway assumptions. Keep service names stable across updates so DB volume continuity is preserved.</action>
  <verify>
    <automated>NAS_STATIC_IP=192.168.1.50 FRONTEND_PORT=8085 BACKEND_PORT=8080 DB_NAME=hact_prod DB_USER=postgres DB_PASSWORD=stack_test JWT_SECRET=test_jwt_secret SESSION_SECRET=test_session_secret HACT_ADMIN_EMAIL=admin@example.com docker compose -f docker-compose.prod.yml config</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`docker-compose.prod.yml` renders valid compose output and defines deployable `frontend`, `backend`, and `postgres` services for Portainer.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce Portainer environment contract mapping including API_URL</name>
  <files>docker-compose.prod.yml, .env.example</files>
  <action>Map required Portainer stack variables into service environments with clear interpolation paths and fail-fast required markers for missing critical values (`NAS_STATIC_IP`, `DB_PASSWORD`, `JWT_SECRET`, `SESSION_SECRET`, `HACT_ADMIN_EMAIL`). Explicitly include `API_URL=http://${NAS_STATIC_IP}:8085/api` in compose env mapping per locked phase contract and keep `FRONTEND_ORIGIN=http://${NAS_STATIC_IP}:${FRONTEND_PORT}` aligned with backend CORS behavior. Update `.env.example` to hold one canonical Phase 21 checklist of required production stack variables and stable example values; keep optional surface minimal to preserve deterministic runtime behavior.</action>
  <verify>
    <automated>NAS_STATIC_IP=192.168.1.50 FRONTEND_PORT=8085 BACKEND_PORT=8080 DB_NAME=hact_prod DB_USER=postgres DB_PASSWORD=stack_test JWT_SECRET=test_jwt_secret SESSION_SECRET=test_session_secret HACT_ADMIN_EMAIL=admin@example.com docker compose -f docker-compose.prod.yml config | rg "API_URL: http://192.168.1.50:8085/api|FRONTEND_ORIGIN: http://192.168.1.50:8085|HACT_ADMIN_EMAIL: admin@example.com"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Compose output proves Portainer-provided env values are injected into service contracts, including required `API_URL` mapping.</done>
</task>

<task type="auto">
  <name>Task 3: Wire NAS bind-mount persistence and prove restart continuity</name>
  <files>docker-compose.prod.yml</files>
  <action>Configure postgres persistence with host bind mount `/volume1/docker/house-erp/db-data:/var/lib/postgresql/data` (not named volumes) to lock NAS storage path requirements. Ensure stack contract preserves mount continuity under in-place redeploy and restart flows. Add command-driven verification that writes sentinel data, restarts postgres (or full stack), and confirms data still exists. Include both backend `/health` and frontend HTTP reachability checks so deployment success is measured as user entrypoint + API readiness, not just container running state.</action>
  <verify>
    <automated>NAS_STATIC_IP=192.168.1.50 FRONTEND_PORT=8085 BACKEND_PORT=8080 DB_NAME=hact_prod DB_USER=postgres DB_PASSWORD=stack_test JWT_SECRET=test_jwt_secret SESSION_SECRET=test_session_secret HACT_ADMIN_EMAIL=admin@example.com docker compose -f docker-compose.prod.yml up -d --build && docker compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d hact_prod -c "create table if not exists stack_persistence_probe(value text); insert into stack_persistence_probe(value) values ('persist-ok');" && docker compose -f docker-compose.prod.yml restart postgres && docker compose -f docker-compose.prod.yml exec -T postgres psql -U postgres -d hact_prod -c "select value from stack_persistence_probe order by ctid desc limit 1;" && docker compose -f docker-compose.prod.yml exec -T backend node -e "fetch('http://127.0.0.1:8080/health').then((r)=>{if(r.status!==200)process.exit(1);}).catch(()=>process.exit(1));" && docker compose -f docker-compose.prod.yml exec -T frontend wget -qO- http://127.0.0.1/ >/dev/null && docker compose -f docker-compose.prod.yml ps</automated>
    <manual>If Docker daemon is unavailable in the execution environment, run this sequence on the NAS host and confirm the final query returns `persist-ok` and backend health remains `healthy` after restart.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Postgres data survives restart using the required NAS host path, backend health is green, and frontend remains reachable after restart.</done>
</task>

</tasks>

<verification>
Run `docker compose -f docker-compose.prod.yml config` with a full env set, then execute the persistence probe sequence to confirm host-env mapping and NAS-backed Postgres restart continuity.
</verification>

<success_criteria>
- `docker-compose.prod.yml` exists and deploys `frontend`, `backend`, and `postgres` as one production stack.
- Portainer environment variables map into stack services, including `API_URL=http://${NAS_STATIC_IP}:8085/api`.
- Postgres writes persist across restart through `/volume1/docker/house-erp/db-data`.
- Backend startup remains health-gated behind DB readiness and migrations during first deployment and restart cycles, and frontend entrypoint is reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/21-portainer-stack-deployment-and-persistence/21-01-SUMMARY.md`
</output>
