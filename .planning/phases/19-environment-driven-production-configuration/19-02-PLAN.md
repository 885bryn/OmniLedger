---
phase: 19-environment-driven-production-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/config/network-targets.js
  - src/api/app.js
  - frontend/src/lib/api-client.ts
  - frontend/src/features/export/use-export-backup.ts
  - frontend/src/__tests__/user-switcher-export-action.test.tsx
  - .env.example
  - test/api/app-network-env.test.js
autonomous: true
requirements:
  - ENV-01
must_haves:
  truths:
    - "Operators can set NAS_STATIC_IP externally and backend network targets resolve from environment-derived values in production."
    - "Frontend API calls resolve from environment-driven production network config instead of hardcoded localhost defaults."
    - "Environment template exposes required network variables so operators know what to set before deployment."
  artifacts:
    - path: "src/config/network-targets.js"
      provides: "Shared production network target resolver sourced from NAS_STATIC_IP"
      exports: ["resolveNetworkTargets"]
    - path: "src/api/app.js"
      provides: "CORS allowed-origin resolution that uses environment-derived production targets"
      exports: ["createApp"]
    - path: "frontend/src/lib/api-client.ts"
      provides: "Frontend API base URL resolution from production env vars (with explicit override support)"
      exports: ["apiRequest"]
    - path: ".env.example"
      provides: "Operator-facing env contract documenting NAS-driven network variables"
    - path: "test/api/app-network-env.test.js"
      provides: "Regression coverage for env-driven backend origin resolution"
  key_links:
    - from: "src/api/app.js"
      to: "src/config/network-targets.js"
      via: "allowed origin resolution sourced from NAS_STATIC_IP-aware config"
      pattern: "resolveNetworkTargets|FRONTEND_ORIGIN|NAS_STATIC_IP"
    - from: "frontend/src/lib/api-client.ts"
      to: "import.meta.env"
      via: "API base URL derivation from VITE_API_BASE_URL/VITE_NAS_STATIC_IP"
      pattern: "VITE_API_BASE_URL|VITE_NAS_STATIC_IP"
    - from: "frontend/src/features/export/use-export-backup.ts"
      to: "frontend/src/lib/api-client.ts"
      via: "shared base URL source-of-truth used by export download request"
      pattern: "API_BASE_URL|resolve"
---

<objective>
Externalize production network targeting so frontend and backend derive runtime addresses from operator-supplied environment configuration.

Purpose: Fulfill ENV-01 by replacing hardcoded localhost-style production assumptions with environment-driven NAS target resolution across API boundary and browser transport calls.
Output: Backend network target resolver, frontend env-based API URL derivation, updated env template guidance, and network-focused regression coverage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-environment-driven-production-configuration/19-CONTEXT.md
@src/api/app.js
@frontend/src/lib/api-client.ts
@frontend/src/features/export/use-export-backup.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend NAS-driven network target resolver and wire CORS origin policy</name>
  <files>src/config/network-targets.js, src/api/app.js, test/api/app-network-env.test.js</files>
  <action>Create a backend network config helper that resolves production targets from `NAS_STATIC_IP` (with optional explicit `FRONTEND_ORIGIN` override) and keeps current developer-local defaults for non-production. Wire `createApp` CORS allowlist resolution through that helper so production origin behavior is environment-driven and deterministic. Add API tests proving production behavior tracks env configuration and no hardcoded localhost fallback is used when `NODE_ENV=production`.</action>
  <verify>
    <automated>npm test -- test/api/app-network-env.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Backend network-origin behavior is environment-driven for production and regression-locked.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize frontend production API URL derivation from env</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/features/export/use-export-backup.ts, frontend/src/__tests__/user-switcher-export-action.test.tsx</files>
  <action>Replace hardcoded frontend API localhost defaults with a shared env-driven resolution path: prefer explicit `VITE_API_BASE_URL`, otherwise derive from `VITE_NAS_STATIC_IP` for production-style deployments while preserving test determinism. Reuse one source of truth so export download and general API requests stay aligned. Update focused frontend tests to assert requests use resolved env-based base URL behavior.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend transport paths are driven by environment configuration and no longer tied to baked-in production host assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh env template with NAS network contract and operator hints</name>
  <files>.env.example</files>
  <action>Update `.env.example` to include Phase 19 production-focused network contract variables (`NAS_STATIC_IP`, frontend/backend derived target hints, and existing required identity/DB variables) with comments that direct operators to set values via Portainer stack environment configuration. Keep examples non-sensitive and avoid checked-in real credentials.</action>
  <verify>
    <automated>npm test -- test/api/app-network-env.test.js --runInBand</automated>
    <manual>Open `.env.example` and confirm NAS/static IP, admin email, and DB password are all documented with Portainer-oriented setup hints and no secret samples.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Operator env template clearly documents the NAS-driven production network configuration contract.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/app-network-env.test.js --runInBand` and `npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand` and confirm backend/frontend requests resolve network targets from environment variables.
</verification>

<success_criteria>
- Backend CORS/network target behavior in production derives from `NAS_STATIC_IP` and configured env values.
- Frontend API requests resolve from environment configuration rather than hardcoded localhost defaults.
- `.env.example` clearly lists NAS-driven network configuration expected for production preparation.
</success_criteria>

<output>
After completion, create `.planning/phases/19-environment-driven-production-configuration/19-02-SUMMARY.md`
</output>
