---
phase: 19-environment-driven-production-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/production-env.js
  - src/scripts/startup.js
  - src/config/database.js
  - src/api/routes/auth.routes.js
  - test/config/production-env.test.js
  - test/scripts/startup-production-env.test.js
  - test/api/auth-role-session.test.js
autonomous: true
requirements:
  - ENV-02
  - ENV-03
  - ENV-04
must_haves:
  truths:
    - "Startup fails immediately in production when required env vars are missing or invalid."
    - "Startup logs list every missing/invalid required variable in one pass with Portainer-focused fix hints."
    - "Admin identity assignment is driven by HACT_ADMIN_EMAIL (no hidden fallback source)."
    - "Database authentication in production uses DB_PASSWORD from environment rather than checked-in defaults."
  artifacts:
    - path: "src/config/production-env.js"
      provides: "Central required-env parsing and aggregated validation errors for production startup"
      exports: ["readRequiredProductionEnv", "ProductionEnvValidationError"]
    - path: "src/scripts/startup.js"
      provides: "Fail-fast startup gate that validates required production env vars before DB wait/migrations"
    - path: "src/config/database.js"
      provides: "Production-safe DB config path that requires environment-provided credentials"
      exports: ["getDatabaseConfig"]
    - path: "src/api/routes/auth.routes.js"
      provides: "Configured admin-role assignment sourced from HACT_ADMIN_EMAIL"
      exports: ["createAuthRouter"]
    - path: "test/config/production-env.test.js"
      provides: "Regression coverage for missing/invalid aggregation and secret-safe messaging"
  key_links:
    - from: "src/scripts/startup.js"
      to: "src/config/production-env.js"
      via: "startup preflight validation before DB readiness/migrations"
      pattern: "readRequiredProductionEnv|ProductionEnvValidationError"
    - from: "src/config/production-env.js"
      to: "process.env"
      via: "required variable parsing for NAS_STATIC_IP, HACT_ADMIN_EMAIL, DB_PASSWORD"
      pattern: "NAS_STATIC_IP|HACT_ADMIN_EMAIL|DB_PASSWORD"
    - from: "src/api/routes/auth.routes.js"
      to: "process.env.HACT_ADMIN_EMAIL"
      via: "admin role resolution during session user hydration"
      pattern: "HACT_ADMIN_EMAIL"
---

<objective>
Establish a production-safe environment contract that blocks bad startup and guarantees identity/database secrets are externalized.

Purpose: Fulfill ENV-02/03/04 by enforcing required production env inputs with explicit operator-facing diagnostics instead of fallback behavior that can mask misconfiguration.
Output: Shared production env validator, startup fail-fast wiring, admin-email role resolution hardening, and focused backend regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-environment-driven-production-configuration/19-CONTEXT.md
@src/scripts/startup.js
@src/config/database.js
@src/api/routes/auth.routes.js
@test/api/auth-role-session.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create aggregated production env validator with actionable, secret-safe diagnostics</name>
  <files>src/config/production-env.js, test/config/production-env.test.js</files>
  <action>Create `readRequiredProductionEnv` that validates `NAS_STATIC_IP`, `HACT_ADMIN_EMAIL`, and `DB_PASSWORD` for production usage. Validation must aggregate all missing/invalid issues in one pass and throw a typed error that includes variable names plus short fix hints for Portainer stack environment variables. Treat blank values as missing, validate IP/email format, and reject placeholder-like secret values without logging actual secret samples. Keep message formatting deterministic for stable tests.</action>
  <verify>
    <automated>npm test -- test/config/production-env.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Validator exposes a reusable production env contract and emits one explicit, aggregated diagnostic output with no sensitive value leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Wire startup fail-fast gate and explicit validation logs</name>
  <files>src/scripts/startup.js, test/scripts/startup-production-env.test.js</files>
  <action>Run production env validation at startup preflight before DB readiness checks, migrations, or server boot. On validation failure, print explicit multiline diagnostics to stderr and exit non-zero so container startup fails clearly in Portainer logs. Keep non-production behavior compatible for local dev/test. Add focused startup tests that assert: no migration/server launch occurs when required env is invalid; all issues are reported together; error output includes variable names and fix hints but no secret values.</action>
  <verify>
    <automated>npm test -- test/scripts/startup-production-env.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Production startup now hard-fails early with explicit, operator-actionable validation logs and zero partial boot on bad config.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce env-driven admin identity and production DB auth paths</name>
  <files>src/api/routes/auth.routes.js, src/config/database.js, test/api/auth-role-session.test.js, test/config/production-env.test.js</files>
  <action>Update admin-email role resolution to use `HACT_ADMIN_EMAIL` as the canonical configured admin source for production behavior (retain no hidden production fallback). Harden database config so production postgres auth must come from environment-supplied values and does not silently default to checked-in credentials. Extend existing auth-role and env-config tests to lock these behaviors and prevent regression to implicit fallback credentials/identity mapping.</action>
  <verify>
    <automated>npm test -- test/api/auth-role-session.test.js test/config/production-env.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Admin role assignment and production database auth are both explicitly environment-driven and regression-guarded.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/config/production-env.test.js test/scripts/startup-production-env.test.js test/api/auth-role-session.test.js --runInBand` and confirm aggregated fail-fast validation, admin-email mapping, and production DB auth guardrails are locked.
</verification>

<success_criteria>
- Missing or invalid required production env variables always fail startup before DB wait/migration/server boot.
- Startup diagnostics list all detected env issues at once and include Portainer fix hints without exposing secret samples.
- `HACT_ADMIN_EMAIL` drives admin assignment behavior.
- Production DB auth paths do not rely on checked-in default credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/19-environment-driven-production-configuration/19-01-SUMMARY.md`
</output>
