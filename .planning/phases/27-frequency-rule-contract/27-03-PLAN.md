---
phase: 27-frequency-rule-contract
plan: "03"
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/domain/items/get-item-net-status.js
  - test/api/items-net-status.test.js
autonomous: true
gap_closure: true
requirements: [CASH-04]
must_haves:
  truths:
    - User does not see one-time rows due in future months counted in the current month's obligations or net cashflow.
    - User sees one-time rows included only when due date falls within the active monthly period, including year and month.
    - User sees recurring rows continue to behave with existing monthly baseline totals.
  artifacts:
    - path: src/domain/items/get-item-net-status.js
      provides: One-time inclusion gate that enforces full active-period date containment for monthly summaries
      contains: active_period
    - path: test/api/items-net-status.test.js
      provides: Regression coverage for cross-month one-time exclusion and in-period inclusion
      contains: one_time
  key_links:
    - from: src/domain/items/get-item-net-status.js
      to: test/api/items-net-status.test.js
      via: net-status summary assertions for one-time rows across period boundaries
      pattern: monthly_obligation_total|net_monthly_cashflow
---

<objective>
Close the diagnosed one-time period leak where future-dated one-time obligations appear in the current monthly summary.

Purpose: CASH-04 requires one-time handling to be period-bounded and predictable, and this regression breaks that contract by showing May due amounts in March totals.
Output: Tight backend logic correction for active-period inclusion and focused API regression tests proving future-month one-time rows stay excluded until their due period.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-frequency-rule-contract/27-CONTEXT.md
@.planning/phases/27-frequency-rule-contract/27-VERIFICATION.md
@.planning/phases/27-frequency-rule-contract/27-frequency-rule-contract-01-SUMMARY.md
@src/domain/items/get-item-net-status.js
@test/api/items-net-status.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix one-time active-period gate for future-month exclusion</name>
  <files>src/domain/items/get-item-net-status.js</files>
  <action>Update one-time summary inclusion logic so due-date checks are evaluated against the full active monthly period bounds (year, month, and inclusive day boundaries), not partial month matching or fallback paths that can leak future rows into current totals. Preserve recurring-row monthly behavior and existing malformed/null/zero amount guardrails. Keep response contract fields unchanged so UI integrations remain stable.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>A one-time obligation due on 2026-05-03 is excluded from March monthly obligations and net totals, while in-period one-time rows still include correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused regression tests for cross-period one-time behavior</name>
  <files>test/api/items-net-status.test.js</files>
  <action>Add a regression scenario that reproduces the diagnosed leak: monthly active period in March with a one-time obligation due in May must not affect `monthly_obligation_total` or `net_monthly_cashflow`. Pair it with a positive control case where a one-time obligation due inside March is included, so the contract verifies both exclusion and inclusion boundaries in one focused test section.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>API tests fail before the fix and pass after it, locking future-month exclusion for one-time rows while preserving in-period inclusion.</done>
</task>

</tasks>

<verification>
Run the net-status API suite and confirm the March summary excludes May one-time obligations, then confirm in-period one-time obligations still contribute once to monthly totals.
</verification>

<success_criteria>
The diagnosed regression is closed: one-time entries appear in monthly obligations only when their due date is inside the active month, maintaining CASH-04 predictability without changing non-summary workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/27-frequency-rule-contract/27-frequency-rule-contract-03-SUMMARY.md`
</output>
