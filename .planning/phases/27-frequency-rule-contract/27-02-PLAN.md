---
phase: 27-frequency-rule-contract
plan: "02"
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/item-detail-ledger.test.tsx
autonomous: true
requirements: [CASH-04]
must_haves:
  truths:
    - User sees obligation, income, and net cards labeled with explicit monthly period context instead of ambiguous monthly assumptions.
    - User sees a clear helper hint explaining that one-time values count only when due date is inside the displayed period.
    - User sees the same one-time rule explanation wherever asset net-status summary cards render in the item detail view.
  artifacts:
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Asset summary UI wired to backend active-period metadata and one-time rule hint
      contains: summaryRuleHint
    - path: frontend/src/locales/en/common.json
      provides: English labels and helper text for period-aware summary wording
      contains: summaryPeriodHint
    - path: frontend/src/locales/zh/common.json
      provides: Chinese labels and helper text for period-aware summary wording
      contains: summaryPeriodHint
    - path: frontend/src/__tests__/item-detail-ledger.test.tsx
      provides: UI regression checks for period context and one-time helper text rendering
      contains: summary
  key_links:
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: src/domain/items/get-item-net-status.js
      via: net-status summary metadata response consumption
      pattern: active_period|rule
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: frontend/src/locales/en/common.json
      via: translation keys for summary labels and helper text
      pattern: items\.detail\.(summary|period|oneTime)
---

<objective>
Make the frequency-rule contract visible and understandable in the asset summary UI.

Purpose: Backend consistency alone does not remove user confusion, so this plan surfaces active-period context and one-time behavior directly on summary cards with clear wording.
Output: Item detail summary cards and helper text wired to net-status metadata, bilingual copy updates, and focused frontend tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-frequency-rule-contract/27-CONTEXT.md
@.planning/phases/27-frequency-rule-contract/27-frequency-rule-contract-01-SUMMARY.md
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/locales/en/common.json
@frontend/src/locales/zh/common.json
@frontend/src/__tests__/item-detail-ledger.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render period-aware summary labels and one-time rule hint in item detail</name>
  <files>frontend/src/pages/items/item-detail-page.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json</files>
  <action>Update the asset summary card copy in `ItemDetailPage` to include explicit monthly period context from net-status metadata (for example, active month range) and add concise helper text that explains one-time rows count once when due date is inside the shown period. Keep wording educational but short per phase context, preserve existing card layout and interactions, and ensure net cashflow text explicitly states it derives from normalized income minus obligations. Use translation keys in both English and Chinese rather than hardcoded text, and keep fallback behavior safe if metadata is missing (render stable monthly wording without breaking).</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>The item detail summary clearly communicates active monthly period and one-time inclusion rules, with localized copy and no layout regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Lock UI behavior with period-context and one-time hint regression tests</name>
  <files>frontend/src/__tests__/item-detail-ledger.test.tsx</files>
  <action>Add or update tests so mocked net-status responses containing active-period metadata and one-time rule fields verify the summary area renders period-aware labels/helper text and still computes and displays obligation, income, and net values consistently. Include a fallback assertion path for responses without new metadata to prove existing workflows remain usable without crashes or required new user steps.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/item-detail-ledger.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Frontend tests prove phase-27 messaging appears when metadata exists and legacy-safe rendering still works when it does not.</done>
</task>

</tasks>

<verification>
Run focused item-detail frontend tests and typecheck, then confirm summary cards show monthly period context and the one-time due-period helper text without changing item/event workflows.
</verification>

<success_criteria>
Users can read one coherent monthly summary contract in the UI: period is explicit, one-time handling is visible, and net cashflow messaging is tied to normalized income minus obligations.
</success_criteria>

<output>
After completion, create `.planning/phases/27-frequency-rule-contract/27-frequency-rule-contract-02-SUMMARY.md`
</output>
