---
phase: 27-frequency-rule-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/items/get-item-net-status.js
  - test/api/items-net-status.test.js
autonomous: true
requirements: [CASH-04]
must_haves:
  truths:
    - User sees one-time commitments and one-time income count only when their due date falls inside the active monthly summary period.
    - User sees one-time rows outside the active period excluded from obligation, income, and net cashflow totals.
    - User sees malformed, null, or zero-value rows excluded consistently so totals stay predictable.
  artifacts:
    - path: src/domain/items/get-item-net-status.js
      provides: Net-status summary contract with one-time period filtering and active-period metadata
      contains: active_period
    - path: test/api/items-net-status.test.js
      provides: API coverage for one-time inclusion, exclusion, boundary inclusion, and invalid-amount guardrails
      contains: one_time
  key_links:
    - from: src/domain/items/get-item-net-status.js
      to: test/api/items-net-status.test.js
      via: net-status response summary assertions
      pattern: summary\.(monthly_obligation_total|monthly_income_total|net_monthly_cashflow)
---

<objective>
Define the backend frequency-rule contract for one-time rows in monthly summaries.

Purpose: Phase 27 must remove ad-hoc one-time handling before cadence switching is added, so this plan locks one deterministic period rule at the net-status source.
Output: Updated net-status summary computation with explicit monthly active-period boundaries and focused API tests proving one-time inclusion/exclusion and amount guardrails.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-frequency-rule-contract/27-CONTEXT.md
@src/domain/items/get-item-net-status.js
@test/api/items-net-status.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement monthly active-period one-time filtering in net-status summary</name>
  <files>src/domain/items/get-item-net-status.js</files>
  <action>Refactor `buildSummary` in `get-item-net-status` so one-time rows (obligation and income) are only counted when their due date lands inside the active monthly baseline period. Build calendar-based boundaries (start and end of month) and treat both boundaries as inclusive. Keep recurring rows on the existing monthly baseline behavior, but apply one shared decision path for both subtype directions (income and obligation) so net cashflow remains symmetric. Enforce guardrails from context: malformed/null/zero amounts are excluded from totals, and rows outside period are excluded from totals. Return active-period metadata and a stable one-time rule descriptor on `summary` so frontend can display period context and helper copy without duplicating logic.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>`GET /items/:id/net-status` emits deterministic monthly totals where one-time values are counted once by due period, excluded outside period, and summary metadata exposes active-period context for UI usage.</done>
</task>

<task type="auto">
  <name>Task 2: Add API regression coverage for one-time period and guardrail semantics</name>
  <files>test/api/items-net-status.test.js</files>
  <action>Add focused cases that prove the new contract: one-time commitments and one-time income with due dates inside the active month are included exactly once; one-time rows outside active month are excluded; due dates exactly on period start/end are included; malformed/null/zero amounts are excluded and do not skew net cashflow. Keep RBAC and wrong-root tests untouched, and assert new summary metadata fields so downstream UI can rely on a stable response shape.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>Net-status API tests lock the one-time monthly contract and prevent regressions to ad-hoc inclusion behavior.</done>
</task>

</tasks>

<verification>
Run the net-status API suite and confirm one-time rows are now bounded by inclusive calendar-month dates while invalid amounts are excluded from totals.
</verification>

<success_criteria>
The backend net-status contract now applies one-time handling predictably (inside-period include, outside-period exclude, invalid amounts excluded) and exposes active-period context needed for clear summary messaging.
</success_criteria>

<output>
After completion, create `.planning/phases/27-frequency-rule-contract/27-frequency-rule-contract-01-SUMMARY.md`
</output>
