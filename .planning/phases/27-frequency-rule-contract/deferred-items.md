# Deferred Items

- 2026-03-07: Pre-existing Jest haste-map collision warning from `.tmp/lockcheck/package.json` vs `frontend/package.json` appears during backend test runs. Out of scope for plan 27-01; no net-status contract behavior impact.
