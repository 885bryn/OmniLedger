---
phase: 24-theme-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/theme/theme-provider.tsx
  - frontend/src/__tests__/theme-provider.test.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/index.css
autonomous: true
requirements:
  - THEME-01
  - THEME-03
  - THEME-04
  - IMPL-01
  - IMPL-02
must_haves:
  truths:
    - "User gets light mode on first authenticated render when no saved theme exists, even if the device prefers dark mode."
    - "User's last explicit theme choice is restored on refresh from local storage."
    - "Theme boot behavior reads only the saved local choice or the built-in light fallback, never OS/browser preference."
  artifacts:
    - path: "frontend/src/features/theme/theme-provider.tsx"
      provides: "Reusable theme provider and hook that own theme state, local persistence, and document class initialization"
      exports: ["ThemeProvider", "useTheme"]
    - path: "frontend/src/app/providers.tsx"
      provides: "Authenticated app provider tree wrapped by the theme provider before router rendering"
      exports: ["AppProviders"]
    - path: "frontend/src/index.css"
      provides: "Light and dark CSS variable sets plus restrained theme transition rules for shell surfaces"
      contains: ".dark"
    - path: "frontend/src/__tests__/theme-provider.test.tsx"
      provides: "Regression coverage for default light boot, persisted restore, and OS-preference ignore behavior"
  key_links:
    - from: "frontend/src/features/theme/theme-provider.tsx"
      to: "document.documentElement"
      via: "class/data-theme sync plus localStorage persistence"
      pattern: "classList|dataset|localStorage"
    - from: "frontend/src/app/providers.tsx"
      to: "frontend/src/features/theme/theme-provider.tsx"
      via: "provider wrapping QueryClient/Auth/AdminScope/Toast/Router composition"
      pattern: "ThemeProvider"
    - from: "frontend/src/__tests__/theme-provider.test.tsx"
      to: "frontend/src/features/theme/theme-provider.tsx"
      via: "assertions for boot theme, persisted restore, and no matchMedia dependency"
      pattern: "light|dark|localStorage|matchMedia"
---

<objective>
Establish the theme state foundation so OmniLedger always boots into a deterministic light-first theme and restores only explicit user choices.

Purpose: Fulfill the phase's initialization and persistence guarantees before any shell toggle UI is added, while preserving the milestone rule that OS/browser preference must not influence the app.
Output: A reusable theme provider/hook, provider-tree integration, global light/dark CSS variable scaffolding, and focused regression coverage for boot/persistence behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-theme-foundation/24-CONTEXT.md
@frontend/src/app/providers.tsx
@frontend/src/index.css
@frontend/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the light-first theme provider with persisted explicit choice rules</name>
  <files>frontend/src/features/theme/theme-provider.tsx, frontend/src/__tests__/theme-provider.test.tsx</files>
  <action>Create `ThemeProvider` and `useTheme` under `frontend/src/features/theme/` with one explicit local-storage key for the user's theme choice. Default to `light` when no saved value exists, restore `dark` or `light` only when previously saved, and sync the active theme onto `document.documentElement` using the class-based Tailwind contract already present in `frontend/tailwind.config.ts`. Do not read `window.matchMedia`, `prefers-color-scheme`, or attach OS/browser theme listeners. Add a focused Vitest regression file that proves first boot stays light without saved state, saved choice is restored on mount, and provider toggles only through explicit provider actions.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/theme-provider.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Theme state is reusable, persisted locally, deterministic on boot, and regression-locked against OS-driven behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire theme foundation into app boot and global surface variables</name>
  <files>frontend/src/app/providers.tsx, frontend/src/index.css</files>
  <action>Wrap the existing app provider tree with `ThemeProvider` so all authenticated screens share one theme source of truth before the router renders. Extend `frontend/src/index.css` with a `.dark` variable block for the future dual-theme token rollout while keeping light variables as the initial baseline. Add restrained, utility-app theme transitions for background, text, border, and shadow-related properties so theme changes feel polished and Mac-like without introducing dramatic animations, toasts, or Framer Motion ownership that belongs to later phases.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/theme-provider.test.tsx --runInBand && npm --prefix frontend run typecheck</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The app boots through a shared theme provider, light and dark variable sets exist for the shell, and theme changes use subtle global transitions only.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend test -- src/__tests__/theme-provider.test.tsx --runInBand` and `npm --prefix frontend run typecheck`; confirm the test suite proves default light boot, saved-choice restore, and no `matchMedia`-driven theme behavior.
</verification>

<success_criteria>
- No-saved-state boot resolves to light mode only.
- A saved explicit theme value is restored on refresh through local storage.
- Theme initialization stays class-driven and ignores OS/browser preference APIs.
- The authenticated app provider tree exposes one reusable theme boundary for later shell controls.
</success_criteria>

<output>
After completion, create `.planning/phases/24-theme-foundation/24-01-SUMMARY.md`
</output>
