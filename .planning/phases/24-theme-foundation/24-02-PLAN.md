---
phase: 24-theme-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - frontend/src/app/shell/theme-toggle.tsx
  - frontend/src/app/shell/app-shell.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/theme-toggle-shell.test.tsx
autonomous: true
requirements:
  - THEME-02
  - THEME-03
  - IMPL-01
must_haves:
  truths:
    - "User can intentionally switch between light and dark themes from the authenticated app chrome."
    - "Desktop exposes the theme control as a compact icon button in the header's top-right action area, while smaller screens expose theme access inside the mobile menu."
    - "Theme changes update immediately from direct toggle interaction, reflect the active state in the control icon, and do not show extra confirmation UI."
  artifacts:
    - path: "frontend/src/app/shell/theme-toggle.tsx"
      provides: "Reusable compact icon-button toggle bound to theme context with active-state icon feedback"
      exports: ["ThemeToggle"]
    - path: "frontend/src/app/shell/app-shell.tsx"
      provides: "Global authenticated chrome wiring for desktop header placement and mobile-menu theme access"
      exports: ["AppShell"]
    - path: "frontend/src/locales/en/common.json"
      provides: "English shell copy for theme toggle labels and mobile theme access"
    - path: "frontend/src/locales/zh/common.json"
      provides: "Chinese shell copy for theme toggle labels and mobile theme access"
    - path: "frontend/src/__tests__/theme-toggle-shell.test.tsx"
      provides: "Regression coverage for manual theme switching and shell-level toggle placement/wiring"
  key_links:
    - from: "frontend/src/app/shell/theme-toggle.tsx"
      to: "frontend/src/features/theme/theme-provider.tsx"
      via: "useTheme hook and explicit set/toggle actions"
      pattern: "useTheme|setTheme|toggleTheme"
    - from: "frontend/src/app/shell/app-shell.tsx"
      to: "frontend/src/app/shell/theme-toggle.tsx"
      via: "desktop header action slot and mobile menu action slot"
      pattern: "ThemeToggle"
    - from: "frontend/src/__tests__/theme-toggle-shell.test.tsx"
      to: "frontend/src/app/shell/app-shell.tsx"
      via: "assertions for icon toggle behavior, local persistence, and mobile-menu access path"
      pattern: "dark|light|menu|theme"
---

<objective>
Add the authenticated shell controls that let users intentionally switch themes without expanding scope into the broader dashboard visual rollout.

Purpose: Fulfill the phase's manual-toggle UX decisions exactly by placing a compact icon toggle in the desktop header, moving theme access into the mobile menu, and keeping the interaction polished but restrained.
Output: A reusable shell theme-toggle component, localized labels, global app-shell placement for desktop/mobile, and regression coverage for manual switching behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-theme-foundation/24-CONTEXT.md
@.planning/phases/24-theme-foundation/24-01-SUMMARY.md
@frontend/src/app/shell/app-shell.tsx
@frontend/src/app/shell/language-switcher.tsx
@frontend/src/app/shell/user-switcher.tsx
@frontend/src/locales/en/common.json
@frontend/src/locales/zh/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a compact icon-based theme toggle with active-state feedback</name>
  <files>frontend/src/app/shell/theme-toggle.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json, frontend/src/__tests__/theme-toggle-shell.test.tsx</files>
  <action>Create a reusable `ThemeToggle` component for the authenticated shell that consumes `useTheme` and renders a compact icon button, not a labeled switch or segmented control, per the locked user decision. Change the icon to reflect the active theme, expose localized accessible labels/tooltips for switching to the opposite theme, and keep interaction feedback subtle with restrained CSS transitions only. Do not add toast/snackbar confirmation or any extra state beyond the theme provider's persisted choice. In the same task, create the initial `theme-toggle-shell` Vitest scaffold that exercises the standalone toggle's manual light/dark switching behavior and accessible label/icon-state contract so the task's verification command is runnable before shell wiring lands in Task 2.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/theme-toggle-shell.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The shell has a reusable icon-button theme control with localized accessible text and active-theme icon feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Wire desktop header and mobile-menu theme access into the authenticated shell</name>
  <files>frontend/src/app/shell/app-shell.tsx, frontend/src/__tests__/theme-toggle-shell.test.tsx</files>
  <action>Integrate `ThemeToggle` into `AppShell` so desktop users see it in the header's top-right action cluster beside existing shell controls, while smaller screens get theme access inside the mobile menu/sidebar instead of a permanently visible header control. Keep the toggle available from the global authenticated chrome on every signed-in screen, preserve the utility-dashboard feel, and ensure manual toggle interaction updates the active theme immediately through the provider with persisted restore on remount. Add shell-level regression coverage that exercises manual light/dark switching, proves the desktop header and mobile menu wiring paths exist, and confirms no OS/browser preference logic is involved.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/theme-toggle-shell.test.tsx --runInBand && npm --prefix frontend run typecheck</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Users can switch themes from the authenticated shell with the exact desktop/mobile placement rules from context, and the behavior is regression-locked.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend test -- src/__tests__/theme-toggle-shell.test.tsx --runInBand` and `npm --prefix frontend run typecheck`; confirm the shell toggle changes theme manually, persists the choice, and exposes desktop-header plus mobile-menu access without any toast confirmation.
</verification>

<success_criteria>
- The authenticated shell exposes a compact icon toggle in the desktop header's top-right actions.
- Mobile users access theme switching from the mobile menu instead of a visible header toggle.
- Manual toggle interaction is the only runtime source of theme changes after boot, and the control icon reflects the active theme.
- No extra confirmation UI is shown after switching themes.
</success_criteria>

<output>
After completion, create `.planning/phases/24-theme-foundation/24-02-SUMMARY.md`
</output>
