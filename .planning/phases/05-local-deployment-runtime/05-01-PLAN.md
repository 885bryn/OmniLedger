---
phase: 05-local-deployment-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/app.js
  - src/api/server.js
  - src/scripts/startup.js
  - package.json
  - test/api/health.test.js
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "API process starts as a network-reachable service and binds to 0.0.0.0 on the configured port."
    - "API startup does not fail-fast when PostgreSQL is still booting; it retries until DB is reachable."
    - "Health checks expose API readiness using an HTTP endpoint that reflects database connectivity."
  artifacts:
    - path: "src/api/server.js"
      provides: "Runtime process entrypoint that starts Express on 0.0.0.0 with configurable port"
      exports:
        - "startServer"
    - path: "src/scripts/startup.js"
      provides: "Wait/retry boot sequence that runs Sequelize migrations against DATABASE_URL before server start"
      contains: "sequelize-cli db:migrate --url"
    - path: "src/api/app.js"
      provides: "Health route that reports readiness and fails unhealthy when DB connectivity is unavailable"
      contains: "GET /health"
    - path: "test/api/health.test.js"
      provides: "Executable verification for health endpoint success and DB-readiness failure behavior"
      contains: "GET /health"
  key_links:
    - from: "src/scripts/startup.js"
      to: "DATABASE_URL"
      via: "migration command uses explicit URL target"
      pattern: "db:migrate --url"
    - from: "src/scripts/startup.js"
      to: "src/api/server.js"
      via: "startup script launches server only after DB-ready migration step"
      pattern: "node src/api/server.js"
    - from: "src/api/app.js"
      to: "src/db/index.js"
      via: "health handler checks sequelize connectivity for readiness"
      pattern: "sequelize\\.authenticate|sequelize\\.query"
---

<objective>
Add runtime boot primitives so the API can start reliably in containers, wait for PostgreSQL readiness, and expose a health signal for Compose operations.

Purpose: Establish a deterministic API startup contract before wiring Docker Compose, so local-network runtime behavior is resilient on first boot and observable during troubleshooting.
Output: Server entrypoint, startup retry/migration script, readiness-aware `/health` route, and focused health endpoint tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-local-deployment-runtime/05-CONTEXT.md
@.planning/phases/05-local-deployment-runtime/05-RESEARCH.md
@src/api/app.js
@src/config/database.js
@src/db/index.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LAN-reachable API server entrypoint and readiness-aware /health endpoint</name>
  <files>src/api/app.js, src/api/server.js</files>
  <action>Create `src/api/server.js` as the canonical runtime entrypoint that starts `createApp()` on `0.0.0.0` and configurable `PORT` (default `8080`). Update `src/api/app.js` to include `GET /health` before not-found handling; health must return success only when DB connectivity check succeeds and return an unhealthy status/body when DB check fails. Keep existing item/event routes and centralized error mapping unchanged.</action>
  <verify>
    <automated>npm test -- test/api/health.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>API has a dedicated process entrypoint and `/health` returns deterministic readiness responses tied to live DB connectivity.</done>
</task>

<task type="auto">
  <name>Task 2: Implement startup wait/retry plus migration-on-boot using DATABASE_URL</name>
  <files>src/scripts/startup.js, package.json</files>
  <action>Create `src/scripts/startup.js` to run an explicit boot sequence: retry DB readiness checks with bounded interval/retry settings, execute `npx sequelize-cli db:migrate --url "$DATABASE_URL"`, then launch `node src/api/server.js`. Add package scripts for local runtime ergonomics (for example `start` and `start:compose`) that route container startup through this script. Use retry behavior that avoids fail-fast DB race conditions and logs clear progress/failure signals.</action>
  <verify>
    <automated>node --check src/scripts/startup.js && node --check src/api/server.js</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Container runtime can boot through one script that waits for DB, applies migrations against PostgreSQL via DATABASE_URL, and starts the API server.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused API health tests for ready and not-ready branches</name>
  <files>test/api/health.test.js</files>
  <action>Add integration tests around `createApp()` that verify `/health` success payload/status when DB connectivity succeeds and unhealthy payload/status when connectivity check fails. Mock/stub DB connectivity in a way that does not alter existing item/event suites and keeps test runtime under one minute.</action>
  <verify>
    <automated>npm test -- test/api/health.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Health endpoint behavior is enforced by executable tests and regressions are caught automatically.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/health.test.js --runInBand` and `node --check src/scripts/startup.js`; both must pass, proving readiness-aware health semantics and valid startup orchestration code paths.</verification>

<success_criteria>
API runtime primitives are in place so Compose can rely on a DB-aware startup path and a machine-checkable health endpoint for local-network operations.</success_criteria>

<output>
After completion, create `.planning/phases/05-local-deployment-runtime/05-01-SUMMARY.md`
</output>
