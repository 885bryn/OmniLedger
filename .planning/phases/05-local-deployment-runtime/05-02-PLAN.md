---
phase: 05-local-deployment-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .env.example
  - .gitignore
  - README.md
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - "User can run API and PostgreSQL together with one command: docker compose up -d."
    - "API is reachable from local-network clients on host port 8080 while PostgreSQL is reachable on host port 5433."
    - "Runtime setup is repeatable because required environment variables and troubleshooting steps are documented in one quickstart path."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two-service local runtime contract (api, db) with fixed ports, healthchecks, and startup dependency"
      contains: "services:\n  api:\n  db:"
    - path: "Dockerfile"
      provides: "Container build/runtime definition for API process and startup script"
      contains: "node src/scripts/startup.js"
    - path: ".env.example"
      provides: "Committed environment template with safe placeholders for DB and JWT/local secrets"
      contains: "JWT_SECRET"
    - path: "README.md"
      provides: "Single quickstart workflow, health verification steps, and top-3 troubleshooting fixes"
      contains: "docker compose up -d"
  key_links:
    - from: "docker-compose.yml"
      to: "src/scripts/startup.js"
      via: "api service command routes startup through wait/retry migration boot path"
      pattern: "startup\\.js"
    - from: "docker-compose.yml"
      to: "README.md"
      via: "documented ports and verification commands match compose runtime contract"
      pattern: "8080|5433|docker compose ps"
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "compose environment keys map to documented variable names"
      pattern: "DATABASE_URL|DB_PASSWORD|JWT_SECRET"
---

<objective>
Deliver the Compose runtime contract, environment template, and operator documentation so users can reliably run and verify the full local stack.

Purpose: Complete DEPL-01 with a deterministic one-command workflow (`up`/`down`) and clear readiness/troubleshooting guidance that matches the implemented container contract.
Output: Dockerfile, docker-compose stack, committed `.env.example`, and README quickstart/troubleshooting documentation.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-local-deployment-runtime/05-CONTEXT.md
@.planning/phases/05-local-deployment-runtime/05-RESEARCH.md
@.planning/phases/05-local-deployment-runtime/05-01-SUMMARY.md
@package.json
@src/config/database.js
@src/db/config/config.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile and Compose stack with fixed ports, healthchecks, and healthy-start dependency</name>
  <files>Dockerfile, docker-compose.yml</files>
  <action>Create a root `Dockerfile` for the API image and `docker-compose.yml` with fixed service names `api` and `db`, host ports `8080` (API) and `5433` (PostgreSQL), LAN-reachable API binding (no localhost-only host mapping), and healthchecks for both services. Configure `api` to depend on `db` with `condition: service_healthy`, and preserve DB data with a named volume so routine `docker compose down` does not erase state.</action>
  <verify>
    <automated>docker compose config</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Compose file is syntactically valid and encodes the locked runtime contract for service names, ports, healthchecks, and startup ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit local env contract with safe placeholders and ignore local secrets</name>
  <files>.env.example, .gitignore</files>
  <action>Create `.env.example` containing all required runtime variables with safe placeholder values, including sensitive placeholders such as DB password and JWT secret. Keep Compose environment as runtime source of truth by aligning variable names with `docker-compose.yml` and startup script expectations. Update `.gitignore` to exclude local `.env` files while retaining `.env.example` in git.</action>
  <verify>
    <automated>docker compose --env-file .env.example config</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Developers can copy `.env.example` to `.env` and Compose resolves all required environment variables without committing secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Document single-path quickstart, readiness verification, and top troubleshooting fixes</name>
  <files>README.md</files>
  <action>Create root `README.md` as the canonical runtime guide with one quickstart flow: copy env file, run `docker compose up -d`, verify health via `docker compose ps` and API `/health`, and stop via `docker compose down`. Add exactly three troubleshooting sections covering locked issues: port conflicts, missing/invalid env values, and delayed DB readiness. Include LAN verification guidance using host IP on port `8080`.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('README.md','utf8');const required=['docker compose up -d','docker compose down','docker compose ps','/health','8080','5433'];if(required.some((k)=>!t.includes(k))){process.exit(1);}"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Operators have a single deterministic quickstart and troubleshooting reference aligned with the actual Compose runtime behavior.</done>
</task>

</tasks>

<verification>
Run `docker compose config`, `docker compose --env-file .env.example config`, and then `docker compose up -d --build && docker compose ps && curl http://localhost:8080/health && docker compose down`; all checks must pass and show healthy API + DB services.</verification>

<success_criteria>
Users can start and stop the full API+PostgreSQL stack with Compose, reach the API on the local network, and diagnose common startup failures with documented checks.</success_criteria>

<output>
After completion, create `.planning/phases/05-local-deployment-runtime/05-02-SUMMARY.md`
</output>
