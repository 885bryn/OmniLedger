---
phase: 28-cadence-normalized-totals
plan: "02"
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - test/api/items-net-status.test.js
autonomous: true
requirements: [CASH-01, CASH-02]
must_haves:
  truths:
    - User sees mixed-frequency obligation totals normalize correctly between weekly, monthly, and yearly cadence outputs.
    - User sees mixed-frequency income totals normalize correctly between weekly, monthly, and yearly cadence outputs.
    - User sees cadence equivalence remain stable within 0.01 tolerance when comparing weekly or monthly totals against yearly run-rate expectations.
  artifacts:
    - path: test/api/items-net-status.test.js
      provides: API contract regressions for cadence normalization, one-time separation, invalid-frequency exclusion, and equivalence tolerances
      contains: cadence_totals
  key_links:
    - from: test/api/items-net-status.test.js
      to: GET /items/:id/net-status
      via: response summary contract assertions
      pattern: summary\.(cadence_totals|excluded_invalid_frequency_count)
    - from: test/api/items-net-status.test.js
      to: summary.one_time_period_totals
      via: one-time period-bounded assertions
      pattern: one_time_period_totals
---

<objective>
Lock Phase 28 cadence-normalized totals behavior with backend API contract tests.

Purpose: This plan prevents regression to monthly-only assumptions and ensures Phase 29 cadence-toggle UI can rely on a stable, validated summary payload.
Output: Expanded `items-net-status` API tests covering mixed-frequency obligations and income normalization, exclusion behavior, one-time separation, and cross-cadence equivalence.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-cadence-normalized-totals/28-CONTEXT.md
@.planning/phases/28-cadence-normalized-totals/28-01-PLAN.md
@test/api/items-net-status.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mixed-frequency obligation and income contract coverage across all cadences</name>
  <files>test/api/items-net-status.test.js</files>
  <action>Add scenario-driven API tests using one asset with mixed recurring frequencies for both obligations and income. Assert the response includes recurring cadence totals for weekly/monthly/yearly in one payload and verify amounts match yearly-baseline expectations. Include half-cent boundary fixtures to confirm bankers rounding is applied at final cadence totals (2 decimals only), not during per-row conversion steps.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>Net-status API tests prove cadence normalization is correct for mixed-frequency obligations and income, including half-cent rounding boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add exclusion and equivalence regressions for invalid frequencies and one-time separation</name>
  <files>test/api/items-net-status.test.js</files>
  <action>Extend regression coverage to assert missing/invalid recurring frequency rows are excluded and counted in explicit metadata, and that invalid/zero/malformed recurring amount rows are handled by exclusion logic with explicit assertions for both totals impact and metadata counts. Keep one-time amounts in period-bounded fields rather than recurring cadence totals. Add equivalence checks that compare weekly/yearly and monthly/yearly relationships using 0.01 tolerance so cadence view switching remains mathematically trustworthy. Keep existing RBAC, wrong-root, and one-time period rule tests intact.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js  # includes assertions for invalid/zero/malformed amount-row exclusion handling</automated>
  </verify>
  <done>Regression suite now enforces exclusion handling for invalid-frequency plus invalid/zero/malformed amount rows, preserves one-time separation, and guarantees cross-cadence equivalence tolerance for obligations and income totals.</done>
</task>

</tasks>

<verification>
Run the net-status API suite and verify all new mixed-frequency, exclusion, one-time separation, and equivalence assertions pass alongside existing scope and error-envelope tests.
</verification>

<success_criteria>
Automated API coverage proves obligations and income totals are cadence-normalized correctly for weekly/monthly/yearly outputs, invalid recurring frequencies are excluded with metadata, and cadence-equivalence checks hold within 0.01 tolerance.
</success_criteria>

<output>
After completion, create `.planning/phases/28-cadence-normalized-totals/28-cadence-normalized-totals-02-SUMMARY.md`
</output>
