---
phase: 28-cadence-normalized-totals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/items/cadence-normalization.js
  - src/domain/items/get-item-net-status.js
autonomous: true
requirements: [CASH-01, CASH-02]
must_haves:
  truths:
    - User sees recurring obligation totals exposed for weekly, monthly, and yearly cadence without one-time values blended into run-rate totals.
    - User sees recurring income totals exposed for weekly, monthly, and yearly cadence without one-time values blended into run-rate totals.
    - User sees malformed or unsupported recurring frequency rows excluded from cadence totals with explicit exclusion metadata.
  artifacts:
    - path: src/domain/items/cadence-normalization.js
      provides: Yearly-baseline cadence conversion helpers with bankers rounding and frequency constants
      contains: BANKERS_ROUNDING
    - path: src/domain/items/get-item-net-status.js
      provides: Net-status summary payload with recurring cadence totals, one-time period totals, and exclusion metadata
      contains: cadence_totals
  key_links:
    - from: src/domain/items/get-item-net-status.js
      to: src/domain/items/cadence-normalization.js
      via: recurring amount conversion by source frequency
      pattern: normalize.*frequency
    - from: src/domain/items/get-item-net-status.js
      to: summary.cadence_totals
      via: summary response contract assembly
      pattern: cadence_totals\.(weekly|monthly|yearly)
---

<objective>
Implement the backend cadence-normalized totals contract for recurring obligations and income.

Purpose: Phase 28 must make mixed-frequency rollups mathematically trustworthy before Phase 29 adds cadence switching in the UI.
Output: Shared cadence conversion helper and updated net-status summary payload that includes recurring cadence totals, explicit one-time period totals, and exclusion metadata.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-cadence-normalized-totals/28-CONTEXT.md
@.planning/phases/27-frequency-rule-contract/27-frequency-rule-contract-03-SUMMARY.md
@src/domain/items/get-item-net-status.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add yearly-baseline cadence conversion helpers for recurring rows</name>
  <files>src/domain/items/cadence-normalization.js</files>
  <action>Create a domain helper that normalizes recurring financial amounts through a yearly baseline, then derives monthly and weekly values using locked constants (52 weeks/year, 12 months/year). Support existing recurring frequencies used by items (`weekly`, `biweekly`, `monthly`, `quarterly`, `yearly`) and return explicit invalid-frequency outcomes instead of fallback defaults. Keep full precision during conversion and add bankers rounding utilities that apply only at final cadence totals (2 decimal places). Include a reusable equivalence tolerance helper set to 0.01 for contract-level assertions.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>Reusable cadence conversion helpers exist and define deterministic frequency factors, rounding behavior, and invalid-frequency signaling for recurring rows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire cadence-normalized recurring totals and one-time separation into net-status summary</name>
  <files>src/domain/items/get-item-net-status.js</files>
  <action>Refactor `buildSummary` to produce cadence-normalized recurring totals for obligations and income in one response payload (`weekly`, `monthly`, `yearly`) while preserving the Phase 27 one-time monthly inclusion contract. Keep one-time amounts out of recurring cadence run-rate fields and expose them in a separate period-bounded summary object. When recurring frequency is missing or invalid, exclude the row and increment dedicated exclusion metadata rather than defaulting silently. Preserve existing summary metadata fields (`active_period`, `one_time_rule`) and keep legacy monthly summary keys mapped consistently so current UI behavior does not regress before Phase 29 cadence-toggle wiring.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js</automated>
  </verify>
  <done>`GET /items/:id/net-status` returns explicit recurring cadence totals for obligations and income plus separate one-time period impact and exclusion metadata, with no silent frequency fallback.</done>
</task>

</tasks>

<verification>
Run net-status API tests and confirm mixed-frequency recurring rows now emit weekly/monthly/yearly totals in one payload while one-time values stay separated and exclusion metadata is populated for invalid frequency rows.
</verification>

<success_criteria>
Backend net-status summary now exposes mathematically consistent recurring cadence totals for obligations and income (weekly/monthly/yearly), uses bankers rounding at final totals, and keeps one-time effects period-bounded instead of blending them into recurring run-rate fields.
</success_criteria>

<output>
After completion, create `.planning/phases/28-cadence-normalized-totals/28-cadence-normalized-totals-01-SUMMARY.md`
</output>
