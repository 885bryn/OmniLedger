---
phase: 15-assets-and-contracts-workbook-model
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/api/routes/exports.routes.js
  - test/api/exports-backup-scope.test.js
autonomous: true
requirements:
  - EXPT-03
  - EXPT-04
  - RELA-01
must_haves:
  truths:
    - "Export route responses include workbook-facing `Assets` and `Financial Contracts` sheet contracts in addition to the existing scoped datasets envelope."
    - "Assets and contracts rows in the response retain deterministic ordering and explicit relationship marker semantics from the workbook model."
    - "Workbook shaping never widens scope; owner/all/lens behavior from Phase 14 remains unchanged."
  artifacts:
    - path: "src/api/routes/exports.routes.js"
      provides: "Export route response wiring that adds workbook model output to scoped export payload"
      exports: ["createExportsRouter"]
    - path: "test/api/exports-backup-scope.test.js"
      provides: "Integration regression coverage for workbook response shape plus scope-preservation behavior"
  key_links:
    - from: "src/api/routes/exports.routes.js"
      to: "src/domain/exports/workbook-model.js"
      via: "route-level workbook shaping from scoped dataset rows"
      pattern: "buildWorkbookModel"
    - from: "src/api/routes/exports.routes.js"
      to: "src/domain/exports/export-scope-query.js"
      via: "scope-authoritative source dataset"
      pattern: "exportScopeQuery\(\{\s*scope:\s*req\.scope"
    - from: "test/api/exports-backup-scope.test.js"
      to: "GET /exports/backup.xlsx"
      via: "API contract assertions for workbook and scope"
      pattern: "response\.body\.workbook|response\.body\.sheets"
---

<objective>
Wire the Phase 15 workbook model into the authenticated export API contract while preserving Phase 14 scope guarantees and endpoint stability.

Purpose: Expose the new sheet contracts to downstream workbook generation work without breaking existing scoped dataset semantics.
Output: Additive export response envelope and integration tests proving workbook structure plus unchanged SCOP behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-assets-and-contracts-workbook-model/15-CONTEXT.md
@.planning/phases/15-assets-and-contracts-workbook-model/15-RESEARCH.md
@.planning/phases/15-assets-and-contracts-workbook-model/15-01-SUMMARY.md
@src/api/routes/exports.routes.js
@src/domain/exports/export-scope-query.js
@test/api/exports-backup-scope.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workbook model payload to export route response contract</name>
  <files>src/api/routes/exports.routes.js</files>
  <action>Update `GET /exports/backup.xlsx` handling to build workbook sheet data from scoped dataset rows using `buildWorkbookModel`, and return it additively alongside existing `datasets` fields (do not remove or rename existing phase-14 keys). Keep route trust boundary unchanged: workbook model must consume only `exportScopeQuery({ scope: req.scope })` results and must not read client owner/lens hints. Ensure response keys and sheet naming are explicit and stable for next-phase workbook serialization work.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Export responses include deterministic workbook sheet contracts while preserving pre-existing scoped dataset envelope and route auth/scope behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extend export API regression suite for workbook sheet contract and scope preservation</name>
  <files>test/api/exports-backup-scope.test.js</files>
  <action>Expand API tests to assert workbook `Assets` and `Financial Contracts` sections are present and include representative flattened/relationship fields (IDs + readable references + unresolved markers). Add at least one assertion per scope mode (owner, admin all, admin lens) that workbook row membership mirrors scope-filtered dataset ownership. Keep existing SCOP regression assertions intact to prevent accidental weakening while adding phase-15 coverage.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Integration tests verify additive workbook contract correctness and prove Phase 14 scope enforcement remains intact after workbook-model wiring.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/exports/workbook-model.test.js --runInBand` then `npm test -- test/api/exports-backup-scope.test.js --runInBand` to confirm sheet-model behavior and route-level scope preservation both hold.
</verification>

<success_criteria>
- `GET /exports/backup.xlsx` returns workbook-facing `Assets` and `Financial Contracts` payload sections with deterministic schema/ordering.
- Export API contract remains backward-compatible for existing `datasets` consumers.
- Scope mode behavior (owner/all/lens plus override resistance) stays unchanged after workbook addition.
- API tests lock both workbook shape and scope invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/15-assets-and-contracts-workbook-model/15-02-SUMMARY.md`
</output>
