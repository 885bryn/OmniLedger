---
phase: 15-assets-and-contracts-workbook-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/exports/workbook-columns.js
  - src/domain/exports/workbook-formatters.js
  - src/domain/exports/workbook-model.js
  - test/domain/exports/workbook-model.test.js
autonomous: true
requirements:
  - EXPT-03
  - EXPT-04
  - RELA-01
must_haves:
  truths:
    - "Assets sheet rows expose stable identity, ownership, lifecycle, and flattened attribute columns in deterministic order."
    - "Financial Contracts sheet rows expose subtype, recurrence, status, amount, and due context in deterministic order."
    - "Relationship fields expose both stable IDs and readable references, with explicit unresolved markers when links cannot be resolved."
  artifacts:
    - path: "src/domain/exports/workbook-columns.js"
      provides: "Frozen Assets and Financial Contracts column contracts with stable order metadata"
      exports: ["ASSETS_COLUMNS", "FINANCIAL_CONTRACT_COLUMNS"]
    - path: "src/domain/exports/workbook-formatters.js"
      provides: "Shared marker, enum-label, amount/date, and relationship formatting helpers"
    - path: "src/domain/exports/workbook-model.js"
      provides: "Pure workbook transform that splits scoped item rows into Assets and Financial Contracts sheet rows"
      exports: ["buildWorkbookModel"]
    - path: "test/domain/exports/workbook-model.test.js"
      provides: "Deterministic transform coverage for flattening, ordering, relationship fallback, and unresolved markers"
  key_links:
    - from: "src/domain/exports/workbook-model.js"
      to: "src/domain/exports/workbook-columns.js"
      via: "column-driven row construction"
      pattern: "ASSETS_COLUMNS|FINANCIAL_CONTRACT_COLUMNS"
    - from: "src/domain/exports/workbook-model.js"
      to: "src/domain/exports/workbook-formatters.js"
      via: "shared explicit marker and formatting helpers"
      pattern: "format.*|resolve.*"
    - from: "src/domain/exports/workbook-model.js"
      to: "test/domain/exports/workbook-model.test.js"
      via: "deterministic comparator and relationship fallback assertions"
      pattern: "buildWorkbookModel"
---

<objective>
Build the Phase 15 workbook transformation layer that converts Phase 14 scoped export rows into deterministic, readable `Assets` and `Financial Contracts` sheet contracts.

Purpose: Fulfill EXPT-03, EXPT-04, and RELA-01 using stable column specs, explicit marker policy, and compatibility-safe relationship resolution.
Output: New export workbook domain modules plus domain tests that lock flattening, sorting, and relationship fidelity behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-assets-and-contracts-workbook-model/15-CONTEXT.md
@.planning/phases/15-assets-and-contracts-workbook-model/15-RESEARCH.md
@src/domain/exports/export-scope-query.js
@src/domain/items/get-item-net-status.js
@src/domain/items/financial-metrics.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define frozen workbook column contracts and formatter policy</name>
  <files>src/domain/exports/workbook-columns.js, src/domain/exports/workbook-formatters.js</files>
  <action>Create explicit frozen column specs for `Assets` and `Financial Contracts` with relationship columns grouped near identity keys and with deterministic ordering metadata. Implement shared formatter helpers that enforce locked decisions: human-readable enum labels, explicit null/unresolved markers (for example `N/A` and `UNLINKED`), fixed two-decimal amount formatting, and stable ISO-like date output. Include attribute flattening helpers that map baseline keys to dedicated columns and collect unknown keys into a deterministic overflow text/JSON column.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Column contracts and formatting helpers encode deterministic order and explicit marker rules for both sheets without relying on dynamic key iteration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement workbook model builder with compatibility-safe relationship resolution</name>
  <files>src/domain/exports/workbook-model.js</files>
  <action>Implement `buildWorkbookModel()` as a pure transform over scoped dataset rows. Split item rows into Assets (`RealEstate`, `Vehicle`) and Financial Contracts (`FinancialItem`), flatten baseline fields, and keep overflow attributes in a stable serialized column. Resolve parent and linked asset relationships with precedence across canonical columns and compatibility attributes (`parent_item_id`, `linked_asset_item_id`, `attributes.parentItemId`, `attributes.linkedAssetItemId`). Emit both ID and readable title/name reference columns, with explicit unresolved markers when target rows are missing. Apply deterministic sorting exactly per locked decisions: Assets by type then title then stable id; Financial Contracts by status then next due date then stable id.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The workbook model returns deterministic `Assets` and `Financial Contracts` rows with readable flattened values and stable relationship fidelity across canonical and legacy link sources.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic domain tests for flattening, ordering, and relationship markers</name>
  <files>test/domain/exports/workbook-model.test.js</files>
  <action>Add focused Jest coverage for both sheet contracts with mixed canonical/compatibility relationship fixtures, missing link targets, enum label formatting, amount/date formatting, and overflow attribute preservation. Include tie-breaker cases proving deterministic sort order when primary keys are equal. Keep assertions strict on column presence/order and marker values so regressions are immediately visible before API wiring.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Domain tests fail on any drift in sheet schema, ordering, marker policy, or relationship fallback behavior for Phase 15 requirements.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/exports/workbook-model.test.js --runInBand` and confirm deterministic sheet model assertions pass for Assets/Financial Contracts flattening and RELA-01 link fidelity scenarios.
</verification>

<success_criteria>
- `buildWorkbookModel` emits `Assets` and `Financial Contracts` sheet rows from scoped export items with stable schemas.
- Sheet rows expose readable flattened values and explicit markers for null/unresolved values.
- Relationship references include both stable IDs and readable linked names/titles with compatibility fallback coverage.
- Test coverage locks deterministic order, marker policy, and overflow attribute retention.
</success_criteria>

<output>
After completion, create `.planning/phases/15-assets-and-contracts-workbook-model/15-01-SUMMARY.md`
</output>
