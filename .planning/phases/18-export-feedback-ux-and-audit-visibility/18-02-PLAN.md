---
phase: 18-export-feedback-ux-and-audit-visibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/routes/exports.routes.js
  - src/domain/items/get-item-activity.js
  - src/api/routes/items.routes.js
  - test/api/exports-backup-scope.test.js
  - frontend/src/lib/api-client.ts
  - frontend/src/features/audit/item-activity-timeline.tsx
  - frontend/src/__tests__/item-activity-attribution.test.tsx
autonomous: true
requirements:
  - SECU-02
must_haves:
  truths:
    - "Every export attempt (success and failure) is written to audit history with actor, lens context, and timestamp."
    - "Audit history UI shows export actions in the existing activity surface without creating a new export-only page."
    - "Export attribution is human-readable and machine-stable, including explicit all-data lens labeling when lens user is null."
  artifacts:
    - path: "src/api/routes/exports.routes.js"
      provides: "Export-route audit persistence for succeeded and failed outcomes using server-resolved scope attribution"
      exports: ["createExportsRouter"]
    - path: "src/domain/items/get-item-activity.js"
      provides: "Activity query contract extended to include export audit rows relevant to current actor/lens context"
      exports: ["getItemActivity"]
    - path: "frontend/src/features/audit/item-activity-timeline.tsx"
      provides: "Existing activity timeline rendering of export actions with readable actor/lens labels plus stable IDs"
      exports: ["ItemActivityTimeline"]
    - path: "test/api/exports-backup-scope.test.js"
      provides: "API regressions proving export audit rows persist for both outcomes with actor/lens attribution"
    - path: "frontend/src/__tests__/item-activity-attribution.test.tsx"
      provides: "Frontend regressions proving export rows are visible and attribution tuple includes readable + stable context"
  key_links:
    - from: "src/api/routes/exports.routes.js"
      to: "src/db/models/audit-log.model.js"
      via: "AuditLog.create for both success and failure branches"
      pattern: "export\.backup\.(succeeded|failed)|actor_user_id|lens_user_id"
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/get-item-activity.js"
      via: "route passes scope context so activity query can include matching export audit entries"
      pattern: "getItemActivity\(\{.*scope"
    - from: "frontend/src/features/audit/item-activity-timeline.tsx"
      to: "frontend/src/lib/api-client.ts"
      via: "typed transport rows for export outcome and attribution labels/ids"
      pattern: "export|actor|lens"
---

<objective>
Complete audit traceability for exports so administrators can verify who exported data, under which lens, and with what outcome directly in existing audit history.

Purpose: Fulfill SECU-02 by wiring server-side export audit writes and exposing those rows in the established activity timeline contract instead of creating a new export-only view.
Output: Export route audit persistence for success/failure attempts, activity-query exposure of export entries with actor/lens attribution, and backend/frontend regressions that lock visibility behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-export-feedback-ux-and-audit-visibility/18-CONTEXT.md
@.planning/phases/18-export-feedback-ux-and-audit-visibility/18-RESEARCH.md
@src/api/routes/exports.routes.js
@src/api/routes/items.routes.js
@src/domain/items/get-item-activity.js
@src/db/models/audit-log.model.js
@test/api/exports-backup-scope.test.js
@frontend/src/lib/api-client.ts
@frontend/src/features/audit/item-activity-timeline.tsx
@frontend/src/__tests__/item-activity-attribution.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist export audit rows for success and failure outcomes in export route</name>
  <files>src/api/routes/exports.routes.js, test/api/exports-backup-scope.test.js</files>
  <action>In `GET /exports/backup.xlsx`, record an audit row for every attempt using `req.scope` attribution (`actor_user_id`, `lens_user_id`, and scope-aware `user_id` fallback) with verb-style actions `export.backup.succeeded` and `export.backup.failed`. Ensure failed attempts are logged in catch/error paths before rethrowing to middleware, and keep scope-authoritative export behavior unchanged. Extend API tests to assert both success and failure audit writes include actor/lens/timestamp fields and mode-aware lens semantics (`null` for all-data).
  </action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Export endpoint always emits attributable audit rows for both successful and failed attempts.</done>
</task>

<task type="auto">
  <name>Task 2: Extend existing activity query contract to surface relevant export audit entries</name>
  <files>src/domain/items/get-item-activity.js, src/api/routes/items.routes.js, frontend/src/lib/api-client.ts</files>
  <action>Update item activity query contract to include export audit rows that are relevant to the current actor/lens scope (without introducing a new dedicated export endpoint/surface). Pass scope context from items route into the domain query, merge export rows with existing item/event rows in deterministic timestamp order, and include both stable IDs and readable labels for actor/lens attribution. Preserve existing item activity ownership protections and legacy-row behavior.
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Existing activity payload now carries attributable export actions and remains backward-compatible for current item/event consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Render export audit outcomes and attribution tuple in existing activity timeline</name>
  <files>frontend/src/features/audit/item-activity-timeline.tsx, frontend/src/__tests__/item-activity-attribution.test.tsx</files>
  <action>Update timeline rendering to show export actions alongside existing activity rows with explicit outcome visibility, actor/lens readable labels, and stable ID context. Handle admin all-data lens as an explicit readable label while preserving null lens ID semantics from transport. Add frontend tests asserting export row presence, outcome text, and tuple rendering across attributed, all-data, and legacy cases.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/item-activity-attribution.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Administrators can trace export actions and outcomes in the current activity surface with readable+stable attribution context.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/exports-backup-scope.test.js --runInBand` and `npm --prefix frontend test -- src/__tests__/item-activity-attribution.test.tsx --runInBand` and confirm export success/failure audit rows plus UI attribution rendering are locked.
</verification>

<success_criteria>
- Export route writes audit rows for both success and failure, carrying actor/lens/timestamp attribution.
- Existing activity API contract includes export audit rows relevant to current actor/lens scope without adding a new export-only surface.
- Existing activity UI renders export outcomes with readable actor/lens labels and stable ID context, including all-data lens handling.
- Backend and frontend regression tests fail if export audit visibility or attribution wiring regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/18-export-feedback-ux-and-audit-visibility/18-02-SUMMARY.md`
</output>
