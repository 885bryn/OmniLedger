---
phase: 18-export-feedback-ux-and-audit-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/export/use-export-backup.ts
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/features/ui/toast-provider.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/user-switcher-export-action.test.tsx
autonomous: true
requirements:
  - UXEX-02
must_haves:
  truths:
    - "Export action shows immediate inline loading state, disables repeat clicks, and keeps users informed when generation is still running."
    - "Successful exports show both inline confirmation and toast acknowledgement that completion and download start occurred."
    - "Failed exports keep retry available in the same context, with re-auth guidance for session failures and escalated recovery copy on repeated failures."
  artifacts:
    - path: "frontend/src/features/export/use-export-backup.ts"
      provides: "Export feedback state machine with phase/error classification, retry attempts, and long-running hint state"
      exports: ["useExportBackup"]
    - path: "frontend/src/app/shell/user-switcher.tsx"
      provides: "Inline loading/success/failure feedback with one-click retry and admin attribution-aware success copy"
      exports: ["UserSwitcher"]
    - path: "frontend/src/features/ui/toast-provider.tsx"
      provides: "Medium-prominence export toast channel reused by shell export feedback"
      exports: ["ToastProvider", "useToast"]
    - path: "frontend/src/__tests__/user-switcher-export-action.test.tsx"
      provides: "Regression coverage for loading lock, still-working hint, dual-channel success, and actionable failure ladder"
  key_links:
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/features/export/use-export-backup.ts"
      via: "derived export phase and error-kind rendering branches"
      pattern: "phase|errorKind|attemptCount|isLongRunning"
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/features/ui/toast-provider.tsx"
      via: "toast emission on successful export completion"
      pattern: "useToast|push"
    - from: "frontend/src/__tests__/user-switcher-export-action.test.tsx"
      to: "frontend/src/app/shell/user-switcher.tsx"
      via: "assertions for pending-disable, success toast+inline text, retry CTA, and auth guidance"
      pattern: "Exporting|still working|retry|session"
---

<objective>
Harden export UX trust signals so users always get clear in-context feedback while backups are generated, downloaded, or fail.

Purpose: Fulfill UXEX-02 by moving from basic success/error flags to explicit, actionable state communication aligned with locked decisions (inline loading, long-running hint, dual success confirmation, and retry-first failures).
Output: Updated export hook and shell UI behavior, toast support for export acknowledgements, localized copy updates, and frontend regressions that lock the feedback contract.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-export-feedback-ux-and-audit-visibility/18-CONTEXT.md
@.planning/phases/18-export-feedback-ux-and-audit-visibility/18-RESEARCH.md
@frontend/src/features/export/use-export-backup.ts
@frontend/src/app/shell/user-switcher.tsx
@frontend/src/features/ui/toast-provider.tsx
@frontend/src/locales/en/common.json
@frontend/src/locales/zh/common.json
@frontend/src/__tests__/user-switcher-export-action.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend export hook into explicit feedback state machine</name>
  <files>frontend/src/features/export/use-export-backup.ts</files>
  <action>Refactor `useExportBackup` to return a derived `phase` (`idle|pending|success|error`), `errorKind` classification (`network|session|server`), `attemptCount`, and timer-driven `isLongRunning` state after a threshold while request remains pending. Keep GET `/exports/backup.xlsx` contract unchanged and keep browser blob download behavior in the hook boundary. Classify HTTP 401 as session/auth guidance input, and clear stale status state on fresh trigger/reset so new attempts never reuse stale success/failure banners.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Export hook exposes deterministic feedback state needed for inline loading, long-running hinting, and actionable failure variants without changing API shape.</done>
</task>

<task type="auto">
  <name>Task 2: Add medium-prominence export toast channel and localized trust copy</name>
  <files>frontend/src/features/ui/toast-provider.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json</files>
  <action>Extend existing toast provider with a generic non-safety toast path usable by export UX while preserving current safety toast behavior and dedupe rules. Add localized copy keys for export success acknowledgement, long-running hint, retry CTA guidance ladder (including session re-auth text), and admin attribution-aware success messaging. Keep copy trust-oriented and actionable, with short auto-dismiss timing that remains readable.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>UI has localized copy and reusable toast plumbing to show export completion acknowledgement alongside inline status.</done>
</task>

<task type="auto">
  <name>Task 3: Wire UserSwitcher feedback flow with retry ladder and attribution-aware success details</name>
  <files>frontend/src/app/shell/user-switcher.tsx, frontend/src/__tests__/user-switcher-export-action.test.tsx</files>
  <action>Update export action UI to consume hook state machine: disable button and show loading label while pending, render persistent inline "still working" hint for long requests, show inline success plus toast acknowledgement on completion, and show inline failure with one-click retry in the same control area. Differentiate session failures with re-auth guidance, escalate copy after repeated failures, and include actor/lens context in admin success text. Ensure feedback auto-dismisses after a brief interval and resets cleanly on next export attempt.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>UserSwitcher provides deterministic loading, success, and actionable error UX (with retry + session guidance + escalation) that matches phase decisions.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand` and confirm pending lock, long-running hint, dual success confirmation, and failure ladder assertions pass.
</verification>

<success_criteria>
- Export action remains inline, disables during pending, and shows explicit long-running feedback when generation is slow.
- Success confirms both completion and download start via inline + toast channels, then auto-clears after a readable delay.
- Failure keeps one-click retry in place and provides session-specific guidance plus escalated recovery steps on repeated failures.
- Admin success messaging includes actor/lens context inline.
</success_criteria>

<output>
After completion, create `.planning/phases/18-export-feedback-ux-and-audit-visibility/18-01-SUMMARY.md`
</output>
