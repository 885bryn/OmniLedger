---
phase: 03-net-status-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/items/item-net-status-errors.js
  - src/domain/items/get-item-net-status.js
  - test/domain/items/get-item-net-status.test.js
autonomous: true
requirements:
  - ITEM-05
must_haves:
  truths:
    - "Net-status domain retrieval returns one canonical root asset and direct child commitments only, with no event preview expansion."
    - "Child commitments are ordered deterministically by derived due date ascending, null due dates last, then created_at ascending."
    - "Summary reports monthly obligation total from valid commitment rows and tracks how many rows were excluded from aggregation."
  artifacts:
    - path: "src/domain/items/get-item-net-status.js"
      provides: "Service-first net-status retrieval, ownership/type guards, deterministic ordering, and summary computation"
      exports:
        - "getItemNetStatus"
    - path: "src/domain/items/item-net-status-errors.js"
      provides: "Domain error taxonomy for not-found, forbidden ownership, and wrong-root-type failures"
      exports:
        - "ItemNetStatusError"
    - path: "test/domain/items/get-item-net-status.test.js"
      provides: "Executable guardrails for ordering, summary exclusion behavior, and 404/403/422 domain branches"
      contains: "getItemNetStatus domain service"
  key_links:
    - from: "src/domain/items/get-item-net-status.js"
      to: "src/db/index.js"
      via: "Item lookup by id, child lookup by parent_item_id"
      pattern: "models\\.Item\\.(findByPk|findAll)"
    - from: "src/domain/items/get-item-net-status.js"
      to: "src/domain/items/item-net-status-errors.js"
      via: "guard failures mapped to stable domain categories"
      pattern: "new ItemNetStatusError"
---

<objective>
Implement the net-status domain read model so API wiring can remain thin and deterministic.

Purpose: Enforce locked retrieval rules in one place (ownership distinction, wrong-type rejection, one-level child nesting, deterministic ordering, and summary aggregation) before transport concerns are added.
Output: Net-status domain service, endpoint-specific error taxonomy, and domain tests covering ordering, summary exclusions, and guarded failure modes.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-net-status-retrieval/03-CONTEXT.md
@.planning/phases/03-net-status-retrieval/03-RESEARCH.md
@.planning/phases/02-item-creation-workflow/02-02-SUMMARY.md
@src/domain/items/create-item.js
@src/db/models/item.model.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define net-status domain error taxonomy for locked failure branches</name>
  <files>src/domain/items/item-net-status-errors.js</files>
  <action>Create endpoint-specific domain error primitives and categories that map cleanly to locked 404 (unknown id), 403 (foreign-owned item), and 422 (commitment root/wrong type) semantics. Keep issue payload shape compatible with the established error-envelope style (`error.code`, `error.category`, `error.message`, `error.issues`) so API mapping can remain centralized and deterministic.</action>
  <verify>
    <automated>npm test -- test/domain/items/get-item-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Domain errors exist with stable exports and explicit categories aligned to all locked net-status failure outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement getItemNetStatus service with guards, sort order, and summary</name>
  <files>src/domain/items/get-item-net-status.js</files>
  <action>Implement `getItemNetStatus({ itemId, actorUserId })` as a service-first read flow: fetch root by id, enforce ownership (403), reject commitment roots with actionable wrong-type category (422), then fetch direct child commitments (`parent_item_id = root.id`). Serialize root and children to canonical item fields only, add `child_commitments` and `summary`, and never include event previews. Derive due-date sort keys from commitment attributes, then in-memory stable-sort by due date ascending (null last) and `created_at` ascending. Compute `summary.monthly_obligation_total` by summing valid numeric commitment amounts and include excluded-row metadata for rows missing required aggregate inputs.</action>
  <verify>
    <automated>npm test -- test/domain/items/get-item-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Service returns deterministic net-status payloads for valid assets and emits locked 404/403/422 domain failures for invalid retrieval paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add domain tests for ordering determinism, summary exclusions, and guard branches</name>
  <files>test/domain/items/get-item-net-status.test.js</files>
  <action>Add domain tests using sqlite-backed mocked models to prove: root asset + direct child commitments are returned in canonical shape; sorting obeys due-date ascending with null-last and created_at tie-break; summary totals include only valid numeric amounts and report excluded-row count; unknown ids throw not-found category, foreign ownership throws forbidden category, and commitment-root requests throw wrong-type category. Assert deterministic response field names chosen in this phase.</action>
  <verify>
    <automated>npm test -- test/domain/items/get-item-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Domain suite fails on ordering/summary/guard regressions and passes only when net-status behavior matches locked phase decisions.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/items/get-item-net-status.test.js --runInBand`; the suite must prove canonical root/child shaping, deterministic child ordering, summary aggregation exclusion tracking, and distinct 404/403/422 domain outcomes.</verification>

<success_criteria>
Net-status domain retrieval is fully deterministic and ready for route exposure, with all locked behavioral branches and summary semantics enforced by tests.</success_criteria>

<output>
After completion, create `.planning/phases/03-net-status-retrieval/03-01-SUMMARY.md`
</output>
