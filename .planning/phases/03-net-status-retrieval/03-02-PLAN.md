---
phase: 03-net-status-retrieval
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/api/routes/items.routes.js
  - src/api/errors/http-error-mapper.js
  - src/api/app.js
  - test/api/items-net-status.test.js
autonomous: true
requirements:
  - ITEM-05
must_haves:
  truths:
    - "User can call GET /items/:id/net-status and receive one root item response with canonical fields, child_commitments, and summary."
    - "Unknown item id returns 404, foreign-owned item returns 403, and commitment-root requests return 422 in the established issue-envelope style."
    - "Net-status API responses keep deterministic child ordering and do not include related event previews."
  artifacts:
    - path: "src/api/routes/items.routes.js"
      provides: "GET /items/:id/net-status route that delegates to domain service"
      contains: "router.get('/items/:id/net-status'"
    - path: "src/api/errors/http-error-mapper.js"
      provides: "Centralized mapping from net-status domain errors to stable HTTP envelopes"
      exports:
        - "mapItemCreateError"
    - path: "test/api/items-net-status.test.js"
      provides: "Endpoint contract tests for 200 and locked 404/403/422 branches"
      contains: "GET /items/:id/net-status"
  key_links:
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/get-item-net-status.js"
      via: "route handler delegates service call with item id and actor id"
      pattern: "getItemNetStatus"
    - from: "src/api/app.js"
      to: "src/api/errors/http-error-mapper.js"
      via: "shared error middleware maps net-status errors into HTTP responses"
      pattern: "mapItemCreateError"
---

<objective>
Expose the net-status read model through the HTTP API contract and validate end-to-end behavior.

Purpose: Deliver ITEM-05 as a user-callable endpoint with deterministic output and stable failure envelopes that match locked status/category decisions.
Output: GET route wiring, centralized net-status error mapping integration, and API integration coverage for success and all locked failures.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-net-status-retrieval/03-CONTEXT.md
@.planning/phases/03-net-status-retrieval/03-RESEARCH.md
@.planning/phases/03-net-status-retrieval/03-01-SUMMARY.md
@src/api/app.js
@src/api/routes/items.routes.js
@src/api/errors/http-error-mapper.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /items/:id/net-status route with thin service delegation</name>
  <files>src/api/routes/items.routes.js</files>
  <action>Extend the existing items router with `GET /items/:id/net-status` that delegates to `getItemNetStatus` and returns the service payload as-is (single root object, not envelope wrapper). Pass `itemId` from path params and use one temporary actor transport for ownership checks via `x-user-id` request header as recommended in research, keeping the transport isolated so future auth can replace it without rewriting domain logic.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Route is mounted and delegates all business logic to the domain service while preserving locked response-shape constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Extend centralized HTTP error mapper for net-status domain errors</name>
  <files>src/api/errors/http-error-mapper.js, src/api/app.js</files>
  <action>Add net-status error mapping alongside existing item-create mapping so 404/403/422 responses share the established issue-envelope style (`error.code`, `error.category`, `error.message`, `error.issues`). Keep mapper centralized in app-level middleware; avoid route-local custom failure bodies. Ensure non-net-status and non-item-create errors still fall through to default handlers unchanged.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Net-status failures are consistently translated into locked status codes and actionable issue envelopes via shared middleware.</done>
</task>

<task type="auto">
  <name>Task 3: Add endpoint integration tests for success contract and locked failure branches</name>
  <files>test/api/items-net-status.test.js</files>
  <action>Create integration tests against `createApp()` that verify: success response includes canonical root fields with `child_commitments` and `summary`; child commitments are one-level nested and deterministically ordered; summary includes monthly obligation total plus excluded-row metadata; unknown id returns 404, foreign ownership returns 403, commitment-root id returns 422; and all failures use the same issue-envelope style used in Phase 2. Assert no event preview fields are present.</action>
  <verify>
    <automated>npm test -- test/api/items-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>API tests prove ITEM-05 from HTTP entrypoint through domain retrieval and centralized error translation, including all locked branch semantics.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/items/get-item-net-status.test.js --runInBand` and `npm test -- test/api/items-net-status.test.js --runInBand`; both suites must pass and show deterministic success payloads plus locked 404/403/422 envelope behavior for GET net-status.</verification>

<success_criteria>
Users can retrieve net status through `GET /items/:id/net-status` with canonical root + direct child commitments + summary, and receive consistent actionable failures for all locked invalid paths.</success_criteria>

<output>
After completion, create `.planning/phases/03-net-status-retrieval/03-02-SUMMARY.md`
</output>
