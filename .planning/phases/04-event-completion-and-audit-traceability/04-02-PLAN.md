---
phase: 04-event-completion-and-audit-traceability
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/api/routes/events.routes.js
  - src/api/errors/http-error-mapper.js
  - src/api/app.js
  - test/api/events-complete.test.js
autonomous: true
requirements:
  - EVNT-02
  - EVNT-03
  - AUDT-01
must_haves:
  truths:
    - "User can call PATCH /events/:id/complete and receive a data-only success payload containing canonical event fields plus completed_at and prompt_next_date."
    - "Unknown event id returns 404 envelope, foreign-owned event returns 403 envelope, and state/transition failures return issue-envelope responses with stable category semantics."
    - "Calling completion twice for the same event remains idempotent from the API surface and does not create duplicate completion audit history."
  artifacts:
    - path: "src/api/routes/events.routes.js"
      provides: "PATCH /events/:id/complete route delegating to completeEvent with actor transport"
      contains: "router.patch('/events/:id/complete'"
    - path: "src/api/errors/http-error-mapper.js"
      provides: "Centralized completion error mapping into established issue-envelope HTTP responses"
      exports:
        - "mapEventCompletionError"
    - path: "test/api/events-complete.test.js"
      provides: "Endpoint integration coverage for success, idempotency, prompt behavior, and locked failure envelopes"
      contains: "PATCH /events/:id/complete"
  key_links:
    - from: "src/api/routes/events.routes.js"
      to: "src/domain/events/complete-event.js"
      via: "thin route delegates event id and x-user-id actor"
      pattern: "completeEvent"
    - from: "src/api/app.js"
      to: "src/api/errors/http-error-mapper.js"
      via: "central middleware maps completion domain errors"
      pattern: "mapEventCompletionError"
---

<objective>
Expose event completion behavior through a PATCH endpoint with deterministic payload and centralized error translation.

Purpose: Deliver user-facing EVNT-02, EVNT-03, and AUDT-01 contract semantics without duplicating domain logic, preserving the established thin-route and shared-mapper architecture.
Output: Events router, app wiring and mapper integration for completion errors, and API integration tests for success/idempotency/failure contracts.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-event-completion-and-audit-traceability/04-CONTEXT.md
@.planning/phases/04-event-completion-and-audit-traceability/04-RESEARCH.md
@.planning/phases/04-event-completion-and-audit-traceability/04-01-SUMMARY.md
@src/api/app.js
@src/api/errors/http-error-mapper.js
@src/api/routes/items.routes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PATCH /events/:id/complete route with thin delegation</name>
  <files>src/api/routes/events.routes.js</files>
  <action>Create a dedicated events router module with `PATCH /events/:id/complete` that delegates to `completeEvent` and returns the service payload directly (data-only, no success wrapper message). Pass actor context through `x-user-id` header as the current boundary transport, and keep route responsibilities limited to request extraction, success status, and error delegation.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Events route exists, mounted through app wiring, and preserves thin-controller architecture with deterministic completion payload passthrough.</done>
</task>

<task type="auto">
  <name>Task 2: Extend centralized HTTP error mapper and app middleware for completion errors</name>
  <files>src/api/errors/http-error-mapper.js, src/api/app.js</files>
  <action>Add `mapEventCompletionError` using completion error categories to status mappings that honor locked semantics (404 unknown event, 403 foreign-owned event, issue-envelope format for invalid transition/state branches). Integrate the new mapper in the existing app-level error middleware chain and mount the events router in `createApp` without regressing item-create or net-status behaviors.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Completion failures are translated by shared middleware into stable envelope responses while existing endpoint error mapping remains intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add API integration tests for completion success, idempotency, prompt rules, and failure envelopes</name>
  <files>test/api/events-complete.test.js</files>
  <action>Add sqlite-backed integration tests using `createApp()` to verify: first completion returns status 200 with canonical completion payload fields and `prompt_next_date`; non-recurring completions report `prompt_next_date: true`; recurring completions still include prompt metadata and completion data shape; unknown event id returns 404 envelope; foreign-owned event returns 403 envelope; repeated completion remains idempotent (stable completed_at and no duplicate audit rows). Assert error responses never include `prompt_next_date`.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Endpoint tests prove full completion flow behavior from HTTP entrypoint through domain service and centralized error mapper, including locked idempotency and audit traceability semantics.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/events/complete-event.test.js --runInBand` and `npm test -- test/api/events-complete.test.js --runInBand`; both suites must pass and confirm deterministic completion payloads, stable error envelopes, and no duplicate audit writes on idempotent requests.</verification>

<success_criteria>
Users can complete events through `PATCH /events/:id/complete` and get deterministic completion + prompt signaling with auditable at-most-once behavior and stable failure semantics.</success_criteria>

<output>
After completion, create `.planning/phases/04-event-completion-and-audit-traceability/04-02-SUMMARY.md`
</output>
