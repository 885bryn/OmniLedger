---
phase: 04-event-completion-and-audit-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/events/event-completion-errors.js
  - src/domain/events/complete-event.js
  - test/domain/events/complete-event.test.js
autonomous: true
requirements:
  - EVNT-02
  - EVNT-03
  - AUDT-01
must_haves:
  truths:
    - "Completing a pending event transitions status to Completed exactly once and returns canonical completion payload fields including completed_at and prompt_next_date."
    - "Re-completing an already completed event is idempotent: it returns success without changing completed_at and without writing a second audit row."
    - "Completion guards distinguish unknown event id (404-equivalent domain category) from foreign-owned event (403-equivalent domain category)."
  artifacts:
    - path: "src/domain/events/complete-event.js"
      provides: "Transactional event completion service with canonical serializer, prompt_next_date rules, and audit write semantics"
      exports:
        - "completeEvent"
    - path: "src/domain/events/event-completion-errors.js"
      provides: "Domain error taxonomy for completion guard and transition failures"
      exports:
        - "EventCompletionError"
    - path: "test/domain/events/complete-event.test.js"
      provides: "Executable domain coverage for first-complete, idempotent re-complete, prompt behavior, and audit at-most-once semantics"
      contains: "completeEvent domain service"
  key_links:
    - from: "src/domain/events/complete-event.js"
      to: "src/db/index.js"
      via: "managed sequelize transaction across Event lookup/update and AuditLog insert"
      pattern: "sequelize\\.transaction|models\\.(Event|AuditLog)"
    - from: "src/domain/events/complete-event.js"
      to: "src/domain/events/event-completion-errors.js"
      via: "guard failures mapped to stable completion categories"
      pattern: "new EventCompletionError"
---

<objective>
Implement transactional domain completion behavior for events, including idempotency and auditable first-complete writes.

Purpose: Lock down EVNT-02, EVNT-03, and AUDT-01 semantics in one domain service before HTTP wiring so endpoint behavior stays thin, deterministic, and easy to validate.
Output: Completion domain error taxonomy, `completeEvent` service with canonical response serializer, and domain tests covering first-complete, re-complete, prompt signaling, and audit side effects.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-event-completion-and-audit-traceability/04-CONTEXT.md
@.planning/phases/04-event-completion-and-audit-traceability/04-RESEARCH.md
@.planning/phases/03-net-status-retrieval/03-02-SUMMARY.md
@src/domain/events/status-and-completion-rules.js
@src/db/models/event.model.js
@src/db/models/audit-log.model.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define event-completion domain error taxonomy for locked failure branches</name>
  <files>src/domain/events/event-completion-errors.js</files>
  <action>Create `EventCompletionError` and explicit categories that cover: unknown event id, foreign-owned event, and invalid transition/state failures for PATCH completion flows. Include structured `issues` support so API mapping can emit the established issue-envelope shape (`error.code`, `error.category`, `error.message`, `error.issues`) without route-local formatting.</action>
  <verify>
    <automated>npm test -- test/domain/events/complete-event.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Completion domain errors export stable categories and carry enough metadata to drive deterministic HTTP status and issue-envelope mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Implement completeEvent transactional service with idempotent audit semantics</name>
  <files>src/domain/events/complete-event.js</files>
  <action>Implement `completeEvent({ eventId, actorUserId, now })` as a transaction-safe domain workflow: resolve event by id (not found branch), resolve parent item ownership via `event.item_id` (forbidden branch), and on first completion set `status=Completed` and `completed_at` once, then insert one `AuditLog` row with action `event.completed`, actor id, deterministic target encoding in `entity`, and timestamp. For already completed events, return idempotent success without mutating `completed_at` and without additional audit insert. Return canonical success payload with event fields (`id`, `item_id`, `type`, `due_date`, `amount`, `status`, `recurring`, timestamps, `completed_at`) plus `prompt_next_date`, where success always includes prompt metadata and missing recurrence metadata is treated as non-recurring.</action>
  <verify>
    <automated>npm test -- test/domain/events/complete-event.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Domain service enforces at-most-once completion transition and audit write semantics while returning deterministic completion payloads for first and repeated requests.</done>
</task>

<task type="auto">
  <name>Task 3: Add domain tests for completion transition, prompt signaling, and at-most-once audit writes</name>
  <files>test/domain/events/complete-event.test.js</files>
  <action>Add sqlite-backed domain tests proving: pending event completion returns canonical payload with `status=Completed`, persisted `completed_at`, and `prompt_next_date`; non-recurring and recurring events both include prompt metadata with deterministic semantics; unknown id maps to not-found category; foreign-owned event maps to forbidden category; and idempotent re-complete returns success while preserving original `completed_at` and keeping audit row count unchanged after the first completion.</action>
  <verify>
    <automated>npm test -- test/domain/events/complete-event.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Domain tests fail on any regression of completion idempotency, prompt rules, or audit side-effect guarantees and pass only when locked decisions are honored.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/events/complete-event.test.js --runInBand`; the suite must prove first-complete transition correctness, idempotent re-complete behavior, deterministic prompt signaling, and audit-at-most-once guarantees.</verification>

<success_criteria>
Event completion domain behavior is deterministic, transaction-safe, and fully test-backed so API wiring can expose EVNT-02, EVNT-03, and AUDT-01 without duplicating business logic.</success_criteria>

<output>
After completion, create `.planning/phases/04-event-completion-and-audit-traceability/04-01-SUMMARY.md`
</output>
