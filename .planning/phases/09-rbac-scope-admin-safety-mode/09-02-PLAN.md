---
phase: 09-rbac-scope-admin-safety-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/api/routes/items.routes.js
  - src/domain/items/create-item.js
  - src/domain/items/list-items.js
  - src/domain/items/update-item.js
  - src/domain/items/soft-delete-item.js
  - src/domain/events/list-events.js
  - src/domain/events/complete-event.js
autonomous: true
requirements:
  - AUTH-05
must_haves:
  truths:
    - "Standard users can only read records they own across list, detail, timeline, and activity endpoints."
    - "Standard users cannot mutate foreign-owned resources because all authorization checks derive from authenticated scope rather than request payloads."
    - "Create endpoints derive ownership from authenticated scope and ignore any client-provided owner identifier fields."
  artifacts:
    - path: "src/domain/items/create-item.js"
      provides: "Owner assignment derived from server scope, not request payload user_id"
      contains: "scope"
    - path: "src/domain/events/complete-event.js"
      provides: "Ownership checks for event complete/undo using scope-based owner resolution"
      contains: "scope"
  key_links:
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/create-item.js"
      via: "POST /items passes request scope and strips client owner fields"
      pattern: "createItem\(\{"
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/list-items.js"
      via: "read entry points pass normalized scope instead of client owner filters"
      pattern: "listItems\(\{"
---

<objective>
Enforce ownership boundaries end-to-end for standard users by removing payload-controlled ownership and threading normalized scope through item/event domain entry points.

Purpose: Satisfy strict owner-only access before enabling admin bypass flows so baseline security semantics are deterministic and regression-protected.
Output: Scope-derived owner enforcement across item/event domain operations with route-to-domain scope threading complete.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@src/api/routes/items.routes.js
@src/domain/items/create-item.js
@src/domain/events/complete-event.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove client-controlled ownership and thread scope-derived owner checks through item/event domain paths</name>
  <files>src/api/routes/items.routes.js, src/domain/items/create-item.js, src/domain/items/list-items.js, src/domain/items/update-item.js, src/domain/items/soft-delete-item.js, src/domain/events/list-events.js, src/domain/events/complete-event.js</files>
  <action>Refactor affected domain functions to accept shared request scope (or normalized owner filter derived from scope) rather than raw `actorUserId` primitives. For `POST /items`, derive owner from authenticated scope and reject/ignore payload `user_id`. Preserve existing item/event business behavior while ensuring read/mutate operations enforce owner scope for standard users.</action>
  <verify>
    <automated>npm test -- test/api/items-list-and-mutate.test.js --runInBand && npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All owner checks are server-scope-derived and no API path can write/read foreign data for standard users via payload manipulation.</done>
</task>

</tasks>

<verification>
Run owner-scope API suites and confirm standard users can only access/mutate owned records and cannot influence ownership via request payload/query fields.</verification>

<success_criteria>
Owner-only enforcement is consistent across items/events and route/domain scope threading is complete for follow-on regression locking.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-02-SUMMARY.md`
</output>
