---
phase: 09-rbac-scope-admin-safety-mode
plan: 13
type: execute
wave: 9
depends_on:
  - 09-06
  - 09-12
files_modified:
  - frontend/src/features/admin-scope/admin-safety-banner.tsx
  - frontend/src/features/admin-scope/admin-scope-context.tsx
  - frontend/src/__tests__/admin-safety-signals.test.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - TIME-04
must_haves:
  truths:
    - "Admin safety banner always reflects the current admin scope mode and lens immediately after scope transitions."
    - "Owner-to-all transitions render Lens: All users in the persistent banner without waiting for auth-session scope resync."
    - "Regression coverage fails if banner attribution is sourced from stale AuthContext scope instead of active AdminScopeContext state."
  artifacts:
    - path: "frontend/src/features/admin-scope/admin-safety-banner.tsx"
      provides: "Banner tuple rendering tied to active admin-scope context state"
      contains: "useAdminScope"
    - path: "frontend/src/features/admin-scope/admin-scope-context.tsx"
      provides: "Single source of truth for current mode/lens used by banner and switcher"
      contains: "mode"
    - path: "frontend/src/__tests__/admin-safety-signals.test.tsx"
      provides: "Regression for owner-to-all transition showing immediate Lens: All users"
      contains: "Lens: All users"
  key_links:
    - from: "frontend/src/features/admin-scope/admin-safety-banner.tsx"
      to: "frontend/src/features/admin-scope/admin-scope-context.tsx"
      via: "banner lens label derives from active mode/lens context values"
      pattern: "useAdminScope"
    - from: "frontend/src/__tests__/admin-safety-signals.test.tsx"
      to: "frontend/src/features/admin-scope/admin-safety-banner.tsx"
      via: "test simulates stale auth scope and asserts banner still shows all-users lens"
      pattern: "Admin mode active"
---

<objective>
Close the remaining verification gap where the persistent admin safety banner can show stale lens attribution after admin scope transitions.

Purpose: Complete AUTH-08 safeguard correctness by making the banner consume live admin scope state rather than stale lens context.
Output: Banner/context wiring fix plus regression coverage that reproduces and guards against stale owner-to-all lens labeling.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-VERIFICATION.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-06-SUMMARY.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-12-SUMMARY.md
@frontend/src/features/admin-scope/admin-safety-banner.tsx
@frontend/src/features/admin-scope/admin-scope-context.tsx
@frontend/src/__tests__/admin-safety-signals.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-anchor banner lens attribution to active AdminScopeContext state</name>
  <files>frontend/src/features/admin-scope/admin-safety-banner.tsx, frontend/src/features/admin-scope/admin-scope-context.tsx</files>
  <action>Update banner attribution logic so lens text is derived from the same mode/lens state that gates banner visibility (`useAdminScope`) instead of `AuthContext.sessionScope`. Keep actor identity from auth session, but ensure owner/all transitions immediately update the rendered tuple with no stale lens carryover.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/admin-safety-signals.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Banner no longer displays stale owner-lens attribution after admin scope updates; displayed lens always matches active admin scope state.</done>
</task>

<task type="auto">
  <name>Task 2: Add stale-auth-scope regression for owner-to-all banner lens transition</name>
  <files>frontend/src/__tests__/admin-safety-signals.test.tsx</files>
  <action>Add a focused regression that simulates a temporary auth/admin-scope mismatch during owner-to-all transition (AuthContext still owner lens while AdminScopeContext is all mode) and assert the persistent safety banner renders `Lens: All users` immediately. Keep test isolated to banner correctness and avoid broad test rewrites.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/admin-safety-signals.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Regression proves stale auth-session scope cannot reintroduce incorrect banner lens attribution after admin scope transitions.</done>
</task>

</tasks>

<verification>
Run the admin safety signal suite and confirm the persistent banner shows `Lens: All users` immediately on owner-to-all transitions even when auth session scope lags behind admin scope local state.
</verification>

<success_criteria>
The stale-lens banner gap is closed: persistent admin safeguard copy always reflects active scope mode/lens context across transitions and is covered by deterministic regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-13-SUMMARY.md`
</output>
