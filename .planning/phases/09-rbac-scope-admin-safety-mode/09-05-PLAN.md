---
phase: 09-rbac-scope-admin-safety-mode
plan: 05
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
files_modified:
  - src/db/migrations/20260226093000-expand-audit-log-actor-lens-attribution.js
  - src/db/models/audit-log.model.js
  - src/domain/items/create-item.js
  - src/domain/items/update-item.js
  - src/domain/items/soft-delete-item.js
  - src/domain/events/complete-event.js
  - test/api/audit-attribution.test.js
autonomous: true
requirements:
  - AUTH-07
must_haves:
  truths:
    - "Create, complete/undo, delete, and restore actions persist acting user plus selected lens attribution in audit-visible history."
    - "Update actions also persist actor+lens attribution so all Phase 9 write categories have consistent provenance."
    - "Persisted attribution values come from enforced request scope, not client-provided actor/lens inputs."
  artifacts:
    - path: "src/db/models/audit-log.model.js"
      provides: "Audit schema with actor_user_id and lens_user_id attribution fields"
      contains: "actor_user_id"
    - path: "src/domain/events/complete-event.js"
      provides: "Event completion/undo audit writes include actor+lens tuple"
      contains: "actor_user_id"
    - path: "test/api/audit-attribution.test.js"
      provides: "Regression coverage for attribution persistence across required write categories"
      contains: "restore"
  key_links:
    - from: "src/domain/events/complete-event.js"
      to: "src/db/models/audit-log.model.js"
      via: "event complete/undo writes audit rows with actor+lens attribution"
      pattern: "AuditLog\.create"
    - from: "src/domain/items/soft-delete-item.js"
      to: "src/db/models/audit-log.model.js"
      via: "delete and restore-category writes persist actor/lens context"
      pattern: "AuditLog\.create"
---

<objective>
Upgrade audit attribution from single-user logging to explicit actor+lens tuples at the schema and write-path layer, including delete/restore lifecycle rows.

Purpose: Provide immutable action provenance required for admin safety and ensure restore attribution expectations are encoded before API/UI read rendering is layered on top.
Output: Audit schema/model expansion, write-path audit updates including restore-category attribution coverage, and backend regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@src/db/models/audit-log.model.js
@src/domain/events/complete-event.js
@src/domain/items/soft-delete-item.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand audit schema for actor+lens tuple persistence with backwards-safe migration</name>
  <files>src/db/migrations/20260226093000-expand-audit-log-actor-lens-attribution.js, src/db/models/audit-log.model.js, test/api/audit-attribution.test.js</files>
  <action>Add audit columns for actor and lens attribution (`actor_user_id`, `lens_user_id`) while preserving compatibility for existing rows via migration backfill strategy. Update model constraints/default handling to keep historical rows readable while new writes require deterministic scope-derived attribution fields.</action>
  <verify>
    <automated>npm test -- test/api/audit-attribution.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Audit storage supports actor+lens attribution without breaking existing retained history rows.</done>
</task>

<task type="auto">
  <name>Task 2: Persist actor+lens tuple across required write categories and lock restore coverage</name>
  <files>src/db/migrations/20260226093000-expand-audit-log-actor-lens-attribution.js, src/db/models/audit-log.model.js, src/domain/items/create-item.js, src/domain/items/update-item.js, src/domain/items/soft-delete-item.js, src/domain/events/complete-event.js, test/api/audit-attribution.test.js</files>
  <action>Update create/update/delete/complete/undo write paths to store both values from request scope (lens null or actor-equivalent for owner mode). Explicitly include restore attribution coverage by adding restore-category action assertions in API tests (seeded/simulated restore row if dedicated restore endpoint is not yet in this phase) so AUTH-07 delete/restore requirements are enforced now.</action>
  <verify>
    <automated>npm test -- test/api/audit-attribution.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Create/complete/undo/delete/restore and update action categories all have actor+lens attribution coverage, with deterministic values tied to enforced request scope.</done>
</task>

</tasks>

<verification>
Run attribution-focused API suite and confirm create/update/delete/complete/undo/restore history rows persist actor+lens context end-to-end.
</verification>

<success_criteria>
Audit storage and write paths can answer who acted and on whose lens context for create, complete/undo, delete/restore, and update action categories without relying on inferred ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-05-SUMMARY.md`
</output>
