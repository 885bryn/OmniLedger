---
phase: 09-rbac-scope-admin-safety-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/api/auth/scope-context.js
  - src/api/routes/auth.routes.js
  - src/api/routes/users.routes.js
  - src/api/routes/items.routes.js
  - src/api/routes/events.routes.js
  - src/domain/items/list-items.js
  - src/domain/events/list-events.js
  - test/api/admin-scope-lens.test.js
autonomous: true
requirements:
  - AUTH-06
  - TIME-04
must_haves:
  truths:
    - "Admin defaults into all-data mode on login and can intentionally switch between all-users and specific-user lens."
    - "Server auth/session state carries mode+lens metadata and rejects non-admin attempts to set admin lens fields."
    - "Items/events/dashboard API reads honor resolved admin lens filters while standard users remain owner-scoped."
  artifacts:
    - path: "src/api/routes/auth.routes.js"
      provides: "Admin mode/lens API contract and session persistence for intentional scope changes"
      contains: "admin-scope"
    - path: "src/api/auth/scope-context.js"
      provides: "Single resolver for effective actor role, mode, and lens owner filter"
      exports: ["buildScopeContext", "resolveOwnerFilter"]
    - path: "test/api/admin-scope-lens.test.js"
      provides: "API regression coverage for admin default-all lens, intentional lens switching, and non-admin guardrails"
      contains: "admin"
  key_links:
    - from: "src/api/auth/scope-context.js"
      to: "src/domain/items/list-items.js"
      via: "scope resolver computes effective owner filter for all/admin+lens reads"
      pattern: "resolveOwnerFilter"
    - from: "src/api/routes/auth.routes.js"
      to: "src/api/auth/scope-context.js"
      via: "session and admin-scope update responses serialize enforced mode+lens metadata"
      pattern: "scope"
---

<objective>
Implement intentional admin all-data mode and user-lens filtering at the API/session boundary after owner-only enforcement is locked in.

Purpose: Deliver the secure enforcement layer for admin scope and selected-user lens behavior so downstream UI controls cannot drift from authorization truth.
Output: Admin scope API/session controls, scope-aware list routing/domain logic, and backend regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@src/api/auth/scope-context.js
@src/api/routes/auth.routes.js
@src/api/routes/items.routes.js
@src/domain/items/list-items.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-enforced admin scope mode + lens APIs with default-all admin login behavior</name>
  <files>src/api/auth/scope-context.js, src/api/routes/auth.routes.js, src/api/routes/users.routes.js, src/api/routes/items.routes.js, src/api/routes/events.routes.js, src/domain/items/list-items.js, src/domain/events/list-events.js, test/api/admin-scope-lens.test.js</files>
  <action>Implement session-backed admin scope state with default `all` lens at admin login. Add explicit API to update admin mode/lens intentionally (non-admins cannot set these fields), and serialize enforced scope metadata in auth/session payloads. Keep all owner-filter decisions derived from scope resolver, never request query/body fields, so standard users remain owner-only and admin reads can switch between all-users and selected-user lens.</action>
  <verify>
    <automated>npm test -- test/api/admin-scope-lens.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Admin default-all login scope and intentional lens switching are server-enforced, and all relevant list/feed APIs consume the same resolved scope contract.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize lens-owner resolution for all list and dashboard feed domain entry points</name>
  <files>src/api/auth/scope-context.js, src/api/routes/items.routes.js, src/api/routes/events.routes.js, src/api/routes/users.routes.js, src/domain/items/list-items.js, src/domain/events/list-events.js, test/api/admin-scope-lens.test.js</files>
  <action>Unify owner-filter mapping so items, events, and dashboard feed routes all call a shared `resolveOwnerFilter(scope)` path that supports owner-only, all-users, and selected-user lens semantics. Add regression assertions for lens target invalidation/fallback behavior and for non-admin attempts to request all-users scope via query params.</action>
  <verify>
    <automated>npm test -- test/api/admin-scope-lens.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>List/feed read paths share one scope resolver and consistently apply owner/all/specific-user lens behavior without query-level bypasses.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/admin-scope-lens.test.js --runInBand` and confirm admin scope mode/lens APIs and list/feed enforcement are consistent and non-admin override attempts are rejected.
</verification>

<success_criteria>
Admin all-data mode and selected-user lens behavior are intentional and server-enforced across auth/session plus read endpoints, forming a safe foundation for frontend lens controls.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-03-SUMMARY.md`
</output>
