---
phase: 09-rbac-scope-admin-safety-mode
plan: 09
type: execute
wave: 5
depends_on:
  - 09-05
files_modified:
  - src/domain/items/get-item-activity.js
  - frontend/src/lib/api-client.ts
  - frontend/src/features/audit/item-activity-timeline.tsx
  - frontend/src/__tests__/item-activity-attribution.test.tsx
autonomous: true
requirements:
  - AUTH-07
must_haves:
  truths:
    - "Activity responses expose actor+lens attribution fields required to identify who acted and under which lens context."
    - "Item activity timeline rows display Actor + Lens attribution for retained history including restore-category rows."
    - "Legacy rows without lens attribution remain renderable with explicit fallback labels rather than silent omission."
  artifacts:
    - path: "src/domain/items/get-item-activity.js"
      provides: "Activity payload mapping with actor/lens tuple fields"
      contains: "lens_user_id"
    - path: "frontend/src/features/audit/item-activity-timeline.tsx"
      provides: "UI row rendering for Actor + Lens attribution tuple"
      contains: "Actor"
    - path: "frontend/src/__tests__/item-activity-attribution.test.tsx"
      provides: "Regression coverage for tuple visibility and legacy-row fallback behavior"
      contains: "Lens"
  key_links:
    - from: "frontend/src/features/audit/item-activity-timeline.tsx"
      to: "src/domain/items/get-item-activity.js"
      via: "timeline consumes actor/lens tuple transport from activity API response"
      pattern: "actor_user_id|lens_user_id"
    - from: "frontend/src/lib/api-client.ts"
      to: "src/domain/items/get-item-activity.js"
      via: "client transport preserves attribution fields for timeline rendering"
      pattern: "activity"
---

<objective>
Surface persisted audit attribution in API transport and timeline UI so actor+lens provenance is visible in retained in-app history.

Purpose: Complete AUTH-07 visibility requirements after backend write attribution is established by separating read/render concerns from schema/write-path changes.
Output: Attribution-aware activity API mapping, timeline tuple rendering, client transport updates, and frontend regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-05-PLAN.md
@src/domain/items/get-item-activity.js
@frontend/src/features/audit/item-activity-timeline.tsx
@frontend/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose actor+lens attribution through item activity API mapping</name>
  <files>src/domain/items/get-item-activity.js, frontend/src/lib/api-client.ts, frontend/src/__tests__/item-activity-attribution.test.tsx</files>
  <action>Extend item activity response mapping to include `actor_user_id` and `lens_user_id` tuple metadata for create/update/delete/complete/undo/restore categories. Preserve compatibility for historical rows missing lens attribution by supplying explicit fallback semantics that the UI can render deterministically.</action>
  <verify>
    <automated>npm test -- frontend/src/__tests__/item-activity-attribution.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Activity API payloads provide attribution tuple fields across retained history while remaining compatible with legacy rows.</done>
</task>

<task type="auto">
  <name>Task 2: Render actor+lens tuple on activity timeline rows with legacy fallback labels</name>
  <files>frontend/src/features/audit/item-activity-timeline.tsx, frontend/src/__tests__/item-activity-attribution.test.tsx</files>
  <action>Update timeline row rendering to show `Actor: ... | Lens: ...` for each action, including restore rows, with fallback copy for missing historical lens values. Keep placement dense-but-readable per phase discretion and ensure owner-mode rows where actor and lens collapse remain explicit.</action>
  <verify>
    <automated>npm test -- frontend/src/__tests__/item-activity-attribution.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>In-app activity history visibly and consistently communicates actor+lens provenance for new and historical rows.</done>
</task>

</tasks>

<verification>
Run `npm test -- frontend/src/__tests__/item-activity-attribution.test.tsx --runInBand` and confirm activity rows display actor+lens attribution with deterministic legacy fallbacks.
</verification>

<success_criteria>
Retained activity history is attribution-explicit for all required write categories and remains readable for older rows with partial metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-09-SUMMARY.md`
</output>
