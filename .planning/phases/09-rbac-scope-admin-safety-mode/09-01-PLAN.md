---
phase: 09-rbac-scope-admin-safety-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/20260226090000-add-user-role-and-admin-scope-session-support.js
  - src/db/models/user.model.js
  - src/api/routes/auth.routes.js
  - src/api/auth/require-auth.js
  - src/api/auth/scope-context.js
  - test/api/auth-role-session.test.js
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Each authenticated user is resolved as either role user or role admin by server-side data, never by client input."
    - "Exactly one configured admin account receives admin role behavior after login, while all other users default to role user."
    - "Protected requests carry role-aware actor context from session resolution into API middleware."
  artifacts:
    - path: "src/db/models/user.model.js"
      provides: "Role field with strict user/admin enum defaulting to user"
      contains: "role"
    - path: "src/api/auth/require-auth.js"
      provides: "req.actor hydration including actor role from server session"
      exports: ["requireAuth"]
    - path: "test/api/auth-role-session.test.js"
      provides: "Regression coverage for role defaults, admin assignment, and role-aware session payload"
      contains: "admin"
  key_links:
    - from: "src/api/routes/auth.routes.js"
      to: "src/db/models/user.model.js"
      via: "session resolution reads persisted role for /auth/session and login response"
      pattern: "attributes: \[.*role"
    - from: "src/api/auth/require-auth.js"
      to: "src/api/auth/scope-context.js"
      via: "middleware builds reusable role-aware request scope"
      pattern: "buildScopeContext"
---

<objective>
Establish role-backed session identity so Phase 9 authorization can branch on trusted server role context rather than ad hoc route checks.

Purpose: Provide the minimum RBAC foundation before owner-scope and admin-mode behavior are threaded across APIs and frontend surfaces.
Output: User role schema migration/model updates, role-aware auth/session transport, shared scope-context helper, and API regression coverage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@src/db/models/user.model.js
@src/api/routes/auth.routes.js
@src/api/auth/require-auth.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent role schema and deterministic admin designation</name>
  <files>src/db/migrations/20260226090000-add-user-role-and-admin-scope-session-support.js, src/db/models/user.model.js, src/api/routes/auth.routes.js</files>
  <action>Create a migration that adds `Users.role` as enum-like `user|admin` with default `user`, backfills existing rows, and enforces non-null constraints. Designate one admin deterministically using configured admin email at migration/runtime bootstrap (tests should set role directly in fixtures). Update auth session resolution and login/register response payloads to include `role` from database attributes; do not accept or trust role from client payloads.</action>
  <verify>
    <automated>npm test -- test/api/auth-role-session.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>User role is persisted server-side with a default of user, and auth session payload always returns role from trusted DB state.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce shared role-aware request scope context at auth boundary</name>
  <files>src/api/auth/scope-context.js, src/api/auth/require-auth.js, test/api/auth-role-session.test.js</files>
  <action>Add a shared scope-context builder used by `requireAuth` that hydrates `req.actor` and `req.scope` with actor user id, actor role, and owner-default mode for non-admins. Keep behavior backward-compatible for existing routes by retaining `req.actor.userId` while adding role/scope fields required by downstream plans.</action>
  <verify>
    <automated>npm test -- test/api/auth-role-session.test.js --runInBand && npm test -- test/api/authz-session-enforcement.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Protected route middleware exposes a consistent role-aware scope object and preserves current authenticated-route contracts.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/auth-role-session.test.js --runInBand` and confirm user/admin role resolution is server-enforced and surfaced through authenticated session context.</verification>

<success_criteria>
Role assignment and middleware actor scope are in place so every protected request has trusted `user|admin` role context available for authorization decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-01-SUMMARY.md`
</output>
