---
phase: 09-rbac-scope-admin-safety-mode
plan: 04
type: execute
wave: 5
depends_on:
  - 09-06
files_modified:
  - frontend/src/app/shell/app-shell.tsx
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/features/admin-scope/admin-safety-banner.tsx
  - frontend/src/features/ui/confirmation-dialog.tsx
  - frontend/src/__tests__/admin-safety-signals.test.tsx
autonomous: true
requirements:
  - AUTH-08
must_haves:
  truths:
    - "Admin mode state is always visible via a persistent top warning banner showing Actor + Lens context."
    - "Exiting admin mode requires explicit confirmation before mode changes are applied."
    - "Banner and exit-confirmation UX remain active across dashboard, items, and events navigation surfaces."
  artifacts:
    - path: "frontend/src/features/admin-scope/admin-safety-banner.tsx"
      provides: "Persistent safety banner with visible mode state and actor/lens tuple"
      exports: ["AdminSafetyBanner"]
    - path: "frontend/src/features/ui/confirmation-dialog.tsx"
      provides: "Exit-admin-mode confirmation dialog contract requiring explicit user confirmation"
      exports: ["ConfirmationDialog"]
    - path: "frontend/src/app/shell/app-shell.tsx"
      provides: "Shell-level placement ensuring admin safety banner persistence across routed content"
      contains: "AdminSafetyBanner"
  key_links:
    - from: "frontend/src/app/shell/app-shell.tsx"
      to: "frontend/src/features/admin-scope/admin-safety-banner.tsx"
      via: "banner renders persistently above routed content while admin mode is active"
      pattern: "AdminSafetyBanner"
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/features/ui/confirmation-dialog.tsx"
      via: "admin exit action is gated by explicit confirmation before mode mutation fires"
      pattern: "confirm|ConfirmationDialog"
---

<objective>
Ship the admin safety frame so elevated scope is unmistakable at all times and leaving admin mode is always deliberate.

Purpose: Fulfill safeguard requirements for persistent mode visibility and explicit exit confirmation before mutation-focused affordances are layered in.
Output: Persistent admin warning banner, explicit exit-confirmation flow, and shell-level safety regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-06-PLAN.md
@frontend/src/app/shell/app-shell.tsx
@frontend/src/app/shell/user-switcher.tsx
@frontend/src/features/admin-scope/admin-safety-banner.tsx
@frontend/src/features/ui/confirmation-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent shell-level admin warning banner with Actor+Lens tuple</name>
  <files>frontend/src/features/admin-scope/admin-safety-banner.tsx, frontend/src/app/shell/app-shell.tsx, frontend/src/__tests__/admin-safety-signals.test.tsx</files>
  <action>Render a persistent top warning banner whenever admin mode is active and keep it mounted across dashboard/items/events route transitions. Banner content must display both acting user and current lens tuple (`Actor: ... | Lens: ...`) so safety context is continuously visible.</action>
  <verify>
    <automated>npm test -- frontend/src/__tests__/admin-safety-signals.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Admin mode state and actor/lens attribution remain visible at shell level throughout navigation while admin mode is active.</done>
</task>

<task type="auto">
  <name>Task 2: Gate admin-mode exit behind explicit confirmation in switcher flow</name>
  <files>frontend/src/app/shell/user-switcher.tsx, frontend/src/features/ui/confirmation-dialog.tsx, frontend/src/__tests__/admin-safety-signals.test.tsx</files>
  <action>Require explicit confirmation before executing any admin-mode exit mutation. Keep the dialog copy direct and safety-focused, and ensure cancellation leaves mode/lens untouched. Wire tests so accidental click paths cannot disable admin mode without confirmation.</action>
  <verify>
    <automated>npm test -- frontend/src/__tests__/admin-safety-signals.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Leaving admin mode always requires explicit confirmation, and canceling exit preserves the current admin scope state.</done>
</task>

</tasks>

<verification>
Run `npm test -- frontend/src/__tests__/admin-safety-signals.test.tsx --runInBand` and verify persistent banner visibility plus explicit confirmation before admin-mode exit.
</verification>

<success_criteria>
Admin safety mode remains visibly active at shell level and cannot be exited accidentally because mode transitions require explicit confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-04-SUMMARY.md`
</output>
