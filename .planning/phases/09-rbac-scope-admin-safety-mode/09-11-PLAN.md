---
phase: 09-rbac-scope-admin-safety-mode
plan: 11
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - test/api/items-create.test.js
  - test/api/items-list-and-mutate.test.js
  - test/api/events-complete.test.js
autonomous: true
requirements:
  - AUTH-05
must_haves:
  truths:
    - "Ownership enforcement regressions are caught when create/list/mutate flows receive foreign user identifiers."
    - "Create paths prove owner assignment comes from authenticated scope and not client payload fields."
    - "Item and event mutation tests fail if foreign-owner bypass behavior reappears."
  artifacts:
    - path: "test/api/items-create.test.js"
      provides: "Create-path assertions for scope-derived ownership and user_id payload bypass prevention"
      contains: "user_id"
    - path: "test/api/items-list-and-mutate.test.js"
      provides: "List/mutate assertions that foreign query/payload owner values cannot widen scope"
      contains: "owner"
    - path: "test/api/events-complete.test.js"
      provides: "Event complete/undo ownership guard regression coverage"
      contains: "forbidden|not_found"
  key_links:
    - from: "test/api/items-create.test.js"
      to: "src/domain/items/create-item.js"
      via: "tests assert create ownership is always derived from authenticated scope"
      pattern: "user_id|owner"
    - from: "test/api/events-complete.test.js"
      to: "src/domain/events/complete-event.js"
      via: "tests lock non-owner complete/undo denial behavior"
      pattern: "complete|undo"
---

<objective>
Lock API regression coverage for owner-scope enforcement after scope-threading changes are completed.

Purpose: Keep AUTH-05 durable by separating broad regression assertions from owner-scope implementation wiring.
Output: Tightened create/list/mutate/event API tests that prevent payload/query ownership bypass regressions.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-CONTEXT.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-RESEARCH.md
@.planning/phases/09-rbac-scope-admin-safety-mode/09-02-PLAN.md
@test/api/items-create.test.js
@test/api/items-list-and-mutate.test.js
@test/api/events-complete.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add create/list regression assertions for scope-only ownership derivation</name>
  <files>test/api/items-create.test.js, test/api/items-list-and-mutate.test.js</files>
  <action>Expand create/list tests so foreign `user_id` payload and query filters are ignored or rejected without affecting resolved owner scope. Add negative assertions that fail if client inputs can widen read scope or remap create ownership.</action>
  <verify>
    <automated>npm test -- test/api/items-create.test.js --runInBand && npm test -- test/api/items-list-and-mutate.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Create/list test coverage proves ownership remains authenticated-scope-derived regardless of client owner parameters.</done>
</task>

<task type="auto">
  <name>Task 2: Extend event mutation regression checks for foreign-owner bypass prevention</name>
  <files>test/api/events-complete.test.js</files>
  <action>Tighten event complete/undo tests to assert non-owner attempts are denied under owner scope and cannot be bypassed by client-provided user identifiers. Keep expectations aligned with current denial contract established for AUTH-05.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Event mutation regressions fail immediately if foreign ownership bypass behavior reappears.</done>
</task>

</tasks>

<verification>
Run ownership regression suites and confirm create/list/mutate/event paths reject client-controlled owner scope.
</verification>

<success_criteria>
AUTH-05 owner-scope behavior is regression-locked by focused API tests independent from implementation-threading changes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-rbac-scope-admin-safety-mode/09-11-SUMMARY.md`
</output>
