---
phase: 16-event-history-and-downloadable-workbook
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - package.json
  - package-lock.json
  - src/domain/exports/workbook-xlsx.js
  - src/api/routes/exports.routes.js
  - test/domain/exports/workbook-xlsx.test.js
  - test/api/exports-backup-scope.test.js
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
must_haves:
  truths:
    - "`GET /exports/backup.xlsx` returns a downloadable `.xlsx` attachment instead of JSON payload data."
    - "Downloaded workbook contains three sheets in locked order: `Assets`, `Financial Contracts`, `Event History`."
    - "Generated sheets open with frozen headers and filtering enabled, with date values serialized consistently from workbook model output."
  artifacts:
    - path: "src/domain/exports/workbook-xlsx.js"
      provides: "Workbook serializer that maps sheet contracts to ExcelJS workbook and binary buffer"
      exports: ["serializeWorkbookToXlsx"]
    - path: "src/api/routes/exports.routes.js"
      provides: "Binary attachment response wiring for export route with scope-authoritative workbook generation"
      exports: ["createExportsRouter"]
    - path: "test/domain/exports/workbook-xlsx.test.js"
      provides: "Serializer unit coverage for sheet order, header/filter defaults, and row/column mapping"
    - path: "test/api/exports-backup-scope.test.js"
      provides: "API regression coverage for attachment headers, parseable workbook payload, and scope-preserving row membership"
  key_links:
    - from: "src/api/routes/exports.routes.js"
      to: "src/domain/exports/export-scope-query.js"
      via: "route reads only session-derived scope dataset"
      pattern: "exportScopeQuery\(\{\s*scope:\s*req\.scope"
    - from: "src/api/routes/exports.routes.js"
      to: "src/domain/exports/workbook-xlsx.js"
      via: "workbook model serialized to XLSX buffer"
      pattern: "serializeWorkbookToXlsx"
    - from: "test/api/exports-backup-scope.test.js"
      to: "GET /exports/backup.xlsx"
      via: "attachment/content-type and workbook sheet assertions"
      pattern: "content-disposition|application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
---

<objective>
Ship backend XLSX generation and transport so export requests return a real downloadable workbook attachment with locked sheet structure and workbook defaults.

Purpose: Fulfill EXPT-01 and complete EXPT-02 download transport by converting the route from JSON contract response to binary `.xlsx` output while preserving Phase 14 scope trust boundaries.
Output: ExcelJS serializer module, binary export route response, and regression tests proving attachment behavior plus scope-safe workbook content.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-CONTEXT.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-RESEARCH.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-01-SUMMARY.md
@src/api/routes/exports.routes.js
@src/domain/exports/workbook-model.js
@test/api/exports-backup-scope.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XLSX serializer module using ExcelJS with locked sheet defaults</name>
  <files>package.json, package-lock.json, src/domain/exports/workbook-xlsx.js, test/domain/exports/workbook-xlsx.test.js</files>
  <action>Install `exceljs` and create a serializer that accepts workbook model sheets and returns an XLSX buffer via `writeBuffer()`. Write sheets in locked order (`Assets`, `Financial Contracts`, `Event History`) with human-readable sheet names, header row generation from column labels, row mapping from column keys, frozen header view, and auto-filter enabled on all populated sheet ranges. Add serializer tests validating deterministic sheet order, header mappings, freeze/filter config, and non-empty binary output.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-xlsx.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Serializer reliably converts workbook model contracts into a parseable `.xlsx` buffer with locked sheet defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Convert export route to attachment download while preserving scoped source data path</name>
  <files>src/api/routes/exports.routes.js</files>
  <action>Update `GET /exports/backup.xlsx` to keep existing auth and ignored client scope-hint behavior, build workbook from `exportScopeQuery({ scope: req.scope })`, serialize using the new XLSX module, and return binary attachment response (`Content-Disposition` filename + XLSX content type). Do not add any alternate unscoped data source or client-provided owner override path.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Route responds with downloadable XLSX bytes generated exclusively from server-scoped datasets.</done>
</task>

<task type="auto">
  <name>Task 3: Update API regressions for binary transport and scope-safe workbook content</name>
  <files>test/api/exports-backup-scope.test.js</files>
  <action>Refactor export API tests to assert attachment/content-type headers and parse workbook bytes (using ExcelJS in tests) to verify three-sheet presence/order and representative row membership in owner/all/lens scope modes. Preserve override-resistance assertions and ensure new checks still prove scope is not widened by query/body hints after JSON->binary migration.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Integration tests lock binary download contract and confirm scope enforcement survives transport migration.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/exports/workbook-xlsx.test.js --runInBand` then `npm test -- test/api/exports-backup-scope.test.js --runInBand` and confirm serializer and route transport/scope regressions both pass.
</verification>

<success_criteria>
- Export endpoint returns a downloadable `.xlsx` attachment with non-empty binary payload.
- Workbook contains `Assets`, `Financial Contracts`, and `Event History` sheets in locked order.
- All sheets include frozen headers and active auto-filter defaults in generated workbook.
- Route continues deriving export scope only from authenticated session scope, with regression coverage for owner/all/lens and override attempts.
</success_criteria>

<output>
After completion, create `.planning/phases/16-event-history-and-downloadable-workbook/16-02-SUMMARY.md`
</output>
