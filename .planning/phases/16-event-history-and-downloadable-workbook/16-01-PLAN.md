---
phase: 16-event-history-and-downloadable-workbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/exports/workbook-columns.js
  - src/domain/exports/workbook-model.js
  - test/domain/exports/workbook-model.test.js
autonomous: true
requirements:
  - EXPT-02
  - EXPT-05
  - RELA-02
must_haves:
  truths:
    - "Workbook model includes an `Event History` sheet in addition to existing `Assets` and `Financial Contracts` sheets."
    - "Event rows are newest-first and lifecycle-readable, with explicit markers for missing lifecycle values."
    - "Event rows expose both stable IDs and readable contract/asset references, preserving `UNLINKED` when targets cannot be resolved."
  artifacts:
    - path: "src/domain/exports/workbook-columns.js"
      provides: "Frozen `Event History` column contract with deterministic order"
      exports: ["EVENT_HISTORY_COLUMNS"]
    - path: "src/domain/exports/workbook-model.js"
      provides: "Deterministic event sheet projection with lifecycle-first fields and cross-sheet references"
      exports: ["buildWorkbookModel"]
    - path: "test/domain/exports/workbook-model.test.js"
      provides: "Regression coverage for Event History ordering, markers, and relationship fidelity"
  key_links:
    - from: "src/domain/exports/workbook-model.js"
      to: "src/domain/exports/workbook-columns.js"
      via: "column-driven Event History row projection"
      pattern: "EVENT_HISTORY_COLUMNS"
    - from: "src/domain/exports/workbook-model.js"
      to: "src/domain/exports/workbook-formatters.js"
      via: "shared explicit marker/date/label helpers"
      pattern: "toExplicitMarker|formatDate|resolveReadableReference"
    - from: "test/domain/exports/workbook-model.test.js"
      to: "src/domain/exports/workbook-model.js"
      via: "newest-first comparator and UNLINKED assertions"
      pattern: "Event History|UNLINKED"
---

<objective>
Extend the workbook domain model to produce a deterministic `Event History` sheet with lifecycle-readable rows and stable cross-sheet relationship fidelity.

Purpose: Fulfill EXPT-02, EXPT-05, and RELA-02 at the model layer before transport changes, while honoring locked decisions on newest-first order, explicit missing markers, and exact reference semantics.
Output: Updated workbook column/model domain code and tests that lock Event History schema, ordering, lifecycle fields, and link behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-CONTEXT.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-RESEARCH.md
@.planning/phases/15-assets-and-contracts-workbook-model/15-01-SUMMARY.md
@src/domain/exports/export-scope-query.js
@src/domain/exports/workbook-model.js
@src/domain/exports/workbook-columns.js
@src/domain/exports/workbook-formatters.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define frozen Event History columns for lifecycle-first readability</name>
  <files>src/domain/exports/workbook-columns.js</files>
  <action>Add a frozen `EVENT_HISTORY_COLUMNS` contract with deterministic order metadata and human-readable labels. Place lifecycle readability fields first (status/stage, due/completed dates, amount, recurrence/exception indicators), then stable IDs (`event_id`, `item_id`) and relationship references (`contract_id` plus readable contract label, `asset_id` plus readable asset label). Keep explicit sheet-key consistency with existing column contract patterns.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Event History column schema is frozen, deterministic, and encodes lifecycle-first readability plus ID/reference requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Event History projection with deterministic newest-first ordering and UNLINKED semantics</name>
  <files>src/domain/exports/workbook-model.js</files>
  <action>Extend `buildWorkbookModel()` to consume `datasets.events.rows`, resolve each event's source item via `item_id`, and project `Event History` rows using `EVENT_HISTORY_COLUMNS`. Enforce locked decisions: newest-first ordering with explicit comparator policy and stable tie-break, explicit markers for missing lifecycle values, and both ID + readable references for contract and asset links. Preserve unresolved links as ID plus `UNLINKED` label (not blanks), and ensure readable reference values match existing Assets/Financial Contracts formatting semantics.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Workbook model returns three sheets with Event History rows that are deterministic, lifecycle-readable, and relationship-faithful.</done>
</task>

<task type="auto">
  <name>Task 3: Lock Event History behavior with domain regression fixtures</name>
  <files>test/domain/exports/workbook-model.test.js</files>
  <action>Add/extend fixtures for mixed event scenarios: asset-linked events, financial-contract-linked events, unresolved links, missing lifecycle fields, and same-date tie-break rows. Assert sheet presence, column order, newest-first sorting, explicit markers, and ID/readable reference parity for contract and asset columns. Keep pre-existing Assets/Financial Contracts coverage passing unchanged.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Tests fail on any drift in Event History schema, ordering, lifecycle marker policy, or RELA-02 link fidelity.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/exports/workbook-model.test.js --runInBand` and confirm Event History assertions pass alongside existing Assets/Financial Contracts regressions.
</verification>

<success_criteria>
- `buildWorkbookModel` emits `Assets`, `Financial Contracts`, and `Event History` sheets with deterministic contracts.
- Event History rows prioritize lifecycle readability and newest-first ordering with stable tie-break behavior.
- Event rows include stable identifiers and readable contract/asset references, with explicit `UNLINKED` for unresolved links.
- Domain tests lock EXPT-05 and RELA-02 behavior against regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/16-event-history-and-downloadable-workbook/16-01-SUMMARY.md`
</output>
