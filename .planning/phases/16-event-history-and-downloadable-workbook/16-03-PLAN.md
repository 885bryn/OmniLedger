---
phase: 16-event-history-and-downloadable-workbook
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - frontend/src/features/export/use-export-backup.ts
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/user-switcher-export-action.test.tsx
autonomous: true
requirements:
  - EXPT-01
must_haves:
  truths:
    - "User-triggered export initiates a browser file download for the generated `.xlsx` workbook."
    - "Export action immediately enters pending state and blocks duplicate clicks until request completion."
    - "UI presents explicit success feedback on completion and actionable retry guidance on failure."
  artifacts:
    - path: "frontend/src/features/export/use-export-backup.ts"
      provides: "Binary download mutation flow using fetch/blob/object URL and cleanup"
      exports: ["useExportBackup"]
    - path: "frontend/src/app/shell/user-switcher.tsx"
      provides: "Export trigger wiring with pending/disabled state and user-facing status messages"
      exports: ["UserSwitcher"]
    - path: "frontend/src/__tests__/user-switcher-export-action.test.tsx"
      provides: "Frontend regression coverage for pending lock, download trigger, success, and actionable error feedback"
  key_links:
    - from: "frontend/src/features/export/use-export-backup.ts"
      to: "/exports/backup.xlsx"
      via: "credentialed GET + blob download transport"
      pattern: "fetch\(.*\/exports\/backup\.xlsx|response\.blob"
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/features/export/use-export-backup.ts"
      via: "button pending/disabled state and feedback rendering"
      pattern: "exportBackup\.isPending|triggerExport"
    - from: "frontend/src/__tests__/user-switcher-export-action.test.tsx"
      to: "frontend/src/app/shell/user-switcher.tsx"
      via: "UI-level export flow assertions"
      pattern: "Export Backup|Exporting"
---

<objective>
Align frontend export UX with binary workbook downloads so users can trigger and receive `.xlsx` files with clear in-app feedback.

Purpose: Complete EXPT-01 from the user interaction side by replacing JSON-only request handling with browser download behavior while honoring locked decisions for inline loading, duplicate-click prevention, success acknowledgment, and retry-oriented failure messaging.
Output: Updated export hook + shell wiring + translations/tests validating end-user download flow behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-CONTEXT.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-RESEARCH.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-02-SUMMARY.md
@frontend/src/features/export/use-export-backup.ts
@frontend/src/app/shell/user-switcher.tsx
@frontend/src/__tests__/user-switcher-export-action.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace JSON export call with binary download flow</name>
  <files>frontend/src/features/export/use-export-backup.ts</files>
  <action>Update the export mutation to use browser `fetch` directly for `GET /exports/backup.xlsx` with credentials, validate HTTP success, read `Response.blob()`, derive filename from `Content-Disposition` when present, and trigger download via object URL + temporary anchor cleanup. Keep mutation state semantics compatible with existing `UserSwitcher` pending/success/error behavior.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Triggering export initiates a browser download for returned XLSX payload and still exposes reliable mutation states.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten inline export feedback and retry guidance copy</name>
  <files>frontend/src/app/shell/user-switcher.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json</files>
  <action>Keep immediate inline pending state and duplicate-click prevention on the export button, and refine success/error copy to be explicit and actionable (for example: success confirms download start/completion, failure advises retry after checking connection/session). Preserve existing admin lens/actor context behavior and avoid adding new export controls beyond phase scope.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Export UI clearly communicates pending, success, and retry guidance while keeping one-click flow and duplicate-click lock.</done>
</task>

<task type="auto">
  <name>Task 3: Update frontend export tests for binary transport expectations</name>
  <files>frontend/src/__tests__/user-switcher-export-action.test.tsx</files>
  <action>Refactor export action tests to mock browser fetch/blob/object URL behavior instead of `apiRequest` JSON assertions. Cover: standard user trigger calls correct endpoint, pending state appears immediately, button remains disabled during in-flight request, success feedback appears after resolved blob, failure path shows actionable retry/error message, and admin modes retain unchanged trigger behavior.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend tests lock EXPT-01 download UX behavior and prevent regression to JSON-only export transport.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx --runInBand` and confirm pending/disabled, success, and error guidance checks pass for user and admin contexts.
</verification>

<success_criteria>
- Clicking `Export Backup` starts a real browser download for `/exports/backup.xlsx` responses.
- Export button enters immediate pending state and blocks duplicate clicks until completion.
- UI shows explicit success confirmation and actionable retry-oriented failure messaging.
- Frontend tests assert binary-download flow rather than JSON helper behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/16-event-history-and-downloadable-workbook/16-03-SUMMARY.md`
</output>
