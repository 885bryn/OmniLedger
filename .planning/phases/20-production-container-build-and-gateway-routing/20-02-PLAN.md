---
phase: 20-production-container-build-and-gateway-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/Dockerfile.prod
  - frontend/nginx/default.conf.template
  - frontend/docker-entrypoint.sh
autonomous: true
requirements:
  - CONT-02
  - CONT-03
must_haves:
  truths:
    - "Maintainer can build a frontend multi-stage production image from `frontend/Dockerfile.prod` that serves compiled assets via Nginx."
    - "Browser API requests use a single `/api/*` prefix and are proxied through Nginx to a backend target derived from `NAS_STATIC_IP`."
    - "When upstream backend is unreachable, `/api/*` returns a clear JSON 502 response instead of default Nginx HTML."
    - "Gateway routing remains strict in production: missing/invalid backend target env does not silently fall back to localhost."
  artifacts:
    - path: "frontend/Dockerfile.prod"
      provides: "Frontend multi-stage build and Nginx runtime image"
    - path: "frontend/nginx/default.conf.template"
      provides: "Nginx SPA + /api gateway routing contract"
    - path: "frontend/docker-entrypoint.sh"
      provides: "Runtime env-to-Nginx target resolution and strict startup validation"
  key_links:
    - from: "frontend/docker-entrypoint.sh"
      to: "frontend/nginx/default.conf.template"
      via: "envsubst renders backend upstream from NAS_STATIC_IP"
      pattern: "envsubst|NAS_STATIC_IP|BACKEND_UPSTREAM"
    - from: "frontend/nginx/default.conf.template"
      to: "http://${NAS_STATIC_IP}:8080"
      via: "location /api/ proxy_pass to env-rendered upstream"
      pattern: "location /api/|proxy_pass"
    - from: "frontend/nginx/default.conf.template"
      to: "JSON 502 envelope"
      via: "intercept upstream gateway errors"
      pattern: "error_page 502|application/json"
---

<objective>
Create a frontend production container that serves built assets and acts as the API gateway layer for same-origin browser traffic.

Purpose: Fulfill CONT-02 and CONT-03 by shipping a multi-stage frontend image with strict NAS-driven Nginx proxy routing, eliminating production CORS breakage through `/api/*` gatewaying.
Output: New frontend `Dockerfile.prod`, runtime Nginx template, and entrypoint wiring that derives backend targets from `NAS_STATIC_IP` with explicit JSON gateway failures.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-production-container-build-and-gateway-routing/20-CONTEXT.md
@.planning/phases/19-environment-driven-production-configuration/19-02-SUMMARY.md
@frontend/package.json
@frontend/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build frontend multi-stage Dockerfile.prod with compiled asset runtime</name>
  <files>frontend/Dockerfile.prod</files>
  <action>Create a frontend multi-stage production Dockerfile under `frontend/`. Build stage must install deps and run the Vite build. Runtime stage must serve compiled `dist/` assets from Nginx. Set build-time API base to `/api` (for same-origin gateway calls) while preserving explicit `VITE_API_BASE_URL` override support if provided. Keep runtime image lean by excluding build toolchain from final layer.</action>
  <verify>
    <automated>docker build -f frontend/Dockerfile.prod -t hact-frontend-prod:test frontend</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend production image builds successfully and serves compiled assets from Nginx runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Implement strict NAS-derived Nginx /api gateway routing</name>
  <files>frontend/nginx/default.conf.template, frontend/docker-entrypoint.sh</files>
  <action>Add an Nginx config template and entrypoint script that derive backend upstream from `NAS_STATIC_IP` at container start, render the config, and fail startup if required gateway env is missing/invalid (no localhost fallback, per user decision). Route browser API traffic through `location /api/` only, proxying to `http://${NAS_STATIC_IP}:8080`. Keep CORS posture same-origin oriented (no permissive wildcard headers). Configure upstream failure interception so 502/503/504 return a consistent JSON payload with content-type `application/json` and actionable message fields.</action>
  <verify>
    <automated>docker run --rm --name hact-frontend-prod-missing -p 18085:80 hact-frontend-prod:test; docker run -d --name hact-frontend-prod-upstream -p 18086:80 -e NAS_STATIC_IP=203.0.113.10 hact-frontend-prod:test && docker exec hact-frontend-prod-upstream wget -qO- http://127.0.0.1/api/health && docker rm -f hact-frontend-prod-upstream</automated>
    <manual>Confirm first run exits non-zero due to missing NAS_STATIC_IP. Confirm second run returns JSON 502 envelope from `/api/health` (not an Nginx HTML page).</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Gateway startup is strict and `/api/*` consistently proxies to NAS-derived backend targets with JSON error handling on upstream failures.</done>
</task>

<task type="auto">
  <name>Task 3: Prove same-origin gateway behavior avoids production CORS breakage</name>
  <files>frontend/Dockerfile.prod, frontend/nginx/default.conf.template</files>
  <action>Validate that frontend runtime serves SPA assets and API requests flow through same-origin `/api/*` instead of direct cross-origin backend calls. Ensure Nginx serves static app routes and keeps API routing isolated under `/api`. Verification should explicitly confirm that API calls no longer depend on backend CORS allowlist behavior in browser production usage because requests originate from the gateway origin.</action>
  <verify>
    <automated>docker run -d --name hact-frontend-prod-check -p 18087:80 -e NAS_STATIC_IP=203.0.113.10 hact-frontend-prod:test && docker exec hact-frontend-prod-check wget -qO- http://127.0.0.1/ && docker exec hact-frontend-prod-check wget -S -O- http://127.0.0.1/api/health 2>&1 && docker rm -f hact-frontend-prod-check</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend production container demonstrably serves app assets and routes API traffic through Nginx gateway endpoints without requiring browser cross-origin calls.</done>
</task>

</tasks>

<verification>
Run `docker build -f frontend/Dockerfile.prod -t hact-frontend-prod:test frontend` and execute Task 2 + Task 3 container checks to confirm strict env-driven gateway routing and JSON 502 behavior.
</verification>

<success_criteria>
- `frontend/Dockerfile.prod` builds a multi-stage production image that serves compiled assets in runtime.
- Frontend API traffic routes through Nginx `/api/*` to `NAS_STATIC_IP`-derived backend targets.
- Missing gateway env config causes startup failure (no hidden localhost fallback).
- Upstream gateway failures return JSON 502 responses, avoiding default Nginx HTML error pages.
</success_criteria>

<output>
After completion, create `.planning/phases/20-production-container-build-and-gateway-routing/20-02-SUMMARY.md`
</output>
