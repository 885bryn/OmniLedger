---
phase: 20-production-container-build-and-gateway-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.prod
  - .dockerignore
autonomous: true
requirements:
  - CONT-01
must_haves:
  truths:
    - "Maintainer can build the backend production image directly from `Dockerfile.prod` with no manual patching."
    - "Backend production container starts through the existing startup preflight/migration path and listens on port 8080."
    - "Backend production container exits fast with clear env-validation diagnostics when required production env vars are missing."
  artifacts:
    - path: "Dockerfile.prod"
      provides: "Production backend container build/runtime contract"
    - path: ".dockerignore"
      provides: "Deterministic backend production build context with local artifacts excluded"
  key_links:
    - from: "Dockerfile.prod"
      to: "src/scripts/startup.js"
      via: "container CMD uses startup preflight before server boot"
      pattern: "CMD.*src/scripts/startup\\.js"
    - from: "Dockerfile.prod"
      to: "package-lock.json"
      via: "runtime dependency install for reproducible production image"
      pattern: "npm ci --omit=dev"
---

<objective>
Create a deployment-safe backend production image contract that can be built and run consistently on NAS-targeted infrastructure.

Purpose: Fulfill CONT-01 by separating production container behavior from local/dev assumptions and guaranteeing startup uses the validated production preflight path.
Output: New root-level backend `Dockerfile.prod` plus build-context guardrails that keep image contents deterministic and deployment-ready.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-production-container-build-and-gateway-routing/20-CONTEXT.md
@.planning/phases/19-environment-driven-production-configuration/19-01-SUMMARY.md
@Dockerfile
@src/scripts/startup.js
@src/api/server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root backend Dockerfile.prod for deployment runtime</name>
  <files>Dockerfile.prod</files>
  <action>Create a production-focused backend image definition separate from the current local/development Dockerfile. Keep runtime behavior aligned to Phase 19 fail-fast env preflight by launching `node src/scripts/startup.js` (not direct server boot). Use production dependency install (`npm ci --omit=dev`), keep container listener on `0.0.0.0:8080`, and expose port 8080. Include deployment-suitable defaults (`NODE_ENV=production`, `HOST=0.0.0.0`, `PORT=8080`) and avoid dev-only tooling in the final runtime image.</action>
  <verify>
    <automated>docker build -f Dockerfile.prod -t hact-backend-prod:test .</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`Dockerfile.prod` builds successfully and encodes the backend production runtime contract used for deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic backend production build context exclusions</name>
  <files>.dockerignore</files>
  <action>Create root `.dockerignore` tuned for backend production builds so local artifacts do not leak into image context (`node_modules`, test output, git metadata, local env files, planning archives). Keep required runtime sources available (backend `src`, migrations, package manifests). This keeps build size stable and prevents accidental secret/context bleed in production image builds.</action>
  <verify>
    <automated>docker build -f Dockerfile.prod -t hact-backend-prod:test .</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Backend production image context is deterministic, excludes local/noise artifacts, and still builds without missing runtime files.</done>
</task>

<task type="auto">
  <name>Task 3: Prove production runtime behavior and fail-fast env guardrails</name>
  <files>Dockerfile.prod</files>
  <action>Validate container runtime behavior with two concrete checks: (1) run with required production env values and confirm `/health` responds 200 from port 8080 after startup, and (2) run without required production env variables and confirm startup exits non-zero with aggregated validation diagnostics from Phase 19 preflight. Keep this verification strictly command-driven so maintainers can reproduce image readiness before Portainer deployment.</action>
  <verify>
    <automated>docker rm -f hact-backend-prod-ok hact-backend-prod-bad >/dev/null 2>&1; docker run -d --name hact-backend-prod-ok -p 18080:8080 -e NODE_ENV=production -e SESSION_SECRET=test-session-secret -e DB_NAME=hact -e DB_USER=postgres -e DB_PASSWORD=test-db-password -e DB_HOST=host.docker.internal -e DB_PORT=5432 -e NAS_STATIC_IP=192.168.1.50 -e HACT_ADMIN_EMAIL=admin@example.com hact-backend-prod:test && docker logs hact-backend-prod-ok --tail 50 && docker rm -f hact-backend-prod-ok >/dev/null 2>&1; docker run --name hact-backend-prod-bad -e NODE_ENV=production -e SESSION_SECRET=test-session-secret hact-backend-prod:test</automated>
    <manual>Confirm the second run exits non-zero and logs missing required production env variables rather than silently booting.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Maintainer has repeatable proof that backend production image both boots correctly with valid env and fails fast with clear diagnostics on invalid env.</done>
</task>

</tasks>

<verification>
Run `docker build -f Dockerfile.prod -t hact-backend-prod:test .` and then execute the runtime checks from Task 3 to confirm healthy boot on valid env and fail-fast on invalid production env.
</verification>

<success_criteria>
- `Dockerfile.prod` exists at repository root and builds a backend production image.
- Backend production container starts via `src/scripts/startup.js` and serves on port 8080 when required env values are provided.
- Missing required production env values prevent backend container startup with explicit diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/20-production-container-build-and-gateway-routing/20-01-SUMMARY.md`
</output>
