---
phase: 10-financial-contract-occurrence-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 10-04
files_modified:
  - src/domain/items/list-items.js
  - src/domain/items/get-item-net-status.js
  - src/domain/events/list-events.js
  - test/domain/items/list-items.test.js
  - test/domain/items/get-item-net-status.test.js
  - test/api/items-list-and-mutate.test.js
  - test/api/items-net-status.test.js
  - test/api/events-complete.test.js
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
autonomous: true
gap_closure: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-06
must_haves:
  truths:
    - "A recurring FinancialItem linked to an asset is visible in Items list views (including commitments/income filters), not only in Due events surfaces."
    - "The linked asset commitments section includes FinancialItem children linked by the asset relationship and keeps subtype/amount data visible."
    - "Completing a recurring occurrence from Events keeps that occurrence trackable in status=all event reads (upcoming or history) instead of disappearing from lifecycle views."
  artifacts:
    - path: "src/domain/items/list-items.js"
      provides: "Subtype-aware list filtering and canonical FinancialItem fields for list consumers"
      contains: "FinancialItem"
    - path: "src/domain/items/get-item-net-status.js"
      provides: "Asset-linked child financial query that includes FinancialItem relationships"
      contains: "linked_asset_item_id"
    - path: "test/api/events-complete.test.js"
      provides: "Regression proving projected completion remains visible in subsequent event list reads"
      contains: "projected-"
  key_links:
    - from: "src/domain/items/list-items.js"
      to: "frontend/src/pages/items/item-list-page.tsx"
      via: "GET /items filter=commitments|income returns FinancialItem rows with subtype metadata"
      pattern: "filter"
    - from: "src/domain/items/get-item-net-status.js"
      to: "frontend/src/pages/items/item-detail-page.tsx"
      via: "GET /items/:id/net-status returns linked child commitments for asset detail tab"
      pattern: "child_commitments"
    - from: "src/domain/events/list-events.js"
      to: "frontend/src/pages/events/events-page.tsx"
      via: "status=all event refresh includes materialized completion rows after complete"
      pattern: "status"
---

<objective>
Close the diagnosed UAT visibility/lifecycle regression where recurring FinancialItem records appear only in Due events and disappear from expected item/asset surfaces.

Purpose: Restore FIN-02 and FIN-04 behavior across UI/API wiring so FinancialItem parent and occurrence state remain consistently visible before and after event completion.
Output: Domain query fixes plus regression coverage proving FinancialItem visibility in Items, asset commitments, and post-completion event lifecycle reads.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-CONTEXT.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-RESEARCH.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-VERIFICATION.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-01-SUMMARY.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-02-SUMMARY.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-03-SUMMARY.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-04-SUMMARY.md
@src/domain/items/list-items.js
@src/domain/items/get-item-net-status.js
@src/domain/events/list-events.js
@frontend/src/pages/items/item-list-page.tsx
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/pages/events/events-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FinancialItem visibility contracts in item list and asset commitments queries</name>
  <files>src/domain/items/list-items.js, src/domain/items/get-item-net-status.js, test/domain/items/list-items.test.js, test/domain/items/get-item-net-status.test.js, test/api/items-list-and-mutate.test.js, test/api/items-net-status.test.js</files>
  <action>Update item-domain query logic so commitments/income filters include unified `FinancialItem` rows based on subtype (`type=Commitment|Income`) while keeping legacy `FinancialCommitment`/`FinancialIncome` compatibility. Expand canonical list/net-status payload fields to include FinancialItem contract columns (`title`, `type`, `frequency`, `default_amount`, `status`, `linked_asset_item_id`) needed by existing UI display helpers. In net-status child lookup, include asset-linked financial children when either `parent_item_id` or `linked_asset_item_id` references the root asset, preserve owner+soft-delete guards, and ensure summary amount fallback uses FinancialItem default amount when attribute amount keys are absent.</action>
  <verify>
    <automated>npm test -- test/domain/items/list-items.test.js test/domain/items/get-item-net-status.test.js test/api/items-list-and-mutate.test.js test/api/items-net-status.test.js</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Recurring FinancialItem rows are returned by the same item and asset-commitments APIs used by Items tab/detail UI, with subtype and amount data intact.</done>
</task>

<task type="auto">
  <name>Task 2: Lock post-completion lifecycle visibility so recurring occurrences do not disappear</name>
  <files>src/domain/events/list-events.js, test/api/events-complete.test.js, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Add a regression that reproduces the reported flow: create recurring FinancialItem, complete occurrence from Events, then re-read `status=all` events and assert the materialized row remains visible in grouped output. If current list merge/projection behavior drops completed rows, patch `list-events` merge/dedupe ordering to keep persisted completed occurrences present after projected completion while preserving FIN-04 no long-horizon generation and FIN-06 closed-item suppression. Add/adjust frontend events-flow assertions so completion refresh still renders an actionable row state (`Completed` + `Undo`) after refetch.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js && npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Completing from Events no longer causes recurring occurrences to vanish from lifecycle views; status=all read paths and UI regression coverage both confirm persistent visibility.</done>
</task>

</tasks>

<verification>
Run item and event regression suites, then confirm the recreated UAT scenario holds: the same recurring FinancialItem is visible in Items list filters, visible under linked asset commitments, and still represented in Events after completion with `status=all` refresh.
</verification>

<success_criteria>
The diagnosed visibility gap is closed end-to-end: FinancialItem records are consistently discoverable across Items, asset commitments, and Events lifecycle states, with automated regressions preventing reintroduction.
</success_criteria>

<output>
After completion, create `.planning/phases/10-financial-contract-occurrence-foundation/10-05-SUMMARY.md`
</output>
