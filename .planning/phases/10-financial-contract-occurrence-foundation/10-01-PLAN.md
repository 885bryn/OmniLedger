---
phase: 10-financial-contract-occurrence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/20260226100000-financial-item-contract-foundation.js
  - src/db/models/item.model.js
  - src/domain/items/default-attributes.js
  - src/domain/items/minimum-attribute-keys.js
  - src/domain/items/create-item.js
  - src/domain/items/item-event-sync.js
  - src/api/routes/items.routes.js
  - test/api/items-create.test.js
autonomous: true
requirements:
  - FIN-01
  - FIN-03
must_haves:
  truths:
    - "User can create a Financial item parent with required contract fields and an explicit Commitment or Income subtype."
    - "Linked asset is optional, but create flow supports confirmation intent for unlinked financial items instead of hard blocking."
    - "One-time Financial item creation persists parent plus first child occurrence atomically in one transaction."
  artifacts:
    - path: "src/db/models/item.model.js"
      provides: "FinancialItem parent contract shape and subtype-aware validation"
      contains: "FinancialItem"
    - path: "src/domain/items/create-item.js"
      provides: "Transaction-managed parent create plus first-occurrence materialization"
      exports: ["createItem"]
    - path: "test/api/items-create.test.js"
      provides: "Regression coverage for FIN-01 field requirements and FIN-03 atomic one-time create"
      contains: "one-time"
  key_links:
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/create-item.js"
      via: "POST /items passes financial contract payload and scope-derived ownership"
      pattern: "createItem\\(\\{"
    - from: "src/domain/items/create-item.js"
      to: "src/domain/items/item-event-sync.js"
      via: "One-time create path writes parent and first occurrence in same transaction"
      pattern: "syncItemEvent"
---

<objective>
Establish explicit FinancialItem parent contract persistence and transactional one-time creation semantics.

Purpose: Replace implicit JSON-only financial behavior with a durable contract aggregate that satisfies FIN-01 and FIN-03 before recurrence projection work.
Output: Migration/model/domain updates for FinancialItem parent fields, subtype handling, optional linked-asset confirmation handling, and one-time parent+child transactional creation tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-CONTEXT.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-RESEARCH.md
@src/db/models/item.model.js
@src/domain/items/create-item.js
@src/domain/items/item-event-sync.js
@test/api/items-create.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit FinancialItem parent contract fields and subtype/frequency constraints</name>
  <files>src/db/migrations/20260226100000-financial-item-contract-foundation.js, src/db/models/item.model.js, src/domain/items/default-attributes.js, src/domain/items/minimum-attribute-keys.js</files>
  <action>Create migration and model updates that introduce FinancialItem parent semantics with explicit required fields `title`, `type`, `frequency`, `default_amount`, `status`, optional linked asset, and subtype enum (`Commitment|Income`). Keep ownership server-derived and preserve dual-dialect behavior (Postgres + sqlite test runtime). Enforce preset recurrence intervals only (e.g., one_time/weekly/monthly/yearly), and do not add custom rule DSL or long-horizon generation tables in this phase.</action>
  <verify>
    <automated>npm test -- test/api/items-create.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>FinancialItem parent contracts require the FIN-01 field set, subtype is explicit, and recurrence is constrained to preset intervals.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transactional one-time create path with optional-asset confirmation contract</name>
  <files>src/domain/items/create-item.js, src/domain/items/item-event-sync.js, src/api/routes/items.routes.js, test/api/items-create.test.js</files>
  <action>Refactor item creation so FinancialItem one-time payloads create parent and first Event occurrence in one managed Sequelize transaction together with audit attribution. Add rollback-safe validation for missing required financial fields and invalid linked asset references. Support risky-but-valid no-asset saves through an explicit confirmation flag in the API contract (required when linked asset is omitted for financial subtypes), returning actionable validation issues when confirmation is missing.</action>
  <verify>
    <automated>npm test -- test/api/items-create.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>One-time FinancialItem creates persist parent + first occurrence atomically, and unlinked-asset saves require explicit confirmation rather than hard-block failure.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/items-create.test.js --runInBand` and confirm FinancialItem parent validation plus atomic parent/first-occurrence persistence behavior.
</verification>

<success_criteria>
Financial parent contracts are explicitly modeled and one-time create now guarantees immediate parent + first occurrence persistence under one transaction.
</success_criteria>

<output>
After completion, create `.planning/phases/10-financial-contract-occurrence-foundation/10-01-SUMMARY.md`
</output>
