---
phase: 10-financial-contract-occurrence-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/domain/events/list-events.js
  - src/domain/events/complete-event.js
  - src/domain/items/item-event-sync.js
  - src/api/routes/events.routes.js
  - test/api/events-list.test.js
  - test/api/events-complete.test.js
autonomous: true
requirements:
  - FIN-02
  - FIN-04
  - FIN-06
must_haves:
  truths:
    - "User can view child occurrences with due date, amount, status, and owner scope bound to FinancialItem ownership."
    - "Recurring FinancialItems project upcoming occurrences on read without pre-generating long-horizon rows."
    - "Closed FinancialItems stop generating future projected occurrences while preserving persisted history."
  artifacts:
    - path: "src/domain/events/list-events.js"
      provides: "Scoped occurrence read model with projection-on-read and upcoming/history grouping"
      exports: ["listEvents"]
    - path: "src/domain/events/complete-event.js"
      provides: "Status-focused occurrence mutations that materialize projected rows before mutation"
      exports: ["completeEvent", "undoEventCompletion"]
    - path: "test/api/events-list.test.js"
      provides: "FIN-02/FIN-04/FIN-06 regression coverage for scoped occurrence reads and closed-contract projection gates"
      contains: "closed"
  key_links:
    - from: "src/api/routes/events.routes.js"
      to: "src/domain/events/list-events.js"
      via: "GET /events delegates to scoped projected occurrence read model"
      pattern: "listEvents\\(\\{"
    - from: "src/domain/events/list-events.js"
      to: "src/domain/items/item-event-sync.js"
      via: "Projection path dedupes/materializes nearest occurrences without long-horizon generation"
      pattern: "syncItemEvent"
---

<objective>
Implement owner-scoped occurrence lifecycle and recurrence projection baseline for FinancialItem contracts.

Purpose: Deliver FIN-02, FIN-04, and FIN-06 by making event reads deterministic and recurrence-aware while preventing projection growth and closed-contract drift.
Output: Event domain/routing updates with projection-on-read semantics, closed-contract projection gate, projected-row materialization for status edits, and regression suites.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-CONTEXT.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-RESEARCH.md
@.planning/phases/10-financial-contract-occurrence-foundation/10-01-SUMMARY.md
@src/domain/events/list-events.js
@src/domain/events/complete-event.js
@test/api/events-list.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add projection-on-read occurrence listing with closed-contract status gate</name>
  <files>src/domain/events/list-events.js, src/domain/items/item-event-sync.js, src/api/routes/events.routes.js, test/api/events-list.test.js</files>
  <action>Refactor event list behavior to derive child occurrences from FinancialItem parent contracts: return persisted rows plus bounded projected upcoming rows for recurring contracts, grouped as upcoming/current first and history second. Do not pre-generate long-horizon rows at write time. Apply owner scope using existing scope-context rules and block projection for parent contracts with status `Closed`.</action>
  <verify>
    <automated>npm test -- test/api/events-list.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Occurrence listing is owner-scoped, recurrence-aware, and projection is suppressed for closed parent contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize projected occurrence exceptions for status-focused edits</name>
  <files>src/domain/events/complete-event.js, src/api/routes/events.routes.js, test/api/events-complete.test.js</files>
  <action>Update status mutation paths so operations against projected future occurrences first instantiate a persisted child Event for that contract/date, then apply status transition (default-safe status-focused editing). Keep ownership and 404-style denial semantics from Phase 9 intact. Ensure instantiated exceptions dedupe by contract/date to avoid duplicate rows during repeated reads or retries.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Status edits against projected occurrences create one persisted exception row and then mutate status without ownership-policy regressions.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/events-list.test.js --runInBand && npm test -- test/api/events-complete.test.js --runInBand` and confirm projection, owner scope, exception materialization, and close-status gating all hold.
</verification>

<success_criteria>
Recurring occurrence behavior is projection-based (not pre-generated), child events remain owner-scoped, and closed contracts never generate new projected future rows.
</success_criteria>

<output>
After completion, create `.planning/phases/10-financial-contract-occurrence-foundation/10-02-SUMMARY.md`
</output>
