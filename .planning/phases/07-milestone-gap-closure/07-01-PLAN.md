---
phase: 07-milestone-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/lib/query-keys.ts
  - frontend/src/features/events/complete-event-row-action.tsx
  - frontend/src/features/events/follow-up-modal.tsx
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
  - frontend/src/__tests__/user-switcher-cache-refresh.test.tsx
autonomous: true
requirements:
  - EVNT-03
gap_closure: true
must_haves:
  truths:
    - "Switching actor in the header immediately refreshes dashboard, events, and items data for the newly selected user without showing stale prior-user values."
    - "Completing a non-recurring event opens the follow-up modal from the completion response branch when `prompt_next_date` is true."
    - "Completing an event with `prompt_next_date` false never opens the follow-up modal and keeps the existing success+refresh behavior."
  artifacts:
    - path: "frontend/src/app/shell/user-switcher.tsx"
      provides: "Actor-change cache invalidation/removal and refetch trigger"
      contains: "removeQueries"
    - path: "frontend/src/features/events/complete-event-row-action.tsx"
      provides: "Completion success branch wired to follow-up modal state"
      contains: "prompt_next_date"
    - path: "frontend/src/__tests__/dashboard-events-flow.test.tsx"
      provides: "Regression proof for prompt-next-date true/false modal behavior"
      contains: "Schedule the next date"
    - path: "frontend/src/__tests__/user-switcher-cache-refresh.test.tsx"
      provides: "Regression proof that actor switch forces actor-sensitive query refresh"
      contains: "queryKeys"
  key_links:
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/lib/query-keys.ts"
      via: "actor change clears actor-sensitive namespaces before refetch"
      pattern: "dashboard|events|items"
    - from: "frontend/src/features/events/complete-event-row-action.tsx"
      to: "frontend/src/features/events/follow-up-modal.tsx"
      via: "completion payload toggles modal visibility only on `prompt_next_date: true`"
      pattern: "prompt_next_date"
    - from: "frontend/src/features/events/complete-event-row-action.tsx"
      to: "src/api/routes/events.routes.js"
      via: "PATCH completion response drives follow-up branch selection"
      pattern: "/events/.*/complete"
---

<objective>
Close the code-level milestone blockers by fixing actor-switch cache invalidation and restoring prompt-next-date completion UX wiring.

Purpose: Ensure EVNT-03 is fully wired in frontend behavior and eliminate cross-user stale-data leakage after actor changes.
Output: Updated actor-switch/query cache behavior, completion follow-up modal wiring, and regression tests for both gaps.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-milestone-gap-closure/07-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-6/06-05-SUMMARY.md
@.planning/phases/06-6/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce actor-switch cache purge and deterministic refetch across actor-sensitive queries</name>
  <files>frontend/src/app/shell/user-switcher.tsx, frontend/src/lib/query-keys.ts</files>
  <action>Update the user switcher to use `useQueryClient` and, on actor change, clear/remove actor-sensitive cache namespaces before setting the new active actor id. Keep users-list caching intact, but always purge `dashboard`, `events`, and `items` query branches to prevent stale cross-user flashes. Add or refine query-key helpers if needed so actor-sensitive namespaces are explicit and reused instead of hardcoded string arrays.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Actor changes deterministically invalidate/remove actor-bound dashboard/events/items cache entries, and newly selected actor data refetches without manual page refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Wire completion payload to follow-up modal behavior with explicit Not now and schedule actions</name>
  <files>frontend/src/features/events/complete-event-row-action.tsx, frontend/src/features/events/follow-up-modal.tsx</files>
  <action>Capture the completion API payload in mutation success and open follow-up modal only when `prompt_next_date` is true. Preserve existing success-state and query refresh behavior for both branches. Keep a clear `Not now` close path; route `Schedule next date` to an actionable existing surface (prefer `/items/:itemId/edit` when `itemId` is available, otherwise `/items`) so the button does real work rather than acting as a stub.</action>
  <verify>
    <automated>npm --prefix frontend run test -- dashboard-events-flow --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Completion flow now branches on real API payload semantics: true opens follow-up modal, false keeps modal hidden, and both branches keep deterministic refresh behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for actor-switch refresh and prompt-next-date true/false branches</name>
  <files>frontend/src/__tests__/dashboard-events-flow.test.tsx, frontend/src/__tests__/user-switcher-cache-refresh.test.tsx</files>
  <action>Correct existing prompt-next-date test expectations so `prompt_next_date: true` asserts modal visibility (plus Not now close and schedule action path), while false asserts hidden modal. Add a focused user-switcher regression test that demonstrates actor change triggers cache lifecycle updates and fresh actor-bound fetches for dashboard/events/items query namespaces.</action>
  <verify>
    <automated>npm --prefix frontend run test -- dashboard-events-flow user-switcher-cache-refresh --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Milestone regressions are locked by executable tests that fail if actor switching leaves stale cache or if prompt-next-date modal wiring regresses.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run test -- dashboard-events-flow user-switcher-cache-refresh --runInBand` plus `npm --prefix frontend run typecheck`; verify actor switch immediately refreshes dashboard/events/items context and completion modal behavior follows `prompt_next_date` true/false payload branches.</verification>

<success_criteria>
Frontend no longer exhibits stale cross-user query state after actor switch, and EVNT-03 prompt-next-date follow-up behavior is restored and regression-protected end-to-end.</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-gap-closure/07-01-SUMMARY.md`
</output>
