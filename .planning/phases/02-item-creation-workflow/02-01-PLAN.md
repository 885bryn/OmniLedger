---
phase: 02-item-creation-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/items/default-attributes.js
  - src/domain/items/item-create-errors.js
  - src/domain/items/create-item.js
  - test/domain/items/create-item.test.js
autonomous: true
requirements:
  - ITEM-04
must_haves:
  truths:
    - "Type-aware default attributes are applied before persistence, and client values override defaults when keys overlap."
    - "FinancialCommitment creation fails with actionable validation when parent_item_id is missing or does not resolve to an existing item."
    - "Create flow returns canonical persisted item fields only, including id and timestamps, with no derived relationship objects."
  artifacts:
    - path: "src/domain/items/default-attributes.js"
      provides: "Baseline structural defaults by item type"
      exports:
        - "defaultAttributesByType"
    - path: "src/domain/items/create-item.js"
      provides: "Transaction-bound item create service with defaults merge, parent validation, and canonical serializer"
      exports:
        - "createItem"
    - path: "src/domain/items/item-create-errors.js"
      provides: "Stable domain error categories and field-level issue metadata for API mapping"
      exports:
        - "ItemCreateValidationError"
  key_links:
    - from: "src/domain/items/create-item.js"
      to: "src/domain/items/default-attributes.js"
      via: "defaults merge before Item.create"
      pattern: 'defaultAttributesByType'
    - from: "src/domain/items/create-item.js"
      to: "src/db/index.js"
      via: "sequelize transaction and Item lookup/create"
      pattern: 'sequelize\.transaction'
---

<objective>
Build the domain-level item creation capability that enforces Phase 2 semantics before HTTP wiring is added.

Purpose: Keep create invariants transport-agnostic so `POST /items` can stay thin while still guaranteeing defaults, parent integrity checks, and deterministic persisted output.
Output: Item default map, domain create service with transaction-bound validation, explicit domain error taxonomy, and executable domain tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-item-creation-workflow/02-CONTEXT.md
@.planning/phases/02-item-creation-workflow/02-RESEARCH.md
@.planning/phases/01-domain-model-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define type baseline defaults and domain error taxonomy</name>
  <files>src/domain/items/default-attributes.js, src/domain/items/item-create-errors.js</files>
  <action>Create a compact default map per item type with machine-relevant values only (no UI placeholder text), and define reusable item-create domain errors that support field-level issue arrays and category labels. Keep categories distinct for invalid `item_type`, missing minimum attribute keys, and parent-link failures so API mapping can preserve actionable semantics from locked decisions.</action>
  <verify>
    <automated>npm test -- test/domain/items/create-item.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Defaults and domain error primitives exist with stable exports and explicit category separation required for the Phase 2 error contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transaction-bound create-item domain service</name>
  <files>src/domain/items/create-item.js</files>
  <action>Implement `createItem(input)` that merges attributes using `defaults then client overrides`, validates/normalizes create payloads, and executes parent existence checks plus `Item.create(...)` inside one managed Sequelize transaction. Enforce locked parent behavior (`FinancialCommitment` requires valid parent; `Subscription` parent optional) and return canonical persisted fields only (`id`, `user_id`, `item_type`, `attributes`, `parent_item_id`, `created_at`, `updated_at`) with no expanded relations or defaults-applied metadata.</action>
  <verify>
    <automated>npm test -- test/domain/items/create-item.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Domain create service consistently applies defaults, preserves user overrides, enforces parent rules, and emits canonical persisted payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Add domain tests for defaults merge and parent validation behavior</name>
  <files>test/domain/items/create-item.test.js</files>
  <action>Create focused tests covering ITEM-04 domain semantics: successful creation per type with default key fill, client override precedence for overlapping keys, allowance of extra attribute keys, explicit failures for invalid item type and missing minimum keys, and explicit field-level failures when `FinancialCommitment` parent is missing/nonexistent. Verify serializer output includes canonical persisted fields and excludes expanded parent objects.</action>
  <verify>
    <automated>npm test -- test/domain/items/create-item.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Tests fail when defaults/validation semantics regress and pass only when domain create behavior matches locked Phase 2 decisions.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/items/create-item.test.js --runInBand`; the suite confirms defaults merge behavior, parent-link validation behavior, and canonical persisted response shaping in the domain layer.</verification>

<success_criteria>
Item create logic is ready for API wiring with deterministic default population, explicit validation categories, and canonical persistence output semantics for all item types.</success_criteria>

<output>
After completion, create `.planning/phases/02-item-creation-workflow/02-01-SUMMARY.md`
</output>
