---
phase: 02-item-creation-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - src/api/app.js
  - src/api/routes/items.routes.js
  - src/api/errors/http-error-mapper.js
  - test/api/items-create.test.js
autonomous: true
requirements:
  - ITEM-04
must_haves:
  truths:
    - "User can call POST /items and receive 201 with persisted item data including id and timestamps."
    - "Response attributes include baseline default keys for the submitted item type while preserving client-provided values."
    - "Invalid create requests return one actionable field-level error payload with distinct categories for invalid type, missing keys, and parent-link problems."
  artifacts:
    - path: "src/api/routes/items.routes.js"
      provides: "POST /items endpoint that delegates to domain create service"
      contains: "router.post('/items'"
    - path: "src/api/errors/http-error-mapper.js"
      provides: "Translation of domain/Sequelize failures into stable API error envelope"
      exports:
        - "mapItemCreateError"
    - path: "test/api/items-create.test.js"
      provides: "Endpoint-level behavior coverage for success/defaults and actionable validation responses"
      contains: "POST /items"
  key_links:
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/create-item.js"
      via: "route handler delegates item creation"
      pattern: 'createItem\('
    - from: "src/api/app.js"
      to: "src/api/errors/http-error-mapper.js"
      via: "central error middleware returns mapped envelope"
      pattern: 'mapItemCreateError'
---

<objective>
Expose the Phase 2 create capability through a production-facing HTTP contract and verify it end-to-end.

Purpose: Deliver ITEM-04 as a usable API endpoint with predictable success payloads and client-friendly correction loops for invalid requests.
Output: Express app wiring for `/items`, centralized item-create error translation, dependency updates, and endpoint integration tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-item-creation-workflow/02-CONTEXT.md
@.planning/phases/02-item-creation-workflow/02-RESEARCH.md
@.planning/phases/02-item-creation-workflow/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HTTP runtime dependencies and API bootstrap</name>
  <files>package.json, src/api/app.js</files>
  <action>Add Express runtime dependency and create a minimal API app bootstrap with `express.json()` plus health-safe middleware ordering. Mount item routes under root API scope and keep app construction exportable for tests (no hard-coded listener in app module).</action>
  <verify>
    <automated>npm install && npm test -- test/api/items-create.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Project can boot an Express app instance that parses JSON and mounts item-create routing without coupling tests to a long-running process.</done>
</task>

<task type="auto">
  <name>Task 2: Implement POST /items route and centralized error mapping</name>
  <files>src/api/routes/items.routes.js, src/api/errors/http-error-mapper.js</files>
  <action>Implement `POST /items` as a thin controller that delegates to `createItem`, returns 201 with canonical persisted payload, and never leaks ORM internals. Aggregate all validation issues in one response and map to stable envelope fields with distinct categories/messages for invalid item type, missing required attribute keys, and parent-link errors, using plain actionable language.</action>
  <verify>
    <automated>npm test -- test/api/items-create.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Endpoint success and failure behaviors follow locked response and error semantics, including field-level issue arrays and non-fail-fast reporting.</done>
</task>

<task type="auto">
  <name>Task 3: Add API integration tests for create success, defaults, and validation feedback</name>
  <files>test/api/items-create.test.js</files>
  <action>Create endpoint-level tests that exercise realistic requests against the Express app: success response includes id/timestamps and canonical fields only; default keys populate by item type while submitted values remain unchanged; invalid item type and missing minimum keys return distinct categories; and nonexistent parent for `FinancialCommitment` returns explicit `parent_item_id` issue. Assert multiple validation issues are returned together when applicable.</action>
  <verify>
    <automated>npm test -- test/api/items-create.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Integration suite proves ITEM-04 behavior from HTTP entrypoint through domain create path and response/error serialization.</done>
</task>

</tasks>

<verification>
Run `npm install`, then `npm test -- test/domain/items/create-item.test.js --runInBand` and `npm test -- test/api/items-create.test.js --runInBand`; both suites pass and demonstrate defaults-aware creation plus actionable validation feedback through `POST /items`.</verification>

<success_criteria>
Users can create items through `POST /items` with type-aware defaults and receive deterministic canonical responses plus actionable, multi-issue validation feedback when requests are invalid.</success_criteria>

<output>
After completion, create `.planning/phases/02-item-creation-workflow/02-02-SUMMARY.md`
</output>
