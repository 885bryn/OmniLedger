---
phase: 06-6
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.ts
  - frontend/src/main.tsx
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend workspace boots as a responsive React + TypeScript + Vite application with shadcn-compatible styling primitives."
    - "Repository docs and scripts provide one deterministic local frontend startup/build path for later UI plans."
    - "Frontend entrypoint compiles cleanly and leaves stable extension points for shell, providers, routing, and i18n wiring in the next plan."
  artifacts:
    - path: "frontend/package.json"
      provides: "Frontend dependency and script baseline for build/test/dev flows"
      contains: "scripts"
    - path: "frontend/vite.config.ts"
      provides: "Vite runtime/build configuration for frontend workspace"
      contains: "defineConfig"
    - path: "frontend/src/main.tsx"
      provides: "Frontend mount entrypoint ready for provider/shell integration"
      contains: "createRoot"
    - path: "README.md"
      provides: "Documented frontend setup and execution commands"
      contains: "frontend"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/package.json"
      via: "entrypoint uses dependencies and scripts provisioned by frontend workspace scaffold"
      pattern: "react"
    - from: "frontend/tailwind.config.ts"
      to: "frontend/index.html"
      via: "style baseline hooks into Vite-served app root"
      pattern: "content"
    - from: "README.md"
      to: "frontend/package.json"
      via: "documented commands map directly to frontend npm scripts"
      pattern: "npm --prefix frontend"
---

<objective>
Create the frontend workspace baseline that all Phase 6 UI plans build on.

Purpose: Establish React + TypeScript + Vite + Tailwind/shadcn-compatible foundations, build scripts, and boot entrypoint before layering shell, routing, i18n, and actor-switcher concerns.
Output: Buildable frontend workspace with deterministic startup/build behavior and documented frontend commands.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-6/06-CONTEXT.md
@.planning/phases/06-6/06-RESEARCH.md
@.planning/phases/06-6/06-02-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap frontend workspace and core runtime configuration</name>
  <files>frontend/package.json, frontend/index.html, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tailwind.config.ts, frontend/src/main.tsx, README.md</files>
  <action>Initialize the `frontend/` React + TypeScript + Vite workspace, install shadcn-compatible Tailwind baseline, and ensure build/test scripts are wired in package metadata. Keep setup optimized for responsive web delivery and document frontend startup/build commands in the repository README.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend workspace exists, installs cleanly, and produces a successful production build.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run build` and confirm the frontend workspace compiles with documented startup/build commands and a stable mount entrypoint for subsequent shell/provider work.</verification>

<success_criteria>
Phase 6 has a stable frontend workspace baseline that compiles consistently and is ready for shell, routing, i18n, and active-user plumbing in the next plan.</success_criteria>

<output>
After completion, create `.planning/phases/06-6/06-03-SUMMARY.md`
</output>
