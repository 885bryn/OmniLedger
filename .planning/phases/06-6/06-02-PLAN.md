---
phase: 06-6
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/api/routes/items.routes.js
  - src/api/routes/events.routes.js
  - src/api/routes/users.routes.js
  - src/api/app.js
  - src/api/errors/http-error-mapper.js
  - test/api/items-list-and-mutate.test.js
  - test/api/events-list.test.js
  - test/api/users-list.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend workflows can call API endpoints for items list/edit/soft-delete/activity history using consistent issue-envelope responses."
    - "Dashboard and Events pages can fetch grouped nearest-due event data with deterministic ordering."
    - "Local/dev user switcher can fetch actor options from a server-provided users endpoint and continue using x-user-id semantics."
  artifacts:
    - path: "src/api/routes/items.routes.js"
      provides: "GET/PATCH/DELETE/activity routes for item workflow contracts"
      contains: "router.get('/items'"
    - path: "src/api/routes/events.routes.js"
      provides: "GET /events grouped due-section retrieval"
      contains: "router.get('/events'"
    - path: "src/api/routes/users.routes.js"
      provides: "GET /users actor source contract"
      contains: "router.get('/users'"
    - path: "test/api/items-list-and-mutate.test.js"
      provides: "Endpoint-level contract regression coverage for item workflows"
      contains: "describe"
  key_links:
    - from: "src/api/routes/items.routes.js"
      to: "src/domain/items/list-items.js"
      via: "GET /items maps search/filter/sort query params into domain list service"
      pattern: "listItems"
    - from: "src/api/routes/events.routes.js"
      to: "src/domain/events/list-events.js"
      via: "GET /events returns grouped nearest-due data used by dashboard and events pages"
      pattern: "listEvents"
    - from: "src/api/app.js"
      to: "src/api/errors/http-error-mapper.js"
      via: "new domain query/mutation errors map to deterministic issue-envelope responses"
      pattern: "map"
---

<objective>
Expose the expanded backend HTTP contracts needed by locked Phase 6 UI journeys.

Purpose: Wire route-level contracts for items/events/users on top of Plan 1 domain services while preserving centralized issue-envelope behavior.
Output: Expanded API routes, app mapper wiring updates, and endpoint integration tests that lock frontend-facing contracts.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-6/06-CONTEXT.md
@.planning/phases/06-6/06-RESEARCH.md
@.planning/phases/06-6/06-01-SUMMARY.md
@src/api/app.js
@src/api/routes/items.routes.js
@src/api/routes/events.routes.js
@src/api/errors/http-error-mapper.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand items/events/users routes and mapper wiring for Phase 6 contracts</name>
  <files>src/api/routes/items.routes.js, src/api/routes/events.routes.js, src/api/routes/users.routes.js, src/api/app.js, src/api/errors/http-error-mapper.js</files>
  <action>Expose `GET /items` (search/filter/sort), `PATCH /items/:id`, `DELETE /items/:id` (soft delete), `GET /items/:id/activity`, `GET /events` (grouped nearest-due sections), and `GET /users` for local/dev actor switching. Keep `PATCH /events/:id/complete` one-click behavior unchanged and continue using `x-user-id` transport semantics. Extend centralized error mapping so all new domain errors return deterministic issue-envelope payloads with stable categories and status codes.</action>
  <verify>
    <automated>npm test -- test/api/items-list-and-mutate.test.js test/api/events-list.test.js test/api/users-list.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>API contract surface now covers Phase 6 dashboard/items/events workflows with consistent response and error-envelope behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add endpoint integration suites proving frontend contract completeness</name>
  <files>test/api/items-list-and-mutate.test.js, test/api/events-list.test.js, test/api/users-list.test.js</files>
  <action>Add integration suites using existing createApp + sqlite-backed test patterns. Assert deterministic ordering, grouped due sections, field/page-level issue envelopes for invalid queries and updates, soft-delete visibility semantics, activity payload shape, and user-switcher payload contract so frontend pages can rely on stable transport behavior.</action>
  <verify>
    <automated>npm test -- test/api/items-list-and-mutate.test.js test/api/events-list.test.js test/api/users-list.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Contract regressions fail fast whenever route behavior required by Phase 6 pages drifts.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/items-list-and-mutate.test.js test/api/events-list.test.js test/api/users-list.test.js --runInBand` and confirm contract coverage for list/edit/delete/activity/users and grouped due events endpoints.</verification>

<success_criteria>
Phase 6 frontend can consume complete backend contracts for dashboard/items/events workflows without mocks or placeholder route gaps.</success_criteria>

<output>
After completion, create `.planning/phases/06-6/06-02-SUMMARY.md`
</output>
