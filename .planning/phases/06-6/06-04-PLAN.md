---
phase: 06-6
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - frontend/src/app/router.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/app/shell/app-shell.tsx
  - frontend/src/app/shell/language-switcher.tsx
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/lib/api-client.ts
  - frontend/src/lib/query-keys.ts
  - frontend/src/lib/i18n.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend uses a persistent left-sidebar shell and route topology for Dashboard, Items, and Events journeys."
    - "Global header language switch immediately changes UI language between English and Chinese with fallback to English for missing keys."
    - "Global header user switcher fetches `/users`, persists active actor selection, and updates request context for all API calls."
    - "All network requests flow through a shared API adapter that preserves issue-envelope parsing and injects `x-user-id` from active actor state."
  artifacts:
    - path: "frontend/src/app/shell/app-shell.tsx"
      provides: "Persistent left sidebar + global header shell for desktop/mobile"
      contains: "SidebarProvider"
    - path: "frontend/src/lib/i18n.ts"
      provides: "Runtime language switching with fallback behavior"
      contains: "fallbackLng"
    - path: "frontend/src/lib/api-client.ts"
      provides: "Centralized API transport wrapper and issue-envelope mapping"
      contains: "x-user-id"
    - path: "frontend/src/app/shell/user-switcher.tsx"
      provides: "Header actor picker wired to backend users list and active actor updates"
      contains: "/users"
    - path: "frontend/src/app/router.tsx"
      provides: "Route map for Dashboard, Items, Events, detail/edit/wizard pages"
      contains: "createBrowserRouter"
  key_links:
    - from: "frontend/src/app/providers.tsx"
      to: "frontend/src/app/router.tsx"
      via: "application boot wraps router with shared Query and i18n providers"
      pattern: "RouterProvider"
    - from: "frontend/src/app/shell/language-switcher.tsx"
      to: "frontend/src/lib/i18n.ts"
      via: "header switcher triggers immediate changeLanguage updates"
      pattern: "changeLanguage"
    - from: "frontend/src/lib/api-client.ts"
      to: "src/api/routes/items.routes.js"
      via: "client adapter aligns endpoint and envelope contracts with backend routes"
      pattern: "/items"
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/lib/api-client.ts"
      via: "actor selection hydrates/persists active user id and updates api-client x-user-id header source"
      pattern: "x-user-id"
---

<objective>
Implement the frontend shell/runtime wiring required before page journey implementation.

Purpose: Add routing, providers, app shell, API adapter, bilingual runtime switching, and the explicit `/users`-backed persisted user switcher so all later pages inherit stable cross-app behavior.
Output: Reusable shell/provider/i18n/api primitives, route skeletons, and active-user context plumbing.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-6/06-CONTEXT.md
@.planning/phases/06-6/06-RESEARCH.md
@.planning/phases/06-6/06-03-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement app shell, routing, and shared providers for dashboard/items/events navigation</name>
  <files>frontend/src/app/router.tsx, frontend/src/app/providers.tsx, frontend/src/app/shell/app-shell.tsx, frontend/src/lib/query-keys.ts</files>
  <action>Build a persistent left-sidebar shell and route map for dashboard/items/events/detail/edit/wizard paths. Wire shared TanStack Query providers and query key conventions so page-level features can consume one deterministic server-state foundation.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend shell and provider stack support responsive cross-page navigation and stable query orchestration for subsequent journey plans.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bilingual runtime switching, shared API adapter, and persisted `/users`-backed user switcher</name>
  <files>frontend/src/app/shell/language-switcher.tsx, frontend/src/app/shell/user-switcher.tsx, frontend/src/lib/api-client.ts, frontend/src/lib/i18n.ts</files>
  <action>Implement immediate runtime language switching (`English | 中文`) with fallback to English and no translation of user-entered values. Add shared API adapter that normalizes issue-envelope errors and injects `x-user-id` from one active-actor source. Implement the header user switcher for local/dev flows: fetch `/users`, maintain active actor selection, persist in browser storage, hydrate on app load with safe fallback when saved actor is missing, and propagate updates immediately to API client request headers.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Cross-app runtime behavior is bilingual, actor-aware, and contract-aligned before journey page implementation begins.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run build` and confirm the app boots with sidebar navigation, route shells, language switching, header user switcher backed by `/users`, persisted active actor selection, and API adapter wiring.</verification>

<success_criteria>
Phase 6 has a stable frontend shell/runtime foundation that supports responsive navigation, bilingual behavior, explicit active-user switching, and shared server-state integration patterns.</success_criteria>

<output>
After completion, create `.planning/phases/06-6/06-04-SUMMARY.md`
</output>
