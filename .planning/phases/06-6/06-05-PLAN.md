---
phase: 06-6
plan: 05
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - frontend/src/pages/dashboard/dashboard-page.tsx
  - frontend/src/pages/events/events-page.tsx
  - frontend/src/features/events/complete-event-row-action.tsx
  - frontend/src/features/events/follow-up-modal.tsx
  - frontend/src/lib/date-ordering.ts
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users land on a responsive dashboard with high-level summary cards and due-first grouped event visibility."
    - "Users can complete events inline and see refreshed row/group state after confirm-then-refresh behavior."
    - "Follow-up modal appears only when completion payload returns prompt_next_date true and always offers a clear Not now path."
  artifacts:
    - path: "frontend/src/pages/dashboard/dashboard-page.tsx"
      provides: "Dashboard-first landing with summary cards and grouped due events"
      contains: "useQuery"
    - path: "frontend/src/features/events/complete-event-row-action.tsx"
      provides: "Inline completion mutation with refresh semantics"
      contains: "useMutation"
    - path: "frontend/src/features/events/follow-up-modal.tsx"
      provides: "Prompt-next-date follow-up interaction"
      contains: "prompt_next_date"
    - path: "frontend/src/lib/date-ordering.ts"
      provides: "Shared deterministic nearest-due comparator"
      contains: "compare"
  key_links:
    - from: "frontend/src/pages/events/events-page.tsx"
      to: "frontend/src/lib/date-ordering.ts"
      via: "events grouping uses one comparator across pages for deterministic nearest-due ordering"
      pattern: "sort"
    - from: "frontend/src/features/events/complete-event-row-action.tsx"
      to: "src/api/routes/events.routes.js"
      via: "row action calls completion endpoint and branches on prompt_next_date payload"
      pattern: "prompt_next_date"
    - from: "frontend/src/features/events/follow-up-modal.tsx"
      to: "frontend/src/locales/zh/common.json"
      via: "modal interface copy supports bilingual labels without translating user data"
      pattern: "not_now"
---

<objective>
Implement the Dashboard and Events user journeys with due-first ordering and inline completion flows.

Purpose: Deliver the event-first operational experience locked in context decisions, including grouped nearest-due display, inline completion refresh, and follow-up modal branching.
Output: Dashboard and Events pages, completion + follow-up features, ordering utility, locale strings, and journey tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-6/06-CONTEXT.md
@.planning/phases/06-6/06-RESEARCH.md
@.planning/phases/06-6/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard and Events grouped views with deterministic due ordering</name>
  <files>frontend/src/pages/dashboard/dashboard-page.tsx, frontend/src/pages/events/events-page.tsx, frontend/src/lib/date-ordering.ts</files>
  <action>Implement dashboard-first landing and events page views using grouped nearest-due sections from backend data. Reuse one deterministic comparator utility for due ordering, add skeleton loading for major views, and include action-oriented empty states that direct users to the next meaningful workflow.</action>
  <verify>
    <automated>npm --prefix frontend run test -- dashboard-events-flow --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Dashboard and Events views render grouped due-first data consistently with responsive layout and state feedback behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Add inline event completion flow with payload-driven follow-up modal behavior</name>
  <files>frontend/src/features/events/complete-event-row-action.tsx, frontend/src/features/events/follow-up-modal.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Implement one-click inline completion action that refreshes affected queries on success and shows inline completion feedback. Show follow-up modal only when completion response includes `prompt_next_date: true`; default modal focus to scheduling while preserving a clear `Not now` branch. Add bilingual copy for new states and test end-to-end completion, refresh, and modal branching behavior.</action>
  <verify>
    <automated>npm --prefix frontend run test -- dashboard-events-flow --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Event completion journey is deterministic and aligned with locked one-click, refresh, and follow-up modal requirements.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run test -- dashboard-events-flow --runInBand` and manually verify dashboard-first landing, grouped nearest-due ordering, inline completion refresh, and follow-up modal branching on `prompt_next_date`.</verification>

<success_criteria>
Users can manage due events directly from dashboard/events views with correct ordering, feedback, and follow-up behavior in both supported languages.</success_criteria>

<output>
After completion, create `.planning/phases/06-6/06-05-SUMMARY.md`
</output>
