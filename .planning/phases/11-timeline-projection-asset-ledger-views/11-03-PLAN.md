---
phase: 11-timeline-projection-asset-ledger-views
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - frontend/src/lib/date-ordering.ts
  - frontend/src/features/events/edit-event-row-action.tsx
  - frontend/src/features/events/complete-event-row-action.tsx
  - frontend/src/pages/events/events-page.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
autonomous: true
requirements:
  - TIME-01
  - TIME-02
  - FIN-05
must_haves:
  truths:
    - "Timeline rows visibly distinguish projected versus persisted occurrences with explicit badge and legend cues."
    - "Same-date mixed rows render in deterministic order with persisted rows above projected rows."
    - "Editing a projected row uses explicit exception-save confirmation and updates the same row in place as persisted edited occurrence state."
  artifacts:
    - path: "frontend/src/pages/events/events-page.tsx"
      provides: "Date-header grouped timeline with projected/persisted markers, inline legend, and deterministic row ordering"
      contains: "Projected"
    - path: "frontend/src/features/events/edit-event-row-action.tsx"
      provides: "Projected-edit action flow with save-exception confirmation and field-level change summary"
      contains: "Save exception"
    - path: "frontend/src/__tests__/dashboard-events-flow.test.tsx"
      provides: "Regression coverage for state markers, deterministic ordering, and projected-edit UI behavior"
      contains: "Edited occurrence"
  key_links:
    - from: "frontend/src/features/events/edit-event-row-action.tsx"
      to: "/events/:id"
      via: "PATCH mutation invokes projected-aware backend edit and refetches timeline data"
      pattern: "apiRequest.*PATCH"
    - from: "frontend/src/pages/events/events-page.tsx"
      to: "frontend/src/lib/date-ordering.ts"
      via: "Shared comparator enforces persisted-before-projected same-date ordering"
      pattern: "compareByNearestDue"
---

<objective>
Implement timeline UX cues and projected-edit flow on top of the new occurrence contract.

Purpose: Fulfill TIME-01/TIME-02 visual clarity and FIN-05 user workflow expectations with explicit projected/persisted state communication and in-place projected-edit persistence feedback.
Output: Events page/UI action updates, localized copy, and frontend regressions covering deterministic ordering and exception-save interactions.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-CONTEXT.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-RESEARCH.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-01-SUMMARY.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-02-SUMMARY.md
@frontend/src/pages/events/events-page.tsx
@frontend/src/features/events/complete-event-row-action.tsx
@frontend/src/lib/date-ordering.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit projected/persisted timeline cues with deterministic same-date ordering</name>
  <files>frontend/src/lib/date-ordering.ts, frontend/src/pages/events/events-page.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Update timeline rendering to keep date-header grouping while using the backend source metadata to render explicit `Projected` badges, persisted state badges, and a short inline legend near section headers. Apply subtle projected-row contrast (not heavy) and enforce same-date tie behavior through shared comparator updates so persisted rows appear before projected rows. Keep completed rows interleaved by chronology and avoid any flat-list redesign.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Events timeline clearly shows projected versus persisted state with deterministic persisted-first ties and date-header grouping intact.</done>
</task>

<task type="auto">
  <name>Task 2: Deliver projected edit confirmation flow with in-place exception persistence feedback</name>
  <files>frontend/src/features/events/edit-event-row-action.tsx, frontend/src/features/events/complete-event-row-action.tsx, frontend/src/pages/events/events-page.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Add row-level edit action for editable occurrences that opens a confirmation flow explicitly stating a persisted exception will be created for projected rows, with primary CTA text `Save exception`. When date and amount both change, include a concise field-level before/after summary (`old -> new`) in confirmation content. On mutation success, update row state in place through query invalidation/refetch and show `Edited occurrence` indicator when exception metadata is present; do not show success toast if row state already reflects success. On validation/server failure, show inline error text plus existing toast channel behavior.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx && npm --prefix frontend build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Projected rows can be edited through explicit exception-save UX and then render as persisted edited occurrences without requiring page reload or success toast.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx && npm --prefix frontend build` and verify timeline badges/legend, deterministic ordering, and projected-edit exception UX behavior.
</verification>

<success_criteria>
Timeline UI now communicates occurrence state unambiguously and supports projected-edit exception persistence as an in-place, deterministic workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/11-timeline-projection-asset-ledger-views/11-03-SUMMARY.md`
</output>
