---
phase: 11-timeline-projection-asset-ledger-views
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/db/migrations/20260227120000-add-event-exception-flags.js
  - src/db/models/event.model.js
  - src/domain/events/event-update-errors.js
  - src/domain/events/update-event.js
  - src/api/routes/events.routes.js
  - test/api/events-update.test.js
autonomous: true
requirements:
  - FIN-05
  - TIME-02
must_haves:
  truths:
    - "Editing a projected occurrence creates one persisted occurrence for that exact item/date and applies edits to that stored row."
    - "Persisted rows created via projected edits carry durable exception metadata that survives reloads."
    - "Projected-edit mutation respects existing ownership rules and retains idempotent materialization semantics."
  artifacts:
    - path: "src/db/migrations/20260227120000-add-event-exception-flags.js"
      provides: "Event exception metadata columns for durable projected-edit markers"
      contains: "is_exception"
    - path: "src/domain/events/update-event.js"
      provides: "Projected-aware event edit mutation that materializes then updates occurrence"
      exports: ["updateEvent"]
    - path: "test/api/events-update.test.js"
      provides: "Regression coverage for projected-edit exception materialization, ownership guardrails, and dedupe behavior"
      contains: "projected-"
  key_links:
    - from: "src/api/routes/events.routes.js"
      to: "src/domain/events/update-event.js"
      via: "PATCH /events/:id uses scope-derived ownership and projected materialization path"
      pattern: "updateEvent"
    - from: "src/domain/events/update-event.js"
      to: "src/domain/items/item-event-sync.js"
      via: "Projected IDs materialize persisted rows before mutation"
      pattern: "materializeItemEventForDate"
---

<objective>
Add projected-occurrence edit mutation with durable persisted exception metadata.

Purpose: Fulfill FIN-05 by turning projected row edits into persisted exceptions for the targeted date while preserving owner scope and deterministic occurrence identity.
Output: Event schema/domain/route updates plus API regressions for projected edit materialization and exception persistence.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-CONTEXT.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-RESEARCH.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-01-SUMMARY.md
@src/db/models/event.model.js
@src/domain/events/complete-event.js
@src/api/routes/events.routes.js
@test/api/events-complete.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable event exception metadata for projected-edit persistence</name>
  <files>src/db/migrations/20260227120000-add-event-exception-flags.js, src/db/models/event.model.js</files>
  <action>Create migration/model support for persisted exception markers (minimum: `is_exception` boolean default false; optional projection-source date key if needed for diagnostics). Ensure compatibility with sqlite test runtime and postgres enum/column behavior. Keep existing Event completion semantics unchanged and avoid introducing series-wide edit metadata in this phase.</action>
  <verify>
    <automated>npm test -- test/api/events-complete.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Persisted event rows can durably encode projected-edit exception state without regressing existing completion flows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PATCH /events/:id projected-aware edit mutation and regressions</name>
  <files>src/domain/events/event-update-errors.js, src/domain/events/update-event.js, src/api/routes/events.routes.js, test/api/events-update.test.js</files>
  <action>Add an event update domain path that accepts targeted editable fields (date and amount), resolves projected IDs using existing materialization primitives, writes updates in one transaction, and sets exception metadata when projection materialization occurs. Enforce owner scope with same not_found-style policy behavior used by completion routes. Return canonical event payload including source/exception fields needed by UI state badges. Add focused API regressions for: projected edit materializes exactly one row, repeated saves dedupe correctly, foreign-owner attempts fail with not_found envelope, and invalid date/amount payloads return validation issues.</action>
  <verify>
    <automated>npm test -- test/api/events-update.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>PATCH /events/:id can safely edit projected future occurrences by materializing and persisting a date-specific exception row under owner policy constraints.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/events-update.test.js --runInBand` and verify projected edit requests materialize persisted exceptions, enforce ownership, and return canonical edited payload fields.
</verification>

<success_criteria>
Projected occurrence edits are now first-class persisted mutations with durable exception markers and policy-safe API behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-timeline-projection-asset-ledger-views/11-02-SUMMARY.md`
</output>
