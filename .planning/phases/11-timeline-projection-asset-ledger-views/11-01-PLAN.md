---
phase: 11-timeline-projection-asset-ledger-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/items/item-event-sync.js
  - src/domain/events/list-events.js
  - test/api/events-list.test.js
autonomous: true
requirements:
  - TIME-01
  - TIME-02
must_haves:
  truths:
    - "User can load one unified timeline payload that includes persisted and projected occurrences through a 3-year forward window."
    - "Timeline ordering is deterministic and soonest-first, and same-date persisted rows always sort before projected rows."
    - "Timeline rows expose explicit occurrence source metadata so UI state cues do not depend on ID-pattern inference."
  artifacts:
    - path: "src/domain/items/item-event-sync.js"
      provides: "Date-window projection helper for recurring FinancialItem rows bounded to today..today+3y"
      exports: ["projectItemEvents"]
    - path: "src/domain/events/list-events.js"
      provides: "Unified persisted+projected timeline read model with explicit source metadata and deterministic comparator"
      exports: ["listEvents"]
    - path: "test/api/events-list.test.js"
      provides: "Regression coverage for 3-year horizon projection and persisted-before-projected tie-break ordering"
      contains: "projected"
  key_links:
    - from: "src/domain/events/list-events.js"
      to: "src/domain/items/item-event-sync.js"
      via: "Projection generation passes date-window bounds instead of count limit"
      pattern: "projectItemEvents"
    - from: "src/domain/events/list-events.js"
      to: "test/api/events-list.test.js"
      via: "API response contract includes source metadata and deterministic ordering assertions"
      pattern: "source_state"
---

<objective>
Upgrade the timeline read model to a deterministic 3-year unified occurrence contract.

Purpose: Satisfy TIME-01 and TIME-02 by replacing count-limited projection with a bounded date horizon and explicit persisted/projected source semantics.
Output: Backend projection/list domain updates and API regression coverage proving 3-year horizon behavior plus deterministic persisted-before-projected tie ordering.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-CONTEXT.md
@.planning/phases/11-timeline-projection-asset-ledger-views/11-RESEARCH.md
@src/domain/items/item-event-sync.js
@src/domain/events/list-events.js
@test/api/events-list.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace count-based projection with today..today+3y date-window generation</name>
  <files>src/domain/items/item-event-sync.js, test/api/events-list.test.js</files>
  <action>Refactor recurring FinancialItem projection so generated occurrences are bounded by an explicit forward horizon (`today` through `today + 3 years`) instead of `limit: 3`. Preserve active-status gating, soft-delete exclusion, and deterministic projected IDs (`projected-{itemId}-{YYYY-MM-DD}`). Do not pre-materialize rows in storage; keep projection-on-read semantics from Phase 10.</action>
  <verify>
    <automated>npm test -- test/api/events-list.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Recurring timeline projection produces all eligible future occurrences within the 3-year window and no longer truncates to the first three rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit source metadata and deterministic persisted-first tie sorting</name>
  <files>src/domain/events/list-events.js, test/api/events-list.test.js</files>
  <action>Extend normalized event DTOs with explicit source/state markers (for example `source_state: persisted|projected`, `is_projected`) so UI surfaces can render badges without ID parsing. Update deterministic comparator logic to honor locked ordering: soonest-first by due date, persisted rows before projected rows on same date, completed rows still interleaved by chronology. Keep grouped-by-date response shape and owner scope filtering unchanged.</action>
  <verify>
    <automated>npm test -- test/api/events-list.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>GET /events returns deterministic grouped rows with explicit source metadata and same-date persisted-before-projected ordering.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/events-list.test.js --runInBand` and confirm 3-year projection window coverage, deterministic order guarantees, and source metadata in response payloads.
</verification>

<success_criteria>
Timeline read output is unified, deterministic, and explicit about projected versus persisted state for all occurrences through a 3-year forward window.
</success_criteria>

<output>
After completion, create `.planning/phases/11-timeline-projection-asset-ledger-views/11-01-SUMMARY.md`
</output>
