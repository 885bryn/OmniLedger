---
phase: 14-export-entry-and-scope-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/app.js
  - src/api/routes/exports.routes.js
  - src/domain/exports/export-scope-query.js
  - test/api/exports-backup-scope.test.js
autonomous: true
requirements:
  - SCOP-01
  - SCOP-02
  - SCOP-03
  - SCOP-04
must_haves:
  truths:
    - "Standard users only receive export data for their own owner scope."
    - "Admin all-data mode receives export data across owners."
    - "Admin owner-lens mode receives only lens-user export data, and client scope hints cannot widen scope."
  artifacts:
    - path: "src/api/routes/exports.routes.js"
      provides: "Authenticated export entry route bound to session-derived scope"
      exports: ["createExportsRouter"]
    - path: "src/domain/exports/export-scope-query.js"
      provides: "Scope-filtered export dataset query for phase-14 contract"
      exports: ["exportScopeQuery"]
    - path: "test/api/exports-backup-scope.test.js"
      provides: "Role x mode x override regression matrix for export scope"
  key_links:
    - from: "src/api/routes/exports.routes.js"
      to: "src/api/auth/require-auth.js"
      via: "router.use(requireAuth)"
      pattern: "router\.use\(requireAuth\)"
    - from: "src/api/routes/exports.routes.js"
      to: "src/domain/exports/export-scope-query.js"
      via: "domain delegation with req.scope"
      pattern: "exportScopeQuery\(\{\s*scope:\s*req\.scope"
    - from: "src/domain/exports/export-scope-query.js"
      to: "src/api/auth/scope-context.js"
      via: "resolveOwnerFilter(scope)"
      pattern: "resolveOwnerFilter\(scope\)"
---

<objective>
Lock the Phase 14 backend trust boundary for export so all dataset reads are derived from session-authenticated scope, never from client owner/lens inputs.

Purpose: Fulfill SCOP-01..04 before workbook shaping work in later phases.
Output: A mounted export route plus scoped domain query and regression matrix proving owner/all/lens enforcement and override resistance.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-export-entry-and-scope-enforcement/14-RESEARCH.md
@src/api/auth/require-auth.js
@src/api/auth/scope-context.js
@src/api/app.js
@test/api/admin-scope-lens.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scope-authoritative export route and query contract</name>
  <files>src/api/routes/exports.routes.js, src/domain/exports/export-scope-query.js, src/api/app.js, test/api/exports-backup-scope.test.js</files>
  <action>Create `createExportsRouter()` with `GET /exports/backup.xlsx` behind `requireAuth`, delegate to `exportScopeQuery({ scope: req.scope })`, and ignore client ownership hints (`user_id`, `owner_id`, `scope_mode`, `lens_user_id`) rather than forwarding them. Implement `exportScopeQuery` using `resolveOwnerFilter(scope)` to build owner-scoped reads for `Item` and `Event` models (null filter for admin all-mode), and return a minimal phase-14 JSON contract containing scoped dataset metadata plus raw row payload needed for SCOP verification. Mount the router in `src/api/app.js` with existing route wiring patterns, and add initial integration coverage in `test/api/exports-backup-scope.test.js` for standard-user and admin-all success paths.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>`GET /exports/backup.xlsx` is authenticated, session-scope derived, and positive-path tests prove standard-user owner scoping plus admin all-data visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Expand regression matrix for admin lens and client override resistance</name>
  <files>test/api/exports-backup-scope.test.js</files>
  <action>Extend the export scope integration suite to cover admin owner-lens filtering and explicit override attacks where client query/body attempts to widen scope. Add assertions that override attempts do not change response scope outcome for standard users and lens-mode admins, mirroring established negative-pattern tests from `admin-scope-lens` coverage. Keep envelope/assertion style consistent with existing API suites and include at least one denial-style or containment assertion per SCOP-04 scenario.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Test matrix proves SCOP-01, SCOP-02, SCOP-03, and SCOP-04 with explicit checks that client-supplied owner/lens hints never expand export scope.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/exports-backup-scope.test.js --runInBand` and confirm all scope scenarios pass using only session-derived scope context.
</verification>

<success_criteria>
- Standard-user export route responses include only that user's owner records.
- Admin all-data mode responses include cross-owner records.
- Admin owner-lens mode responses include only selected lens-user records.
- Injected client scope hints do not widen scoped export results.
</success_criteria>

<output>
After completion, create `.planning/phases/14-export-entry-and-scope-enforcement/14-01-SUMMARY.md`
</output>
