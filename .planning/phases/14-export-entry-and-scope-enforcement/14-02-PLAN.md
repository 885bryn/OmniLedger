---
phase: 14-export-entry-and-scope-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/src/features/export/use-export-backup.ts
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/user-switcher-export-action.test.tsx
autonomous: true
requirements:
  - UXEX-01
must_haves:
  truths:
    - "Users can find an Export Backup action in a management surface that already shows identity/lens context."
    - "Clicking Export Backup triggers the backend export entry route without sending owner override identifiers."
    - "The action shows request-pending and error/success feedback states appropriate for a one-click export trigger."
  artifacts:
    - path: "frontend/src/app/shell/user-switcher.tsx"
      provides: "Global management-surface Export Backup action near identity/lens controls"
    - path: "frontend/src/features/export/use-export-backup.ts"
      provides: "Export trigger mutation using credentials-included API call"
      exports: ["useExportBackup"]
    - path: "frontend/src/__tests__/user-switcher-export-action.test.tsx"
      provides: "Frontend regression coverage for action discoverability and trigger behavior"
  key_links:
    - from: "frontend/src/app/shell/user-switcher.tsx"
      to: "frontend/src/features/export/use-export-backup.ts"
      via: "button click handler"
      pattern: "useExportBackup"
    - from: "frontend/src/features/export/use-export-backup.ts"
      to: "/exports/backup.xlsx"
      via: "apiRequest mutation"
      pattern: "apiRequest\('/exports/backup\.xlsx'"
---

<objective>
Add a discoverable, scope-aware export entry action in the shell so users can trigger backup export from the management surface where identity and admin lens context are already visible.

Purpose: Fulfill UXEX-01 and align the click entry with server-authoritative scope behavior from Plan 14-01.
Output: Export mutation hook, `UserSwitcher` action wiring, localized labels, and focused frontend interaction tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-export-entry-and-scope-enforcement/14-RESEARCH.md
@.planning/phases/14-export-entry-and-scope-enforcement/14-01-SUMMARY.md
@frontend/src/app/shell/user-switcher.tsx
@frontend/src/lib/api-client.ts
@frontend/src/locales/en/common.json
@frontend/src/locales/zh/common.json
@frontend/src/__tests__/user-switcher-cache-refresh.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export trigger hook and shell action wiring</name>
  <files>frontend/src/features/export/use-export-backup.ts, frontend/src/app/shell/user-switcher.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json, frontend/src/__tests__/user-switcher-export-action.test.tsx</files>
  <action>Create `useExportBackup()` using React Query mutation and `apiRequest('/exports/backup.xlsx')` with no owner/lens request params. Wire a new `Export Backup` button into `UserSwitcher` adjacent to existing identity/lens controls so scope context is visible when clicked. Provide localized EN/ZH labels plus minimal pending/success/error shell feedback that does not overlap Phase 18's richer UX states. Add initial interaction coverage proving action visibility and click-trigger behavior for a standard authenticated user.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run src/__tests__/user-switcher-export-action.test.tsx</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>The shell shows an Export Backup action and clicking it calls the export endpoint through the shared API client without client-side scope override fields.</done>
</task>

<task type="auto">
  <name>Task 2: Lock scope-context discoverability behavior for admin all/lens states</name>
  <files>frontend/src/__tests__/user-switcher-export-action.test.tsx</files>
  <action>Expand the export action tests to cover admin all-data and owner-lens render states in `UserSwitcher`, asserting the action remains available in both contexts and continues to trigger the same server-authoritative endpoint contract. Include a regression assertion that request construction never injects owner identifiers even when admin lens is active.</action>
  <verify>
    <automated>npm --prefix frontend exec vitest run src/__tests__/user-switcher-export-action.test.tsx</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend regressions prove the export entry remains discoverable across admin scope modes and does not attempt client-side scope widening.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend exec vitest run src/__tests__/user-switcher-export-action.test.tsx` and confirm user/admin contexts both expose the action and call the same export endpoint.
</verification>

<success_criteria>
- `UserSwitcher` renders a localized `Export Backup` action in the management surface.
- Clicking the action triggers one backend export request through shared API client plumbing.
- UI conveys pending plus terminal feedback (success/error) for the trigger action.
- Frontend tests lock discoverability and no-owner-override request behavior for standard and admin contexts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-export-entry-and-scope-enforcement/14-02-SUMMARY.md`
</output>
