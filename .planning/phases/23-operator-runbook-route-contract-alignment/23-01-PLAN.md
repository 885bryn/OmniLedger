---
phase: 23-operator-runbook-route-contract-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Operator can run backend-direct auth and items checks against :8080 without route-not-found false failures caused by `/api/*` path mismatch."
    - "Operator can clearly distinguish frontend gateway checks (`:8085/api/*`) from backend-direct checks (`:8080/auth/*`, `:8080/items`)."
    - "Troubleshooting and verification sections use command examples that match the live backend route mounts."
  artifacts:
    - path: "README.md"
      provides: "Corrected runbook command contract for backend-direct and gateway-prefixed checks"
      contains: "## Troubleshooting (Symptom -> Checks -> Fixes)"
  key_links:
    - from: "README.md"
      to: "src/api/app.js"
      via: "backend-direct command paths align to mounted routes"
      pattern: "app\.use\(\"/auth\"|app\.use\(\"/\", createItemsRouter"
    - from: "README.md"
      to: "README verification gate table"
      via: "backend :8080 commands avoid `/api/*`; gateway examples retain `/api/*` where appropriate"
      pattern: ":8080/(auth/login|items)|:8085/api"
---

<objective>
Align README verification and troubleshooting command paths to the live backend route contract so healthy deployments do not fail due to documentation path errors.

Purpose: Close the DOCS-01 integration gap by removing backend `:8080/api/*` examples that contradict runtime route mounts.
Output: `README.md` with explicit backend-direct vs frontend-gateway path rules and corrected auth/items command examples.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/22-operator-deployment-documentation/22-02-SUMMARY.md
@README.md
@src/api/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit route contract note for backend-direct vs gateway checks</name>
  <files>README.md</files>
  <action>In the troubleshooting/verification area, add a short contract note that backend-direct checks on `http://&lt;NAS_STATIC_IP&gt;:8080` must use mounted backend routes (`/auth/login`, `/items`) while frontend-gateway checks on `http://&lt;NAS_STATIC_IP&gt;:8085` keep `/api/*` prefixes. Keep this rule adjacent to command examples so operators do not mix route styles during incidents.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('README.md','utf8');if(!/:8080/.test(s)||!/\/auth\/login/.test(s)||!/\/items/.test(s)||!/:8085/.test(s)||!/\/api\//.test(s)){process.exit(1);}"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>README includes an explicit and unambiguous route contract separating backend-direct and gateway-prefixed command usage.</done>
</task>

<task type="auto">
  <name>Task 2: Replace backend `:8080/api/*` command examples with live route mounts</name>
  <files>README.md</files>
  <action>Update all backend-direct troubleshooting and verification commands that currently call `http://&lt;NAS_STATIC_IP&gt;:8080/api/auth/login` or `http://&lt;NAS_STATIC_IP&gt;:8080/api/items` to `http://&lt;NAS_STATIC_IP&gt;:8080/auth/login` and `http://&lt;NAS_STATIC_IP&gt;:8080/items`. Also update surrounding expected outcomes and failure-interpretation text so wording matches corrected paths and no longer implies `/api/*` is valid on backend port.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('README.md','utf8');if(/:8080\/api\//.test(s))process.exit(1);if(!/:8080\/auth\/login/.test(s)||!/:8080\/items/.test(s))process.exit(1);"</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>README has no backend `:8080/api/*` examples and all backend-direct auth/items checks use the live mount contract.</done>
</task>

</tasks>

<verification>
Run `node -e "const fs=require('fs');const s=fs.readFileSync('README.md','utf8');console.log(/:8080\/api\//.test(s)?'FAIL':'PASS');"` and confirm result is `PASS`; then confirm corrected examples for `:8080/auth/login` and `:8080/items` remain present.
</verification>

<success_criteria>
- README command examples for backend-direct troubleshooting and gates match runtime backend mounts (`/auth/*`, `/items`).
- README clearly separates backend-direct endpoint style from frontend-gateway `/api/*` endpoint style.
- Operators no longer hit false route-not-found failures caused only by runbook path mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/23-operator-runbook-route-contract-alignment/23-01-SUMMARY.md`
</output>
