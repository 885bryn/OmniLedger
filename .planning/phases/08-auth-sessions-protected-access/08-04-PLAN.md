---
phase: 08-auth-sessions-protected-access
plan: 04
type: execute
wave: 4
depends_on:
  - 08-05
  - 08-06
files_modified:
  - frontend/src/lib/api-client.ts
  - frontend/src/app/shell/app-shell.tsx
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/pages/items/item-list-page.tsx
  - frontend/src/pages/items/item-create-wizard-page.tsx
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/__tests__/items-workflows.test.tsx
  - frontend/src/__tests__/user-switcher-cache-refresh.test.tsx
  - frontend/src/__tests__/auth-session-shell.test.tsx
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "Frontend no longer depends on actor switching or `x-user-id` headers for API identity."
    - "App shell identity UI reflects authenticated session user and offers explicit logout instead of local actor swap."
    - "Item workflows continue working under session-derived server identity without passing actor headers."
  artifacts:
    - path: "frontend/src/lib/api-client.ts"
      provides: "Credentialed API transport without x-user-id injection"
      contains: "credentials: 'include'"
    - path: "frontend/src/app/shell/app-shell.tsx"
      provides: "Header actions aligned to authenticated session (identity + logout)"
      contains: "logout"
    - path: "frontend/src/__tests__/auth-session-shell.test.tsx"
      provides: "Regression coverage for shell auth identity UI and logout flow"
      contains: "session"
  key_links:
    - from: "frontend/src/pages/items/item-create-wizard-page.tsx"
      to: "frontend/src/lib/api-client.ts"
      via: "item mutations rely on session cookies, not actor headers"
      pattern: "apiRequest"
    - from: "frontend/src/app/shell/app-shell.tsx"
      to: "frontend/src/auth/auth-context.tsx"
      via: "shell renders authenticated user state and triggers logout action"
      pattern: "useAuth"
---

<objective>
Remove the legacy frontend actor shim and finish client-side migration to session-derived identity so AUTH-03 is enforced across both API and browser layers.

Purpose: Eliminate dual identity sources that can drift or reintroduce insecure behavior after backend auth enforcement.
Output: Session-only API client usage, shell identity/logout UX, and updated frontend tests replacing actor-switcher assumptions.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-auth-sessions-protected-access/08-CONTEXT.md
@.planning/phases/08-auth-sessions-protected-access/08-RESEARCH.md
@frontend/src/app/shell/app-shell.tsx
@frontend/src/lib/api-client.ts
@frontend/src/pages/items/item-create-wizard-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove x-user-id injection APIs and actor-local-storage semantics from frontend transport</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/pages/items/item-list-page.tsx, frontend/src/pages/items/item-create-wizard-page.tsx, frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Delete `getActiveActorUserId`, `setActiveActorUserId`, and any `hact.active-user-id` logic from API client. Ensure all requests use cookie credentials and remove explicit `'x-user-id'` headers from list/create/update/delete item flows. Keep existing error-envelope parsing and ApiClientError behavior intact while converting callers to session-first contracts.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>No frontend request path injects actor identity headers; session cookies are the only auth transport mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Replace shell actor switcher UX with authenticated identity and explicit logout controls</name>
  <files>frontend/src/app/shell/app-shell.tsx, frontend/src/app/shell/user-switcher.tsx, frontend/src/__tests__/auth-session-shell.test.tsx</files>
  <action>Retire the actor switcher behavior from the app shell. Replace header control with authenticated user identity display and logout action wired to auth context. If keeping `user-switcher.tsx` for compatibility, convert it into a session identity component (no actor selection); otherwise remove it cleanly and update imports/routes accordingly.</action>
  <verify>
    <automated>npm --prefix frontend run test -- auth-session-shell --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Header no longer offers local actor impersonation controls and instead reflects real authenticated session identity with logout.</done>
</task>

<task type="auto">
  <name>Task 3: Replace stale actor-shim tests with session-based frontend regressions</name>
  <files>frontend/src/__tests__/items-workflows.test.tsx, frontend/src/__tests__/user-switcher-cache-refresh.test.tsx, frontend/src/__tests__/auth-session-shell.test.tsx</files>
  <action>Update item workflow tests to run without actor injection helpers. Remove or repurpose user-switcher cache tests so they no longer assert `x-user-id` header behavior; replace with session-oriented assertions (credentialed requests, logout-driven cache/session reset, and shell identity rendering). Ensure test names/descriptions no longer reference actor-switch shim semantics.</action>
  <verify>
    <automated>npm --prefix frontend run test -- items-workflows auth-session-shell --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Frontend regression suite now protects session-based identity behavior and no longer encodes deprecated actor-switch contracts.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run test -- items-workflows auth-session-shell --runInBand` and `npm --prefix frontend run typecheck`; confirm no `x-user-id` transport remains and shell/session behavior is fully session-driven.
</verification>

<success_criteria>
Client-side identity model is fully migrated to authenticated sessions, legacy actor-shim UI/transport paths are removed, and frontend workflows continue functioning under session auth.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-sessions-protected-access/08-04-SUMMARY.md`
</output>
