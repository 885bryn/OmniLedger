---
phase: 08-auth-sessions-protected-access
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/app/router.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/auth/auth-context.tsx
  - frontend/src/auth/require-auth.tsx
  - frontend/src/pages/auth/login-page.tsx
  - frontend/src/pages/auth/register-page.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
  - frontend/src/__tests__/auth-routes-guard.test.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "Unauthenticated users are redirected to login from protected frontend routes and returned to the exact intended deep link after successful login."
    - "Login failures show generic top-level and inline feedback, keep email populated, clear password, and enforce temporary cooldown UX."
    - "Deep links (including detail/edit routes with query params) are restored exactly after successful authentication."
  artifacts:
    - path: "frontend/src/auth/require-auth.tsx"
      provides: "Protected-route guard with returnTo preservation"
      contains: "returnTo"
    - path: "frontend/src/pages/auth/login-page.tsx"
      provides: "Credential form, generic auth errors, cooldown submit lock, and post-login redirect"
      contains: "password"
    - path: "frontend/src/pages/auth/register-page.tsx"
      provides: "Email+password registration flow aligned to Phase 8 contract"
      contains: "password"
  key_links:
    - from: "frontend/src/auth/require-auth.tsx"
      to: "frontend/src/app/router.tsx"
      via: "all protected routes are wrapped by route guard"
      pattern: "RequireAuth"
    - from: "frontend/src/pages/auth/login-page.tsx"
      to: "frontend/src/auth/auth-context.tsx"
      via: "login form uses auth context helpers and returnTo restore"
      pattern: "login"
---

<objective>
Ship frontend authentication UX and route gating so protected pages are only accessible through authenticated sessions while preserving deep-link intent and clear expiry handling.

Purpose: Convert the SPA from open navigation to session-aware access control without breaking user flow continuity.
Output: Auth context, login/register pages, route guard, and frontend regression tests for protected-route and sign-in UX behavior.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-auth-sessions-protected-access/08-CONTEXT.md
@.planning/phases/08-auth-sessions-protected-access/08-RESEARCH.md
@frontend/src/app/router.tsx
@frontend/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend auth session context and protected route guard with deep-link return restoration</name>
  <files>frontend/src/auth/auth-context.tsx, frontend/src/auth/require-auth.tsx, frontend/src/app/router.tsx, frontend/src/app/providers.tsx</files>
  <action>Create an auth context that hydrates from `/auth/session`, exposes `session/loading/login/logout` helpers, and stores sanitized app-relative `returnTo` values only. Wrap dashboard/items/events routes with `RequireAuth` so unauthenticated users always redirect to `/login?returnTo=...`. After successful auth, redirect to exact intended path+query+hash; when no return intent exists, use `/dashboard` as the default authenticated landing route.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Protected frontend routes require an authenticated session and deep links are restored after login using safe internal return intent handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement login/register pages with locked error UX and cooldown behavior</name>
  <files>frontend/src/pages/auth/login-page.tsx, frontend/src/pages/auth/register-page.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json</files>
  <action>Build email+password register/login screens for Phase 8 only. On failed sign-in, show a generic security-safe top alert plus inline field feedback, keep the email value, clear password, and display cooldown messaging that disables submit until retry is allowed. Keep wording and layout implementation at Claude discretion while preserving the locked behavior contract. Ensure successful login returns user to preserved intent or `/dashboard`.</action>
  <verify>
    <automated>npm --prefix frontend run test -- auth-routes-guard --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Auth forms satisfy required failure UX semantics and successful authentication path restoration behavior.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run test -- auth-routes-guard --runInBand` and `npm --prefix frontend run typecheck`; confirm protected route redirects, login failure UX semantics, and return-intent restoration behavior.
</verification>

<success_criteria>
Frontend requires authenticated sessions for protected routes, honors deep-link return flow, and provides the locked login/register failure UX behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-sessions-protected-access/08-02-SUMMARY.md`
</output>
