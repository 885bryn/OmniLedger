---
phase: 08-auth-sessions-protected-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/api/app.js
  - src/api/routes/auth.routes.js
  - src/api/auth/session-options.js
  - src/domain/auth/register-user.js
  - src/domain/auth/authenticate-user.js
  - src/domain/auth/username-from-email.js
  - test/api/auth-routes.test.js
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "User can register with email+password and immediately get an authenticated server session."
    - "User can sign in with valid credentials and invalid credentials return a generic safe error without revealing account existence."
    - "Explicit logout invalidates the current session immediately while other device sessions remain allowed."
  artifacts:
    - path: "src/api/routes/auth.routes.js"
      provides: "Register/login/logout/session endpoints with generic failure envelope and cooldown responses"
      exports: ["createAuthRouter"]
    - path: "src/api/auth/session-options.js"
      provides: "Shared express-session + Sequelize store configuration"
      contains: "httpOnly"
    - path: "test/api/auth-routes.test.js"
      provides: "Executable coverage for register, login, cooldown, logout, and session endpoint behavior"
      contains: "POST /auth/login"
  key_links:
    - from: "src/api/app.js"
      to: "src/api/routes/auth.routes.js"
      via: "session middleware registered before auth and protected routers"
      pattern: "app\.use\(session"
    - from: "src/api/routes/auth.routes.js"
      to: "src/domain/auth/authenticate-user.js"
      via: "login route delegates password verification + session regeneration"
      pattern: "authenticateUser"
---

<objective>
Establish the backend authentication foundation with durable cookie sessions and first-party auth endpoints so Phase 8 can gate all protected access on trusted server session state.

Purpose: Deliver secure credential handling and reliable session lifecycle primitives before route protection and frontend auth UX rollout.
Output: Installed auth/session dependencies, server session middleware wiring, auth endpoints, and backend auth contract tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-auth-sessions-protected-access/08-CONTEXT.md
@.planning/phases/08-auth-sessions-protected-access/08-RESEARCH.md
@src/api/app.js
@src/db/models/user.model.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable server session transport and auth package baseline</name>
  <files>package.json, package-lock.json, src/api/auth/session-options.js, src/api/app.js</files>
  <action>Install and wire `express-session`, `connect-session-sequelize`, `bcryptjs`, and `express-rate-limit`. Add shared session options (httpOnly cookie, sameSite=lax, secure-in-prod, explicit maxAge lifecycle, Sequelize-backed session store) and mount middleware before routers. Update CORS handling for credentialed requests (explicit origin env var + allow credentials) and remove wildcard behavior that breaks cookie auth.</action>
  <verify>
    <automated>npm test -- test/api/health.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>App boots with session middleware active, cookie transport configured safely, and credentialed CORS behavior ready for browser session flows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement register/login/logout/session routes with secure auth behavior</name>
  <files>src/api/routes/auth.routes.js, src/domain/auth/register-user.js, src/domain/auth/authenticate-user.js, src/domain/auth/username-from-email.js, src/db/models/user.model.js</files>
  <action>Create `/auth/register`, `/auth/login`, `/auth/logout`, and `/auth/session` routes. Use bcrypt hashing and compare, session regeneration on successful login (fixation defense), generic invalid-credential response envelope, and express-rate-limit cooldown response metadata for failed attempts. Keep Phase 8 signup email+password-only by deriving an internal unique username from email local-part + numeric suffix when needed (do not add profile username UX here). Ensure logout destroys only the current session and clears cookie immediately.</action>
  <verify>
    <automated>npm test -- test/api/auth-routes.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Auth routes satisfy secure credential handling, generic failure semantics, cooldown contract, and immediate current-session invalidation on logout.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend auth contract tests for success, failure, cooldown, and persistence semantics</name>
  <files>test/api/auth-routes.test.js</files>
  <action>Add focused API tests covering register success, login success, invalid login generic messaging, cooldown 429 envelope after repeated failures, `/auth/session` visibility with/without cookie, and logout invalidation. Use `supertest.agent` flows to prove session persistence across multiple requests and browser-refresh style follow-up requests.</action>
  <verify>
    <automated>npm test -- test/api/auth-routes.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Auth foundation is regression-protected with executable tests proving required Phase 8 credential and session behavior.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/auth-routes.test.js --runInBand` and confirm the suite proves secure register/login behavior, generic credential failures, cooldown handling, session persistence, and immediate logout invalidation.
</verification>

<success_criteria>
Backend exposes working auth endpoints with durable server-managed sessions and security-safe failure behavior, and these contracts are locked by targeted API tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-sessions-protected-access/08-01-SUMMARY.md`
</output>
