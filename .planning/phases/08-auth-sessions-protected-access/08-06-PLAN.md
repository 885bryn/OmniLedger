---
phase: 08-auth-sessions-protected-access
plan: 06
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - test/api/items-create.test.js
  - test/api/items-list-and-mutate.test.js
  - test/api/items-net-status.test.js
  - test/api/events-list.test.js
  - test/api/events-complete.test.js
  - test/api/users-list.test.js
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "API integration tests authenticate using session cookies rather than client-provided x-user-id headers."
    - "Existing items/events/users business behaviors continue to pass under session-derived actor identity."
    - "Supplying x-user-id in migrated tests is unnecessary and does not drive authorization behavior."
  artifacts:
    - path: "test/api/items-list-and-mutate.test.js"
      provides: "Session-authenticated items workflow coverage without x-user-id injection"
      contains: "/auth/login"
    - path: "test/api/events-complete.test.js"
      provides: "Session-authenticated event completion coverage"
      contains: "supertest.agent"
    - path: "test/api/users-list.test.js"
      provides: "Users API access coverage via authenticated session setup"
      contains: "cookie"
  key_links:
    - from: "test/api/items-create.test.js"
      to: "src/api/routes/items.routes.js"
      via: "tests use authenticated session helper instead of x-user-id header"
      pattern: "auth"
    - from: "test/api/events-list.test.js"
      to: "src/api/auth/require-auth.js"
      via: "route access expectations align with requireAuth session enforcement"
      pattern: "401"
---

<objective>
Migrate remaining backend integration suites from the legacy actor header shim to authenticated session-based setup so AUTH-03 stays locked across all existing API behavior tests.

Purpose: Prevent test harnesses from preserving deprecated x-user-id pathways after backend authorization migration.
Output: Updated items/events/users API suites that authenticate with session cookies and retain prior business assertions.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-auth-sessions-protected-access/08-CONTEXT.md
@.planning/phases/08-auth-sessions-protected-access/08-RESEARCH.md
@test/api/items-list-and-mutate.test.js
@test/api/events-complete.test.js
@test/api/users-list.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate items API suites to authenticated session setup</name>
  <files>test/api/items-create.test.js, test/api/items-list-and-mutate.test.js, test/api/items-net-status.test.js</files>
  <action>Refactor item API tests to authenticate through `/auth/register` + `/auth/login` (or shared `supertest.agent` helper) and remove all `.set('x-user-id', ...)` usage. Preserve existing create/list/mutate/net-status assertions while using session cookies as the only identity transport.</action>
  <verify>
    <automated>npm test -- test/api/items-create.test.js --runInBand && npm test -- test/api/items-list-and-mutate.test.js --runInBand && npm test -- test/api/items-net-status.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Items integration suites pass with session authentication only and no header-based actor injection.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate events and users API suites to authenticated session setup</name>
  <files>test/api/events-list.test.js, test/api/events-complete.test.js, test/api/users-list.test.js</files>
  <action>Apply the same session-authenticated setup pattern to events and users suites. Remove x-user-id header assumptions, keep existing domain behavior assertions intact, and ensure unauthorized flows still align with requireAuth behavior where asserted.</action>
  <verify>
    <automated>npm test -- test/api/events-list.test.js --runInBand && npm test -- test/api/events-complete.test.js --runInBand && npm test -- test/api/users-list.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Events/users integration suites pass using only authenticated session identity transport.</done>
</task>

</tasks>

<verification>
Run all six migrated suites and confirm they pass without using `x-user-id` headers in request setup.
</verification>

<success_criteria>
Backend regression tests now encode session-based authorization contracts and no longer preserve deprecated actor-header behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-sessions-protected-access/08-06-SUMMARY.md`
</output>
