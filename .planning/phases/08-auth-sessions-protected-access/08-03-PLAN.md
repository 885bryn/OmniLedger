---
phase: 08-auth-sessions-protected-access
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/api/auth/require-auth.js
  - src/api/routes/items.routes.js
  - src/api/routes/events.routes.js
  - src/api/routes/users.routes.js
  - src/api/app.js
  - test/api/authz-session-enforcement.test.js
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Protected API routes return 401 when no authenticated session exists."
    - "Server derives actor identity from authenticated session context, not from client-provided `x-user-id` headers."
    - "Supplying an arbitrary `x-user-id` no longer grants access to another user's data."
  artifacts:
    - path: "src/api/auth/require-auth.js"
      provides: "Single middleware source for session actor extraction and 401 responses"
      exports: ["requireAuth"]
    - path: "src/api/routes/items.routes.js"
      provides: "Items route handlers wired to req.actor.userId"
      contains: "req.actor.userId"
    - path: "test/api/authz-session-enforcement.test.js"
      provides: "Regression proof that forged x-user-id is ignored and unauthenticated requests are blocked"
      contains: "401"
  key_links:
    - from: "src/api/auth/require-auth.js"
      to: "src/api/routes/items.routes.js"
      via: "route middleware enforces session-derived actor before domain calls"
      pattern: "requireAuth"
    - from: "src/api/routes/events.routes.js"
      to: "src/domain/events/complete-event.js"
      via: "domain actorUserId sourced from req.actor.userId only"
      pattern: "actorUserId"
---

<objective>
Complete backend authorization boundary migration by enforcing session-based actor identity on every protected API route and removing the `x-user-id` trust path.

Purpose: Close the impersonation vector and ensure server-side authorization uses only authenticated session context.
Output: Auth middleware enforcement across API routes plus focused regression coverage that forged actor headers cannot bypass session-derived authorization.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-auth-sessions-protected-access/08-CONTEXT.md
@.planning/phases/08-auth-sessions-protected-access/08-RESEARCH.md
@src/api/routes/items.routes.js
@src/api/routes/events.routes.js
@src/api/routes/users.routes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requireAuth middleware and enforce session actor context in all protected routes</name>
  <files>src/api/auth/require-auth.js, src/api/routes/items.routes.js, src/api/routes/events.routes.js, src/api/routes/users.routes.js, src/api/app.js</files>
  <action>Create reusable `requireAuth` middleware that validates `req.session.userId`, hydrates `req.actor`, and returns a consistent 401 envelope for unauthenticated calls. Apply it to all protected business routes (items/events/users), remove any `req.header('x-user-id')` usage, and pass only `req.actor.userId` into domain services. Also remove `x-user-id` from CORS allow-headers so the old shim path is no longer part of the API contract.</action>
  <verify>
    <automated>npm test -- test/api/authz-session-enforcement.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Protected routes require session auth and actor identity can no longer be injected by client header.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused regression tests for unauthorized access and forged-header rejection</name>
  <files>test/api/authz-session-enforcement.test.js</files>
  <action>Add dedicated tests that (a) assert unauthenticated requests to protected routes return 401, and (b) assert requests with forged `x-user-id` but mismatched session identity still act as the session user (or are rejected), proving AUTH-03 closure explicitly.</action>
  <verify>
    <automated>npm test -- test/api/authz-session-enforcement.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>AUTH-03 is explicitly guarded by tests that fail if client-selected actor headers regain authorization power.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/api/authz-session-enforcement.test.js --runInBand`; confirm protected routes require authenticated session cookies and ignore client-selected actor headers for authorization.
</verification>

<success_criteria>
API authorization boundary is session-derived end-to-end, unauthenticated requests are blocked, and forged actor headers cannot bypass ownership enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-sessions-protected-access/08-03-SUMMARY.md`
</output>
