---
phase: 08-auth-sessions-protected-access
plan: 05
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - frontend/src/lib/api-client.ts
  - frontend/src/auth/auth-context.tsx
  - frontend/src/app/shell/app-shell.tsx
  - frontend/src/features/auth/session-expired-banner.tsx
  - frontend/src/__tests__/session-expiry-redirect.test.tsx
autonomous: true
requirements:
  - AUTH-02
must_haves:
  truths:
    - "When a protected API call returns 401 for an active user, the app shows a session-expired notice and redirects to login."
    - "Session-expired redirect preserves exact return intent including deep-link query params."
    - "Auth routes do not enter a 401 redirect loop while expiry handling is active."
  artifacts:
    - path: "frontend/src/lib/api-client.ts"
      provides: "Credentialed request transport with centralized 401 session-expired signaling"
      contains: "session-expired"
    - path: "frontend/src/features/auth/session-expired-banner.tsx"
      provides: "Visible session-expired UX shown during redirect flow"
      contains: "session"
    - path: "frontend/src/__tests__/session-expiry-redirect.test.tsx"
      provides: "Regression coverage for 401-triggered redirect + preserved returnTo"
      contains: "401"
  key_links:
    - from: "frontend/src/lib/api-client.ts"
      to: "frontend/src/auth/auth-context.tsx"
      via: "401 event dispatch triggers auth expiry redirect workflow"
      pattern: "session-expired"
    - from: "frontend/src/app/shell/app-shell.tsx"
      to: "frontend/src/features/auth/session-expired-banner.tsx"
      via: "shell renders expiry banner while redirecting to login"
      pattern: "session-expired"
---

<objective>
Add frontend session-expiry handling so 401 responses from protected APIs become a clear, user-visible redirect-to-login flow with deep-link return preserved.

Purpose: Complete AUTH-02 UX for expired sessions without weakening the protected-route behavior delivered in 08-02.
Output: Centralized 401 handling, session-expired banner rendering, and dedicated expiry redirect regression tests.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-auth-sessions-protected-access/08-CONTEXT.md
@.planning/phases/08-auth-sessions-protected-access/08-RESEARCH.md
@frontend/src/lib/api-client.ts
@frontend/src/auth/auth-context.tsx
@frontend/src/app/shell/app-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire centralized 401 session-expired signaling into auth context and shell banner UX</name>
  <files>frontend/src/lib/api-client.ts, frontend/src/auth/auth-context.tsx, frontend/src/app/shell/app-shell.tsx, frontend/src/features/auth/session-expired-banner.tsx</files>
  <action>Update API client to emit a centralized session-expired signal on 401 responses for protected requests while keeping credentialed fetch behavior enabled. In auth context + shell, consume that signal to show a clear session-expired banner and redirect to `/login` with preserved current location as sanitized app-relative `returnTo`. Ensure auth pages ignore repeated 401 redirect triggers to prevent loops.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>401 responses consistently trigger visible expiry messaging and deterministic login redirect with preserved return intent.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for session-expired redirect and deep-link restore</name>
  <files>frontend/src/__tests__/session-expiry-redirect.test.tsx</files>
  <action>Create focused tests that simulate protected API 401 responses during an active session, assert session-expired notice visibility, assert redirect to login with encoded `returnTo`, and assert post-login restore to the exact deep link path/query/hash.</action>
  <verify>
    <automated>npm --prefix frontend run test -- session-expiry-redirect --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Expiry redirect flow is locked by tests and fails if return-intent or banner behavior regresses.</done>
</task>

</tasks>

<verification>
Run `npm --prefix frontend run test -- session-expiry-redirect --runInBand` and `npm --prefix frontend run typecheck`; confirm 401 expiry handling shows banner, redirects to login, and preserves deep-link return intent.
</verification>

<success_criteria>
Frontend 401 expiry behavior is explicit and predictable: users see a session-expired notice, are routed to login, and can resume their intended deep link after re-authentication.
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-sessions-protected-access/08-05-SUMMARY.md`
</output>
