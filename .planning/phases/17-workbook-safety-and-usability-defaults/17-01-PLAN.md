---
phase: 17-workbook-safety-and-usability-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/exports/workbook-safety.js
  - src/domain/exports/workbook-formatters.js
  - src/domain/exports/workbook-model.js
  - test/domain/exports/workbook-safety.test.js
  - test/domain/exports/workbook-model.test.js
autonomous: true
requirements:
  - XLSX-02
  - SECU-01
must_haves:
  truths:
    - "Date/time export cells follow a single locale/timezone policy when preference inputs exist, and use one deterministic fallback when they do not."
    - "Invalid or unparseable date/time inputs render as one explicit marker value across all sheets."
    - "User-provided text values that begin with `=`, `+`, `-`, or `@` are apostrophe-prefixed so opened workbooks cannot execute formulas from those cells."
    - "Safe numeric and date values stay typed for export (not globally stringified)."
  artifacts:
    - path: "src/domain/exports/workbook-safety.js"
      provides: "Centralized export safety and date policy boundary for text sanitization and deterministic date resolution"
      exports: ["sanitizeWorkbookTextCell", "resolveExportDatePolicy", "toExportDateCell"]
    - path: "src/domain/exports/workbook-model.js"
      provides: "Workbook row projection that routes all text/date fields through one safety policy while preserving typed numeric/date cells"
      exports: ["buildWorkbookModel"]
    - path: "test/domain/exports/workbook-safety.test.js"
      provides: "Focused coverage for trigger-prefix sanitization, deterministic fallback policy, and invalid-date markers"
    - path: "test/domain/exports/workbook-model.test.js"
      provides: "Regression coverage proving cross-sheet sanitization and date policy application in emitted workbook model rows"
  key_links:
    - from: "src/domain/exports/workbook-model.js"
      to: "src/domain/exports/workbook-safety.js"
      via: "single normalization boundary for projected export cells"
      pattern: "sanitizeWorkbookTextCell|toExportDateCell|resolveExportDatePolicy"
    - from: "src/domain/exports/workbook-model.js"
      to: "src/domain/exports/workbook-formatters.js"
      via: "shared enum/reference/marker behavior kept consistent while moving date policy"
      pattern: "formatEnumLabel|resolveReadableReference|EXPLICIT_MARKERS"
    - from: "test/domain/exports/workbook-model.test.js"
      to: "src/domain/exports/workbook-model.js"
      via: "formula-trigger and date fallback assertions on Assets, Financial Contracts, and Event History rows"
      pattern: "'|N/A|Date"
---

<objective>
Add a centralized export-safety and date-policy layer to guarantee spreadsheet formula hardening plus deterministic locale/timezone date behavior before XLSX serialization.

Purpose: Fulfill SECU-01 and core XLSX-02 behavior at the workbook model boundary so every sheet and every user-text/date field follows one locked policy without per-sheet drift.
Output: New safety/policy utility module, workbook model integration, and regression tests that lock sanitization coverage, explicit invalid markers, and typed-value preservation.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-workbook-safety-and-usability-defaults/17-CONTEXT.md
@.planning/phases/17-workbook-safety-and-usability-defaults/17-RESEARCH.md
@.planning/phases/16-event-history-and-downloadable-workbook/16-02-SUMMARY.md
@src/domain/exports/workbook-model.js
@src/domain/exports/workbook-formatters.js
@src/domain/exports/workbook-columns.js
@test/domain/exports/workbook-model.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized workbook safety/date policy utilities</name>
  <files>src/domain/exports/workbook-safety.js, test/domain/exports/workbook-safety.test.js</files>
  <action>Create a dedicated export-domain utility that enforces the locked spreadsheet safety policy: apostrophe-prefix text values starting with `=`, `+`, `-`, or `@`; no mutation for safe strings; and strict no-op for non-string values. In the same module, add deterministic date policy resolution that accepts optional locale/timezone preferences, applies one explicit fallback locale/timezone when missing, and returns one explicit invalid-date marker for unparseable values. Keep policy functions pure and reusable so every sheet can consume them uniformly.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-safety.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Safety/date policy utilities exist as a single reusable boundary with dedicated unit coverage for trigger-prefix handling, deterministic fallback, and invalid marker behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Route workbook model projections through safety and date policy boundary</name>
  <files>src/domain/exports/workbook-model.js, src/domain/exports/workbook-formatters.js</files>
  <action>Refactor workbook projection so all user-provided text fields (including titles, labels, flattened attributes, overflow JSON text, and reference labels) pass through the sanitizer before rows are emitted. Replace string-only date shaping with policy-driven date cell normalization that honors provided locale/timezone preferences, uses deterministic fallback when unavailable, and emits explicit markers for invalid values. Preserve native `number`/`Date` typing where values are already safe, and avoid blanket conversion of all cells to strings.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Workbook model output reflects global formula sanitization and deterministic date policy while retaining safe numeric/date typing.</done>
</task>

<task type="auto">
  <name>Task 3: Expand workbook-model regressions for cross-sheet safety and date determinism</name>
  <files>test/domain/exports/workbook-model.test.js</files>
  <action>Extend domain fixtures to include formula-trigger strings in representative user-provided text fields across all sheets, valid and invalid date/time samples, and preference-present versus fallback date-policy scenarios. Assert: trigger-prefixed text is apostrophe-sanitized, invalid dates become one explicit marker, fallback behavior is deterministic and uniform, and numeric/date-safe values are not degraded to generic text cells.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-safety.test.js test/domain/exports/workbook-model.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Model-level tests fail on any regression in SECU-01 sanitizer coverage or XLSX-02 deterministic date behavior.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/exports/workbook-safety.test.js test/domain/exports/workbook-model.test.js --runInBand` and confirm sanitizer, date-policy, and workbook-row regressions all pass.
</verification>

<success_criteria>
- Export domain has one centralized sanitizer/policy boundary used by workbook row projection across all sheets.
- Formula-trigger text cells are apostrophe-prefixed globally, while safe non-string values stay typed.
- Date/time output honors provided locale/timezone preferences and uses one deterministic fallback when unavailable.
- Invalid/unparseable dates render as one explicit marker everywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/17-workbook-safety-and-usability-defaults/17-01-SUMMARY.md`
</output>
