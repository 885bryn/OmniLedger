---
phase: 17-workbook-safety-and-usability-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/api/routes/exports.routes.js
  - src/domain/exports/workbook-xlsx.js
  - test/domain/exports/workbook-xlsx.test.js
  - test/api/exports-backup-scope.test.js
autonomous: true
requirements:
  - XLSX-01
  - XLSX-02
  - SECU-01
must_haves:
  truths:
    - "Every workbook sheet opens with a frozen header row and active auto-filter defaults, including header-only sheets."
    - "Serialized workbook output preserves typed export values (including date cells) and does not undo phase-17 sanitization behavior."
    - "Export route passes locale/timezone preference context when available and applies deterministic fallback behavior when absent."
  artifacts:
    - path: "src/domain/exports/workbook-xlsx.js"
      provides: "Serializer-level sheet usability defaults and typed cell handling for workbook model output"
      exports: ["serializeWorkbookToXlsx", "SHEET_ORDER"]
    - path: "src/api/routes/exports.routes.js"
      provides: "Request-context preference wiring for workbook date policy with deterministic fallback path"
      exports: ["createExportsRouter"]
    - path: "test/domain/exports/workbook-xlsx.test.js"
      provides: "Regression assertions for freeze/filter defaults and typed cell serialization behavior"
    - path: "test/api/exports-backup-scope.test.js"
      provides: "End-to-end export assertions for preference-present and fallback date behavior with safe workbook output"
  key_links:
    - from: "src/api/routes/exports.routes.js"
      to: "src/domain/exports/workbook-model.js"
      via: "route passes resolved date preference context into workbook model build"
      pattern: "buildWorkbookModel\(.*date|locale|timezone"
    - from: "src/domain/exports/workbook-xlsx.js"
      to: "src/domain/exports/workbook-model.js"
      via: "serializer consumes typed/sanitized row values without coercing safe types"
      pattern: "worksheet\.addRow|numFmt|Date"
    - from: "test/api/exports-backup-scope.test.js"
      to: "GET /exports/backup.xlsx"
      via: "workbook parse assertions across owner/all/lens with locale/timezone + fallback checks"
      pattern: "content-disposition|Assets|Financial Contracts|Event History"
---

<objective>
Complete workbook transport hardening by wiring preference-aware export context into the route and locking serializer usability defaults/typed behavior with integration regressions.

Purpose: Fulfill XLSX-01 and finalize XLSX-02 + SECU-01 behavior through the binary export boundary so generated `.xlsx` files are consistently usable and policy-correct when opened.
Output: Updated route and serializer code plus regression suites proving freeze/filter defaults, typed cell preservation, and preference/fallback behavior at API level.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-workbook-safety-and-usability-defaults/17-CONTEXT.md
@.planning/phases/17-workbook-safety-and-usability-defaults/17-RESEARCH.md
@.planning/phases/17-workbook-safety-and-usability-defaults/17-01-SUMMARY.md
@src/api/routes/exports.routes.js
@src/domain/exports/workbook-model.js
@src/domain/exports/workbook-xlsx.js
@test/domain/exports/workbook-xlsx.test.js
@test/api/exports-backup-scope.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export request preference wiring with deterministic fallback path</name>
  <files>src/api/routes/exports.routes.js, test/api/exports-backup-scope.test.js</files>
  <action>Extend export route request handling to resolve locale/timezone preference inputs from available app request context (for example authenticated actor/session metadata and request headers) and pass those preferences into workbook model generation. Enforce deterministic constants when either preference is unavailable, and keep existing scope-authoritative behavior untouched. Add API tests that cover both preference-present and fallback flows while preserving owner/all/lens scope assertions.</action>
  <verify>
    <automated>npm test -- test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Route forwards stable locale/timezone context for exports and remains scope-safe under all existing RBAC scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten XLSX serializer defaults and typed value emission guarantees</name>
  <files>src/domain/exports/workbook-xlsx.js, test/domain/exports/workbook-xlsx.test.js</files>
  <action>Keep serializer focused on workbook structure and enforce identical frozen-header and auto-filter defaults for all sheets regardless of row count. Ensure typed values from workbook model (including Date objects) are written without string coercion; apply explicit worksheet date display formatting only where needed for predictable first-open readability. Preserve deterministic sheet order and header mapping behavior from Phase 16.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-xlsx.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Workbook serializer consistently emits usable sheets with frozen/filter defaults and typed cell fidelity.</done>
</task>

<task type="auto">
  <name>Task 3: Lock end-to-end workbook safety/usability behavior in binary export regressions</name>
  <files>test/api/exports-backup-scope.test.js, test/domain/exports/workbook-xlsx.test.js</files>
  <action>Augment route and serializer tests to parse generated workbook bytes and assert phase-17 outcomes together: all sheets have freeze/filter defaults (including header-only cases), formula-trigger user text is sanitized in final cell values, date outputs follow preference context when provided and deterministic fallback otherwise, and existing scope matrices still pass. Keep assertions deterministic and machine-agnostic.</action>
  <verify>
    <automated>npm test -- test/domain/exports/workbook-xlsx.test.js test/api/exports-backup-scope.test.js --runInBand</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>API-level tests prove XLSX-01, XLSX-02, and SECU-01 behavior survives complete export serialization and transport.</done>
</task>

</tasks>

<verification>
Run `npm test -- test/domain/exports/workbook-xlsx.test.js test/api/exports-backup-scope.test.js --runInBand` and confirm serializer + API coverage pass with freeze/filter, preference/fallback date behavior, and sanitized text assertions.
</verification>

<success_criteria>
- All exported sheets keep frozen headers and active auto-filters, including header-only outputs.
- Route passes preference-aware date policy context when available and deterministic fallback when not.
- Serializer preserves typed safe values and does not regress formula-safety output.
- Integration tests lock full-binary workbook behavior across scope modes and policy paths.
</success_criteria>

<output>
After completion, create `.planning/phases/17-workbook-safety-and-usability-defaults/17-02-SUMMARY.md`
</output>
