---
phase: 26-motion-interaction-patterns
plan: "02"
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/features/dashboard/data-card.tsx
  - frontend/src/pages/dashboard/dashboard-page.tsx
  - frontend/src/pages/events/events-page.tsx
  - frontend/src/features/events/complete-event-row-action.tsx
  - frontend/src/features/events/edit-event-row-action.tsx
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
autonomous: true
requirements: [MOTION-01, MOTION-02, MOTION-04, IMPL-04]
must_haves:
  truths:
    - User sees dashboard and events panels enter and leave with the same restrained spring feel instead of abrupt appearance changes.
    - User sees neighboring cards or rows reposition fluidly when event state changes move records between sections or regroup the layout.
    - User sees newly materialized rows use a slight lift plus brief tint confirmation so additions are easy to spot without a dramatic animation.
  artifacts:
    - path: frontend/src/pages/dashboard/dashboard-page.tsx
      provides: Animated dashboard metric, asset, and due-group surfaces
      contains: MotionPanelList
    - path: frontend/src/pages/events/events-page.tsx
      provides: Animated grouped event panels and rows with layout reflow
      contains: MotionPanelList
    - path: frontend/src/features/dashboard/data-card.tsx
      provides: Dashboard card surface compatible with shared motion wrappers
      contains: Card
    - path: frontend/src/__tests__/dashboard-events-flow.test.tsx
      provides: Regression coverage for animated dashboard and events behavior contracts
      contains: EventsPage
  key_links:
    - from: frontend/src/pages/dashboard/dashboard-page.tsx
      to: frontend/src/components/ui/motion-panel-list.tsx
      via: shared metric and group animation wrapper
      pattern: MotionPanelList
    - from: frontend/src/pages/events/events-page.tsx
      to: frontend/src/lib/motion.ts
      via: layout spring and create-confirmation variants
      pattern: layout|stagger|highlight
---

<objective>
Apply the shared motion language to the dashboard and events exemplar surfaces.

Purpose: These dense, high-change views are where fluid layout reflow and shared spring behavior matter most for legibility, so they should become the milestone's reference implementation.
Output: Dashboard and events surfaces using the shared animated list or panel pattern, layout reflow, and restrained create-confirmation feedback.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-motion-interaction-patterns/26-CONTEXT.md
@.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-01-SUMMARY.md
@.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-03-SUMMARY.md
@frontend/src/features/dashboard/data-card.tsx
@frontend/src/pages/dashboard/dashboard-page.tsx
@frontend/src/pages/events/events-page.tsx
@frontend/src/__tests__/dashboard-events-flow.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate dashboard cards, asset rows, and due-date panels with shared layout motion</name>
  <files>frontend/src/features/dashboard/data-card.tsx, frontend/src/pages/dashboard/dashboard-page.tsx</files>
  <action>Replace the dashboard's ad hoc `animate-fade-up` usage with the shared motion primitives from Plan 26-01. Use the reusable animated panel/list wrapper for metric cards, asset tiles, and due-date group panels so mounts and unmounts share the locked spring values and surrounding cards reposition with `layout` animation instead of abrupt reflow. Preserve the Phase 25 surface system, keep motion restrained and mostly consistent, and use only a small stagger for metric or multi-card inserts. Do not change query behavior, data ordering, or business logic.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>The dashboard becomes a clear exemplar of shared spring-driven entry and layout reflow on shadcn-based cards and panels without changing the data contract.</done>
</task>

<task type="auto">
  <name>Task 2: Animate grouped event panels and row state changes with create-confirmation feedback</name>
  <files>frontend/src/pages/events/events-page.tsx, frontend/src/features/events/complete-event-row-action.tsx, frontend/src/features/events/edit-event-row-action.tsx, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Move the events page onto the shared motion list/panel pattern so date groups and individual rows animate with `layout` when rows move between upcoming and history or when projected exceptions materialize as persisted rows. For newly materialized or freshly inserted rows, use the locked create-entry treatment: `opacity: 0, y: 10` to settled state, plus a very brief subtle tint flash and only a small stagger when several rows appear together. Apply the shared light press feel to the row action controls used here, but keep completion, undo, edit, invalid-lens, and refetch behavior unchanged.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Event groups and rows move with fluid spring motion, new or materialized rows register with subtle confirmation feedback, and the existing dashboard/events regression suite still passes.</done>
</task>

</tasks>

<verification>
Run the combined dashboard and events regression suite and confirm it still proves completion, undo, projected-save-exception, and metric-card behavior while the UI now uses shared motion wrappers and layout animation.
</verification>

<success_criteria>
The dashboard and events views satisfy MOTION-01, MOTION-02, and the exemplar portion of IMPL-04, while event row materialization supplies a concrete MOTION-04 reference implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-02-SUMMARY.md`
</output>
