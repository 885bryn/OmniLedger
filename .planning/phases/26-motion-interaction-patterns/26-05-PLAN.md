---
phase: 26-motion-interaction-patterns
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/motion.ts
  - frontend/src/pages/events/events-page.tsx
  - frontend/src/__tests__/dashboard-events-flow.test.tsx
autonomous: true
gap_closure: true
requirements: [MOTION-01, MOTION-02, MOTION-03, MOTION-04, IMPL-04]
must_haves:
  truths:
    - User sees phase-26 exits keep the same restrained spring feel as enters instead of switching to bespoke easing.
    - User sees the events page header mount through the shared Framer Motion contract instead of the legacy CSS fade-up animation.
    - Engineer keeps the events page as a clean IMPL-04 exemplar where panel motion comes from the shared motion module and reusable list pattern.
  artifacts:
    - path: frontend/src/lib/motion.ts
      provides: Shared motion variants whose exit states stay on the phase-26 spring baseline
      contains: stiffness: 400
    - path: frontend/src/pages/events/events-page.tsx
      provides: Events header and grouped panels using the shared Framer Motion contract
      contains: MotionPanelList
    - path: frontend/src/__tests__/dashboard-events-flow.test.tsx
      provides: Regression coverage for the events page motion-backed structure
      contains: EventsPage
  key_links:
    - from: frontend/src/pages/events/events-page.tsx
      to: frontend/src/lib/motion.ts
      via: shared page and entry variants for the header surface
      pattern: motion|variant|spring
    - from: frontend/src/pages/events/events-page.tsx
      to: frontend/src/components/ui/motion-panel-list.tsx
      via: shared grouped-panel and row motion exemplar
      pattern: MotionPanelList
---

<objective>
Close the remaining Phase 26 verification gaps around shared spring exits and the events header motion contract.

Purpose: Phase 26 only fails because one shared motion module still uses bespoke exit timing and one events surface still bypasses the shared Framer Motion language, so this plan aligns those last surfaces with the locked baseline.
Output: Shared spring-based exit variants in `frontend/src/lib/motion.ts`, an events header wired to shared Framer Motion primitives, and regression coverage proving the events page no longer relies on the legacy CSS animation.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-motion-interaction-patterns/26-CONTEXT.md
@.planning/phases/26-motion-interaction-patterns/26-VERIFICATION.md
@.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-01-SUMMARY.md
@.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-02-SUMMARY.md
@frontend/src/lib/motion.ts
@frontend/src/pages/events/events-page.tsx
@frontend/src/__tests__/dashboard-events-flow.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move phase-26 exit motion onto the shared spring baseline</name>
  <files>frontend/src/lib/motion.ts</files>
  <action>Update the shared motion module so every phase-26 exit path that currently uses fixed ease and duration values instead reuses the locked spring baseline from plan 26-01. Keep the restrained Mac utility feel from `26-CONTEXT.md`, preserve the existing subtle offset and opacity targets, and do not introduce a second exit preset, CSS fallback animation, or page-specific transition constants. Treat `createEntryVariants.exit` and `subtlePageShiftVariants.exit` as the blocker called out in `26-VERIFICATION.md` and align them to the same `type: "spring", stiffness: 400, damping: 30` contract already used for enters.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>`frontend/src/lib/motion.ts` exposes one shared spring language for both enter and exit motion, satisfying the remaining shared-baseline portion of MOTION-01.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the events header's legacy CSS animation with shared Framer Motion coverage</name>
  <files>frontend/src/pages/events/events-page.tsx, frontend/src/__tests__/dashboard-events-flow.test.tsx</files>
  <action>Remove the events page header's legacy `animate-fade-up` usage and mount that surface through the shared Framer Motion primitives already established in plans 26-01 and 26-02. Keep the header visually and structurally identical, keep grouped event panels and row actions on their existing MotionPanelList and Pressable contracts, and add or update regression coverage so the events page structure proves the header no longer depends on the legacy CSS animation path. Do not change event querying, grouping, completion flows, or any business behavior while closing this motion-only gap.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/dashboard-events-flow.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>The events page header participates in the shared motion contract, and focused regression coverage confirms the events page stays on the shared Phase 26 exemplar path for MOTION-01 and IMPL-04.</done>
</task>

</tasks>

<verification>
Re-run the focused events regression and typecheck, then confirm `26-VERIFICATION.md` blocker details are directly addressed: no bespoke exit timing remains in `frontend/src/lib/motion.ts`, and the events header no longer uses `animate-fade-up`.
</verification>

<success_criteria>
Phase 26 no longer mixes bespoke exit timing or legacy CSS motion into its exemplar surfaces, so the shared spring baseline fully covers mounts and unmounts while the events page remains a valid IMPL-04 reference.
</success_criteria>

<output>
After completion, create `.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-05-SUMMARY.md`
</output>
