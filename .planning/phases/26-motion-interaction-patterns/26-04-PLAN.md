---
phase: 26-motion-interaction-patterns
plan: "04"
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/features/ui/confirmation-dialog.tsx
  - frontend/src/features/items/item-soft-delete-dialog.tsx
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/app/shell/theme-toggle.tsx
  - frontend/src/app/shell/language-switcher.tsx
  - frontend/src/__tests__/user-switcher-export-action.test.tsx
  - frontend/src/__tests__/theme-toggle-shell.test.tsx
autonomous: true
requirements: [MOTION-03]
must_haves:
  truths:
    - User gets the same light tactile press feedback on dialog actions and authenticated shell controls.
    - User sees shell controls keep one consistent press personality instead of different motion styles per action type.
    - User keeps the existing export, logout, and theme-toggle behavior while the press feedback becomes motion-consistent.
  artifacts:
    - path: frontend/src/features/ui/confirmation-dialog.tsx
      provides: Shared dialog actions using the shared press feel
      contains: Button
    - path: frontend/src/features/items/item-soft-delete-dialog.tsx
      provides: Destructive confirmation surface aligned to the shared press contract
      contains: Button
    - path: frontend/src/app/shell/user-switcher.tsx
      provides: Export and logout shell actions with shared tactile feedback
      contains: Export Backup
    - path: frontend/src/app/shell/theme-toggle.tsx
      provides: Theme control with shared shell press feedback
      contains: Button
  key_links:
    - from: frontend/src/features/ui/confirmation-dialog.tsx
      to: frontend/src/components/ui/pressable.tsx
      via: shared dialog action press wrapper
      pattern: Pressable|scale
    - from: frontend/src/app/shell/user-switcher.tsx
      to: frontend/src/components/ui/pressable.tsx
      via: shared shell-control press feedback
      pattern: Pressable|scale
---

<objective>
Roll the shared press language onto dialog actions and authenticated shell controls.

Purpose: The milestone should feel consistent beyond cards and list rows, so dialog and shell actions need the same light native-like press behavior without reopening larger item-motion work.
Output: Shared tactile press feedback on confirmation dialogs, export/logout controls, theme switching, and language switching.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-motion-interaction-patterns/26-CONTEXT.md
@.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-02-SUMMARY.md
@frontend/src/features/ui/confirmation-dialog.tsx
@frontend/src/features/items/item-soft-delete-dialog.tsx
@frontend/src/app/shell/user-switcher.tsx
@frontend/src/app/shell/theme-toggle.tsx
@frontend/src/app/shell/language-switcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the shared press feel to dialog actions</name>
  <files>frontend/src/features/ui/confirmation-dialog.tsx, frontend/src/features/items/item-soft-delete-dialog.tsx</files>
  <action>Use the shared press wrapper from Plan 26-01 so primary and destructive dialog actions share one light native-like press feel across mouse and touch. Keep the current shadcn visual system, portal behavior, and accessibility semantics intact, and do not create an alternate motion personality for destructive actions.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/items-workflows.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Shared confirmation actions use the same tactile press feedback contract without changing delete or confirmation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the shared press feel to authenticated shell controls</name>
  <files>frontend/src/app/shell/user-switcher.tsx, frontend/src/app/shell/theme-toggle.tsx, frontend/src/app/shell/language-switcher.tsx, frontend/src/__tests__/user-switcher-export-action.test.tsx, frontend/src/__tests__/theme-toggle-shell.test.tsx</files>
  <action>Use the same shared press wrapper on export/logout controls, theme switching, and language switching so shell controls match the dialog and item-surface press language. Preserve the existing behavioral contracts, labels, and regressions for export, logout, and theme switching, and keep the feel subtle and consistent per the locked decision.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx src/__tests__/theme-toggle-shell.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Authenticated shell controls use the shared tactile press feedback while preserving their current regressions and action semantics.</done>
</task>

</tasks>

<verification>
Run the export shell and theme shell regressions to prove the shared press behavior was layered onto existing controls without changing action semantics.
</verification>

<success_criteria>
Dialog and shell controls complete the non-card portion of MOTION-03 while keeping one shared press feel across the authenticated experience.
</success_criteria>

<output>
After completion, create `.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-04-SUMMARY.md`
</output>
