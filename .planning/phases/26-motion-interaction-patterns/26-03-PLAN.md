---
phase: 26-motion-interaction-patterns
plan: "03"
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/pages/items/item-list-page.tsx
  - frontend/src/features/items/item-filters.tsx
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/features/ui/confirmation-dialog.tsx
  - frontend/src/features/items/item-soft-delete-dialog.tsx
  - frontend/src/app/shell/user-switcher.tsx
  - frontend/src/app/shell/theme-toggle.tsx
  - frontend/src/app/shell/language-switcher.tsx
  - frontend/src/__tests__/items-workflows.test.tsx
  - frontend/src/__tests__/user-switcher-export-action.test.tsx
  - frontend/src/__tests__/theme-toggle-shell.test.tsx
autonomous: true
requirements: [MOTION-02, MOTION-03, MOTION-04]
must_haves:
  truths:
    - User gets the same light tactile press feedback on shell controls, dialog actions, filter chips, and item action surfaces.
    - User sees item rows or cards animate in with a slight lift and brief tint confirmation when records are newly created or restored.
    - User sees item list and detail surfaces reposition fluidly when filters, deletes, restores, or linked-record sections change the layout.
  artifacts:
    - path: frontend/src/pages/items/item-list-page.tsx
      provides: Animated item rows with create or restore confirmation behavior
      contains: layout
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Animated detail summary and linked-record surfaces with motion-safe actions
      contains: hover-lift
    - path: frontend/src/features/ui/confirmation-dialog.tsx
      provides: Shared dialog action surface using the shared press feel
      contains: Button
    - path: frontend/src/app/shell/user-switcher.tsx
      provides: Motion-aligned export and logout shell actions
      contains: Export Backup
  key_links:
    - from: frontend/src/pages/items/item-list-page.tsx
      to: frontend/src/lib/motion.ts
      via: new-item highlight key and shared create-entry variants
      pattern: y: 10|highlight|layout
    - from: frontend/src/app/shell/theme-toggle.tsx
      to: frontend/src/components/ui/pressable.tsx
      via: shared shell-control press feedback
      pattern: Pressable|scale
---

<objective>
Roll tactile motion and create-confirmation behavior across item workflows, dialogs, and shell controls.

Purpose: After the dashboard exemplar lands, the rest of the updated shadcn surfaces need the same press language and fluid item-level feedback so the milestone feels coherent instead of partially animated.
Output: Item list/detail motion rollout, create or restore confirmation behavior, and shared press feedback on dialog and shell controls.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-motion-interaction-patterns/26-CONTEXT.md
@.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-02-SUMMARY.md
@.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-03-SUMMARY.md
@frontend/src/pages/items/item-list-page.tsx
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/features/ui/confirmation-dialog.tsx
@frontend/src/app/shell/user-switcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate item list and detail surfaces with fluid reflow and new-record confirmation</name>
  <files>frontend/src/pages/items/item-list-page.tsx, frontend/src/features/items/item-filters.tsx, frontend/src/pages/items/item-detail-page.tsx, frontend/src/__tests__/items-workflows.test.tsx</files>
  <action>Apply the shared motion primitives to the item list and item detail surfaces only where existing shadcn-style cards, rows, and filter controls already exist. Item rows, filter chips, linked commitment rows, and summary cards should use the shared spring language and `layout` reflow so deletes, restores, filter changes, and linked-record changes do not snap. Add creation-confirmation wiring for existing item workflows by carrying a small “new or restored item” signal from the create or restore mutation path into the first rendered row or header card, then animate it with the locked fade-up-with-lift and brief subtle tint flash. Keep all current item CRUD, navigation, unsaved-change, and RBAC behavior untouched.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/items-workflows.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Items pages use shared motion for reflow and entry, and newly created or restored records receive a subtle confirmation treatment without changing workflow semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the shared press feel to dialog actions and authenticated shell controls</name>
  <files>frontend/src/features/ui/confirmation-dialog.tsx, frontend/src/features/items/item-soft-delete-dialog.tsx, frontend/src/app/shell/user-switcher.tsx, frontend/src/app/shell/theme-toggle.tsx, frontend/src/app/shell/language-switcher.tsx, frontend/src/__tests__/user-switcher-export-action.test.tsx, frontend/src/__tests__/theme-toggle-shell.test.tsx</files>
  <action>Use the shared press wrapper from Plan 26-01 so primary dialog actions, destructive confirmations, export/logout controls, theme switching, and language switching all share one light native-like press feel across mouse and touch. Keep the current shadcn visual system, portal behavior, accessibility labels, and export or logout contracts intact. Do not create alternate motion personalities for destructive versus primary actions; all eligible controls should share the same press language per the locked decision.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/user-switcher-export-action.test.tsx src/__tests__/theme-toggle-shell.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Dialog and shell controls all use the shared tactile press feedback while preserving their existing behavioral regressions.</done>
</task>

</tasks>

<verification>
Run the item workflow, export shell, and theme shell regressions to prove motion was layered onto the existing UI without changing delete, restore, export, logout, or theme-toggle contracts.
</verification>

<success_criteria>
Item workflows, dialogs, and shell controls satisfy the tactile-feedback and create-confirmation portions of MOTION-03 and MOTION-04 while extending MOTION-02 reflow behavior beyond the dashboard exemplar.
</success_criteria>

<output>
After completion, create `.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-03-SUMMARY.md`
</output>
