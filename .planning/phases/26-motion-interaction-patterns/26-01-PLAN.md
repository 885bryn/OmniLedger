---
phase: 26-motion-interaction-patterns
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/lib/motion.ts
  - frontend/src/components/ui/pressable.tsx
  - frontend/src/components/ui/motion-panel-list.tsx
  - frontend/src/app/shell/app-shell.tsx
autonomous: true
requirements: [MOTION-01, MOTION-03, IMPL-04]
must_haves:
  truths:
    - User sees in-shell page content shift subtly instead of swapping abruptly inside the authenticated shell.
    - User sees motion across the phase follow one restrained spring baseline instead of page-specific transition values.
    - Engineer has a reusable shadcn-friendly list or panel motion pattern ready for dashboard and item surfaces.
  artifacts:
    - path: frontend/src/lib/motion.ts
      provides: Shared spring, press, stagger, and create-confirmation motion tokens
      contains: stiffness: 400
    - path: frontend/src/components/ui/pressable.tsx
      provides: Shared tactile press wrapper for button or link-like controls
      contains: scale: 0.97
    - path: frontend/src/components/ui/motion-panel-list.tsx
      provides: Reusable animated list or panel wrapper built for shadcn surfaces
      exports: [MotionPanelList]
    - path: frontend/src/app/shell/app-shell.tsx
      provides: Subtle in-shell content-shift wiring for route content
      contains: location.pathname
  key_links:
    - from: frontend/src/app/shell/app-shell.tsx
      to: frontend/src/lib/motion.ts
      via: shared page transition variants and spring config
      pattern: subtle|page|spring
    - from: frontend/src/components/ui/motion-panel-list.tsx
      to: frontend/src/lib/motion.ts
      via: shared layout and create-entry presets
      pattern: layout|spring|highlight
---

<objective>
Establish the shared Framer Motion foundation for the milestone.

Purpose: Lock the restrained premium motion language once so later plans can apply the same spring, press, stagger, and creation-feedback behavior without ad hoc values.
Output: Framer Motion dependency wiring, shared motion utilities, a reusable `@/components/ui` animated list/panel exemplar, and subtle in-shell content-shift behavior in the authenticated shell.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-motion-interaction-patterns/26-CONTEXT.md
@.planning/phases/25-dashboard-surface-system/25-dashboard-surface-system-01-SUMMARY.md
@frontend/package.json
@frontend/src/app/shell/app-shell.tsx
@frontend/src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared motion tokens and reusable animated surface primitives</name>
  <files>frontend/package.json, frontend/src/lib/motion.ts, frontend/src/components/ui/pressable.tsx, frontend/src/components/ui/motion-panel-list.tsx</files>
  <action>Add `framer-motion` to the frontend package and create one shared motion contract in `frontend/src/lib/motion.ts`. Encode the locked spring baseline exactly with `type: "spring"`, `stiffness: 400`, and `damping: 30`, plus the locked press scale `0.97`, restrained stagger helpers for multi-additions, a fade-up-with-lift create preset using `opacity: 0, y: 10`, and a brief subtle tint-highlight helper. Build a reusable `Pressable` wrapper that later plans can apply to buttons and link-like controls, and a reusable `MotionPanelList` (or equivalent named export) under `@/components/ui` that combines `layout`, `AnimatePresence`, the shared spring, optional new-item highlight state, and small stagger support. Keep the API shaped for existing shadcn card/row surfaces; do not introduce route-level cinematic transitions, bespoke easing sets, or non-shared motion values.</action>
  <verify>
    <automated>npm --prefix frontend install &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>The repo has one reusable motion source of truth, one reusable press wrapper, and one reusable `@/components/ui` animated list or panel pattern that downstream plans can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire subtle in-shell content shift onto the authenticated layout</name>
  <files>frontend/src/app/shell/app-shell.tsx</files>
  <action>Update the authenticated shell so routed page content enters and exits with a subtle in-shell content shift that follows the shared spring language from Task 1. Keep the effect restrained and utility-like per the locked decision: a small offset and opacity change inside the shell only, not a large route transition, shared-element animation, or full-page wipe. Preserve the existing shell topology, sticky header, mobile sheet behavior, and React Router outlet structure so later plans can animate inner lists without reworking navigation.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/theme-toggle-shell.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Authenticated route content no longer swaps abruptly, and the shell still passes its focused regression coverage.</done>
</task>

</tasks>

<verification>
Confirm the shared motion utilities expose the locked spring, press, and create-entry values once, and the shell now uses only the subtle in-shell transition that the phase context allows.
</verification>

<success_criteria>
The codebase has one reusable motion baseline for MOTION-01 and MOTION-03 rollout, one exemplar `@/components/ui` motion primitive for IMPL-04, and an authenticated shell prepared for subtle in-shell motion instead of abrupt content swaps.
</success_criteria>

<output>
After completion, create `.planning/phases/26-motion-interaction-patterns/26-motion-interaction-patterns-01-SUMMARY.md`
</output>
