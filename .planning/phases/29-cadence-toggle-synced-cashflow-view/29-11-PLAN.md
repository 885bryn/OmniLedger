---
phase: 29-cadence-toggle-synced-cashflow-view
plan: "11"
type: execute
wave: 2
depends_on: [29-10]
files_modified:
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/__tests__/item-detail-ledger.test.tsx
autonomous: true
gap_closure: true
requirements: [CASH-03, VIEW-01, VIEW-02, VIEW-03, SAFE-01]
must_haves:
  truths:
    - The item-detail UI shows exact active weekly, monthly, and yearly period boundaries without timezone drift or off-by-one end dates.
    - Frontend period filtering and labels stay synchronized with the backend cadence period metadata and with the totals currently rendered on screen.
    - Assets that have qualifying cadence totals from the API no longer appear visually zeroed because the UI mis-derives active periods.
    - Phase 29 remains limited to cadence consistency fixes only, with no progress visual or event-history tab added.
  artifacts:
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Day-safe active-period formatting and filtering aligned with backend cadence metadata
      contains: resolveActivePeriodLabel
    - path: frontend/src/__tests__/item-detail-ledger.test.tsx
      provides: UI regressions for exact cadence boundaries, synchronized labels, and non-zero event-backed assets
      contains: summaryPeriodHint
  key_links:
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: summary.active_period
      via: exact boundary label rendering and active-period comparisons based on backend-provided day ranges
      pattern: active_period
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: summary.cadence_totals.recurring.active_periods
      via: selected display cadence drives both visible totals and visible period wording from the same metadata source
      pattern: active_periods
---

<objective>
Close the remaining item-detail display gap so UI period labels and cadence filtering match the corrected backend contract exactly.

Purpose: Even with correct backend math, users still lose trust if the UI shows shifted month boundaries or zero-looking cadence views because the frontend derives dates differently.
Output: Item-detail date handling updates plus UI regressions for exact cadence windows, synchronized wording, and non-zero event-backed summary rendering.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-CONTEXT.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-VERIFICATION.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-09-SUMMARY.md
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/__tests__/item-detail-ledger.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make item-detail active-period labels and filtering day-safe</name>
  <files>frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Replace the current Date.parse-based active-period formatting and due-date inclusion helpers with a single day-safe path that treats backend `YYYY-MM-DD` cadence metadata as exact calendar days instead of timezone-shiftable instants. Use that same helper for visible period labels and for active-cadence row filtering so monthly boundaries like March 1 through March 31 stay exact in every locale and the rendered count/labels continue to match the values already on screen. Preserve the segmented cadence control, synchronized-card behavior, full linked commitments tab visibility, and current Phase 29 boundaries; do not add progress visuals or an event-history tab.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Item-detail period labels and active-period comparisons no longer drift by a day, and the UI derives displayed cadence ranges from the same exact day boundaries that back the rendered totals.</done>
</task>

<task type="auto">
  <name>Task 2: Add UI regressions for exact boundaries and non-zero cadence rendering</name>
  <files>frontend/src/__tests__/item-detail-ledger.test.tsx</files>
  <action>Add focused UI regressions proving: (a) monthly boundary copy renders the exact backend range instead of an off-by-one end date, (b) switching cadence keeps titles, period hints, and rendered values synchronized from the same active-period metadata, and (c) an asset with valid API totals and qualifying events does not appear as all-zero because of frontend period-derivation mistakes. Keep the fixtures and assertions system-wide in intent; the named repro assets are examples, not special cases.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/item-detail-ledger.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Frontend regressions fail if the UI shows shifted period boundaries, desynchronizes cadence wording from displayed totals, or visually zeroes an asset whose API contract contains valid non-zero cadence data.</done>
</task>

</tasks>

<verification>
Run the focused frontend ledger suite and typecheck, then confirm the item-detail page shows exact cadence date windows and synchronized labels/totals without introducing any next-milestone UI work.
</verification>

<success_criteria>
The item-detail UI displays exact backend cadence windows, keeps labels and values synchronized across cadence changes, and no longer masks valid backend totals behind frontend boundary mistakes.
</success_criteria>

<output>
After completion, create `.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-11-SUMMARY.md`
</output>
