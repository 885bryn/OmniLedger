---
phase: 29-cadence-toggle-synced-cashflow-view
plan: "10"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/items/get-item-net-status.js
  - src/domain/items/item-event-sync.js
  - test/domain/items/get-item-net-status.test.js
  - test/api/items-net-status.test.js
  - test/api/events-list.test.js
autonomous: true
gap_closure: true
requirements: [CASH-03, VIEW-01, VIEW-02, VIEW-03, SAFE-01]
must_haves:
  truths:
    - Any asset with multiple valid in-period recurring obligations or income rows shows all matching monthly amounts, not just a partial subset.
    - Yearly cadence totals accumulate every matching in-year recurring event occurrence and any annual-only payment whose yearly event exists.
    - Assets with qualifying active-period events do not stay at zero just because they are different records from the original repro examples.
    - Event timelines never project or surface financial-item occurrences earlier than the source item's creation or origin point.
  artifacts:
    - path: src/domain/items/get-item-net-status.js
      provides: Shared cadence rollup logic with creation-bounded event inclusion and exact weekly/monthly/yearly active periods
      contains: resolveActiveCadencePeriods
    - path: src/domain/items/item-event-sync.js
      provides: Recurring event projection bounded by the financial item's creation or origin date
      contains: projectItemEvents
    - path: test/domain/items/get-item-net-status.test.js
      provides: Domain regressions for system-wide monthly inclusion, yearly accumulation, and non-zero event-backed assets
      contains: yearly
    - path: test/api/items-net-status.test.js
      provides: Route regressions proving net-status fixes are asset-agnostic and preserve income-minus-obligations parity
      contains: net_cashflow
    - path: test/api/events-list.test.js
      provides: Timeline regression proving projected events cannot predate the source financial item
      contains: projected
  key_links:
    - from: src/domain/items/get-item-net-status.js
      to: financial_events
      via: cadence occurrence lookup that counts every qualifying event inside the shared active period window while rejecting pre-origin events
      pattern: buildEventOccurrenceLookup
    - from: src/domain/items/item-event-sync.js
      to: test/api/events-list.test.js
      via: projected recurring event generation bounded by item creation or origin instead of unconstrained backfill
      pattern: projectItemEvents
---

<objective>
Close the remaining backend consistency gaps so cadence totals and event timelines behave correctly for every asset, not just the reported examples.

Purpose: Monthly, yearly, and projected-event behavior must all come from one system-wide date-window and origin-boundary contract so users can trust the same math everywhere in Phase 29.
Output: Shared backend fixes for cadence rollups and event projection plus focused regressions covering partial monthly sums, yearly accumulation, zeroed assets, and pre-origin timeline leakage.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-CONTEXT.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-VERIFICATION.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-08-SUMMARY.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-09-SUMMARY.md
@src/domain/items/get-item-net-status.js
@src/domain/items/item-event-sync.js
@test/domain/items/get-item-net-status.test.js
@test/api/items-net-status.test.js
@test/api/events-list.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify cadence window and origin-boundary logic across rollups and projected events</name>
  <files>src/domain/items/get-item-net-status.js, src/domain/items/item-event-sync.js</files>
  <action>Refactor the shared active-period and event-inclusion logic so the same UTC-safe window derivation drives weekly, monthly, and yearly summary math everywhere. Fix the system-wide partial-month bug by counting every valid matching event occurrence for each recurring row, not just one matching row per asset, and keep one-time inclusion monthly-only. Clamp recurring event lookup and projected event generation so no event before the financial item's creation timestamp or other reliable origin point can contribute to summaries or appear in timelines. Treat Primary Home, Family SUV, and Downtown Rental Condo as regression examples only; do not hardcode asset-specific exceptions. Keep scope limited to Phase 29 consistency repairs and do not add progress or event-history milestone work.</action>
  <verify>
    <automated>npm test -- test/domain/items/get-item-net-status.test.js --runInBand</automated>
  </verify>
  <done>Backend cadence periods, monthly/yearly totals, and projected-event timelines all follow one asset-agnostic contract that excludes pre-origin occurrences and includes every valid in-period event.</done>
</task>

<task type="auto">
  <name>Task 2: Lock system-wide monthly, yearly, zero-value, and pre-origin regressions</name>
  <files>test/domain/items/get-item-net-status.test.js, test/api/items-net-status.test.js, test/api/events-list.test.js</files>
  <action>Add deterministic regressions that prove: (a) multiple monthly obligations on the same asset all contribute when each has a valid current-month event, (b) yearly totals differ from monthly totals by counting all in-year recurring occurrences and annual-only payments when their yearly event exists, (c) assets with qualifying event-backed rows no longer render all-zero totals just because the asset shape differs from the original repro, and (d) projected timelines never emit dates earlier than the source item's creation or origin boundary. Cover both domain and API layers so the fixes remain system-wide and preserve cadence net parity as income minus obligations.</action>
  <verify>
    <automated>npm test -- test/domain/items/get-item-net-status.test.js test/api/items-net-status.test.js test/api/events-list.test.js --runInBand</automated>
  </verify>
  <done>Focused backend regressions fail if monthly totals drop a valid row, if yearly totals flatten to monthly, if event-backed assets return zero incorrectly, or if projected events leak before the item's creation/origin point.</done>
</task>

</tasks>

<verification>
Run the focused backend suites and confirm the shared net-status contract now sums every valid in-period event system-wide, keeps yearly totals occurrence-aware, and prevents timeline projection before each financial item's creation or origin boundary.
</verification>

<success_criteria>
Monthly and yearly cadence totals are mathematically correct for all assets, and event timelines no longer generate impossible historical rows earlier than the owning financial item could exist.
</success_criteria>

<output>
After completion, create `.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-10-SUMMARY.md`
</output>
