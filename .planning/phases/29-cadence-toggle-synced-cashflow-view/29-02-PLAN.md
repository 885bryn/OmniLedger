---
phase: 29-cadence-toggle-synced-cashflow-view
plan: "02"
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - frontend/src/pages/items/item-detail-page.tsx
autonomous: true
requirements: [CASH-03, VIEW-02]
must_haves:
  truths:
    - User sees obligations, income, and net cashflow values update together after cadence changes.
    - User never sees mixed-cadence card values during loading; the summary section indicates transition state.
    - User sees recurring net cashflow equal to recurring cadence income minus recurring cadence obligations for the selected cadence.
    - User sees one-time impact shown as a separate note under the net card, not blended into recurring net value.
  artifacts:
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Synchronized cadence-switch rendering, section-level loading/error behavior, and cadence-based net/one-time presentation
      contains: cadence_totals
  key_links:
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: summary.cadence_totals.recurring
      via: unified selected-cadence projection for obligations, income, and net cards
      pattern: cadence_totals\.recurring\.(obligations|income|net_cashflow)
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: summary.cadence_totals.one_time_period
      via: one-time impact note rendered separately from recurring net
      pattern: one_time_period
---

<objective>
Implement synchronized cadence-driven cashflow card behavior and robust transition handling.

Purpose: Phase 29 must preserve trust by preventing partial card updates and by making recurring net math explicit at the selected cadence.
Output: Unified cadence projection logic for obligations/income/net cards, section-level loading state, fail-safe rollback behavior, and separate one-time impact note treatment.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-CONTEXT.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-01-PLAN.md
@src/domain/items/get-item-net-status.js
@frontend/src/pages/items/item-detail-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire atomic cadence updates across all summary cashflow cards</name>
  <files>frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Refactor summary value derivation so obligations, income, and net values are all read from one selected-cadence projection object built from `summary.cadence_totals.recurring`. Ensure a cadence change updates all three cards from the same state transition (never per-card independent reads). Keep displayed net equal to selected cadence `income - obligations` using backend-provided recurring net totals, and keep the linked-count card unaffected.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Card updates are synchronized, and displayed recurring net values remain mathematically aligned with selected-cadence recurring totals.</done>
</task>

<task type="auto">
  <name>Task 2: Add section loading, failure fallback, and last-selection-wins cadence behavior</name>
  <files>frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Add a summary-section loading state during cadence transitions so users see one coherent updating block. Implement failure handling that keeps the last known synchronized values on screen and surfaces concise user feedback via existing toast/safety message patterns. Guard rapid toggles so only the latest selected cadence is committed to display state (older in-flight transitions must not overwrite latest selection). Keep one-time impact shown as a separate note directly below the net card, with semantic sign styling (+/-) and neutral zero formatting at 0.00.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Cadence transitions show section-level loading, failed updates preserve prior synced values with feedback, last click wins under rapid toggles, and one-time impact remains separate from recurring net.</done>
</task>

</tasks>

<verification>
Run frontend typecheck, then manually verify on item detail that cadence toggles update all three cashflow cards together, loading appears at section level, and failed updates do not leave mixed-cadence values.
</verification>

<success_criteria>
Cadence switching is trustworthy: all summary cashflow cards stay in sync, recurring net follows normalized recurring totals for the active cadence, and one-time impact remains contextual and separate.
</success_criteria>

<output>
After completion, create `.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-02-SUMMARY.md`
</output>
