---
phase: 29-cadence-toggle-synced-cashflow-view
plan: "09"
type: execute
wave: 2
depends_on: [29-08]
files_modified:
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/__tests__/item-detail-ledger.test.tsx
  - frontend/src/__tests__/items-workflows.test.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
autonomous: true
gap_closure: true
requirements: [VIEW-01, VIEW-03, SAFE-01]
must_haves:
  truths:
    - User sees newly linked commitments and income rows in the commitments tab even when they are not due in the currently selected summary period.
    - User sees summary card titles, period-range copy, and due-this-period count visibly change between weekly, monthly, and yearly views.
    - User can switch cadence without reintroducing non-summary workflow regressions or adding deferred progress/event-history features.
  artifacts:
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Separate all-linked commitments tab data from due-this-period summary rows and cadence-specific label rendering
      contains: commitmentRowsForActiveCadence
    - path: frontend/src/__tests__/item-detail-ledger.test.tsx
      provides: UI regressions for linked-tab visibility and cadence-specific copy/value changes
      contains: commitments
    - path: frontend/src/__tests__/items-workflows.test.tsx
      provides: Workflow safety coverage preserving summary-only cadence scope
      contains: cadence_totals
    - path: frontend/src/locales/en/common.json
      provides: English cadence-range summary wording aligned to the rendered cadence
      contains: summaryLinked
    - path: frontend/src/locales/zh/common.json
      provides: Chinese cadence-range summary wording aligned to the rendered cadence
      contains: summaryLinked
  key_links:
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: /items/:id/net-status
      via: rendered summary labels, period range, and values all read from the same display cadence contract
      pattern: displayCadence
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: child_commitments
      via: commitments tab uses the full linked list while overview count remains cadence-filtered
      pattern: child_commitments
---

<objective>
Close the item-detail presentation gap so linked commitments stay visible and cadence wording tracks the rendered range.

Purpose: Users must be able to find newly attached financial items in the commitments tab while also seeing card labels and period text clearly change with the active weekly, monthly, or yearly summary view.
Output: Item-detail rendering updates, locale copy adjustments, and frontend regressions for linked-tab visibility, cadence copy fidelity, and workflow safety.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-CONTEXT.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-VERIFICATION.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-07-PLAN.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-07-SUMMARY.md
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/__tests__/item-detail-ledger.test.tsx
@frontend/src/__tests__/items-workflows.test.tsx
@frontend/src/locales/en/common.json
@frontend/src/locales/zh/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate due-this-period summary rows from full linked commitments tab visibility</name>
  <files>frontend/src/pages/items/item-detail-page.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json</files>
  <action>Keep the overview summary count and any due-this-period helper copy tied to active-cadence event filtering, but stop reusing that filtered list for the commitments tab. The commitments tab should render all linked financial items returned by net-status so a newly created linked commitment or income row remains discoverable even without a current-period event. At the same time, derive the visible summary period label and card/count wording from the same rendered cadence range as the displayed totals so weekly, monthly, and yearly copy visibly differs in both English and Chinese. Preserve the shared cadence toggle, synchronized-card safety, and existing workflow boundaries.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Users can always see linked financial rows in the commitments tab, while overview summary copy and due-this-period counts still respect the active cadence period and visibly change by cadence.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend regressions for linked-tab visibility and cadence-specific summary copy</name>
  <files>frontend/src/__tests__/item-detail-ledger.test.tsx, frontend/src/__tests__/items-workflows.test.tsx</files>
  <action>Add targeted UI tests proving: (a) a newly linked commitment attached to an asset remains visible in the commitments tab even when it is outside the current summary period, (b) event-backed monthly summary cards render non-zero values from the API contract, (c) switching from monthly to yearly changes both values and visible range wording when fixture totals differ, and (d) workflow payloads and CRUD routes still exclude cadence-summary fields. Keep scope limited to Phase 29 inconsistencies only; do not introduce progress visualizations or an event-history tab.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/item-detail-ledger.test.tsx --runInBand &amp;&amp; npm --prefix frontend test -- src/__tests__/items-workflows.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Frontend regressions fail if linked commitments disappear from the tab, if cadence labels/range text stop changing with the rendered cadence, or if cadence-only fields leak into non-summary workflows.</done>
</task>

</tasks>

<verification>
Run focused frontend tests and typecheck, then confirm the commitments tab shows all linked financial items while overview card labels and range text change with weekly, monthly, and yearly summary output.
</verification>

<success_criteria>
The item-detail UI shows all linked financial rows in the commitments tab, and the summary section presents cadence-specific values and copy that visibly change together without expanding Phase 29 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-09-SUMMARY.md`
</output>
