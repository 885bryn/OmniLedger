---
phase: 29-cadence-toggle-synced-cashflow-view
plan: "07"
type: execute
wave: 2
depends_on: [29-06]
files_modified:
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/__tests__/item-detail-ledger.test.tsx
  - frontend/src/__tests__/items-workflows.test.tsx
autonomous: true
gap_closure: true
requirements: [VIEW-01, VIEW-03, SAFE-01]
must_haves:
  truths:
    - User sees weekly/monthly/yearly commitment and income rows include items when an event exists for that item in the active period.
    - User sees those in-period rows whether event completion is true or false (no completion filter yet).
    - User can keep using existing item/event workflows with cadence behavior isolated to summary presentation and net-status consumption.
  artifacts:
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Summary-card row rendering bound to event-driven active-period API payload for all cadence views
      contains: displayedCadenceTotals
    - path: frontend/src/__tests__/item-detail-ledger.test.tsx
      provides: UI regressions for event-driven row inclusion, completion-agnostic visibility, and yearly-month scoping
      contains: cadence
    - path: frontend/src/__tests__/items-workflows.test.tsx
      provides: Safety coverage ensuring no contract leakage into non-summary item/event workflows
      contains: workflow
  key_links:
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: /items/:id/net-status
      via: shared cadence summary query feeding commitments/income/net cards and row collections
      pattern: net-status
    - from: frontend/src/__tests__/item-detail-ledger.test.tsx
      to: frontend/src/pages/items/item-detail-page.tsx
      via: cadence-switch interaction assertions for in-period event row visibility
      pattern: summary
---

<objective>
Close the UI/test side of the event-driven cadence gap so users see the same in-period event truth the backend now computes.

Purpose: Card rows and synchronized values must visibly reflect event-occurrence inclusion rules across cadence views without introducing new milestone features.
Output: Item-detail rendering and frontend regressions aligned to event-driven, completion-agnostic period behavior with workflow safety preserved.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-CONTEXT.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-VERIFICATION.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-06-PLAN.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-05-SUMMARY.md
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/__tests__/item-detail-ledger.test.tsx
@frontend/src/__tests__/items-workflows.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render cadence card rows from event-driven active-period results</name>
  <files>frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Wire card row visibility for commitments and income to the backend event-driven period contract so rows appear in weekly/monthly/yearly views when in-period events exist for that financial item. Ensure summary card titles and helper/unit wording always use the active cadence term (Weekly/Monthly/Yearly, locale-aware) so labels never drift from selected cadence during toggles. Keep synchronized-card projection and cadence toggle behavior intact. Do not add completion-vs-total progress UI or event-history tab; keep scope to row inclusion and cadence-label fidelity only.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Item detail summary reflects in-period event rows consistently across cadence options, and card labels/helper text remain cadence-correct for the active toggle state while preserving existing synchronized card updates.</done>
</task>

<task type="auto">
  <name>Task 2: Add UI/workflow regressions for event-occurrence row rules</name>
  <files>frontend/src/__tests__/item-detail-ledger.test.tsx, frontend/src/__tests__/items-workflows.test.tsx</files>
  <action>Add focused tests covering: monthly recurring mortgage created in January still appears in current month obligations when a current-month event exists; yearly recurring item appears in monthly view only for months with an event in that year/month; commitments and income rows are shown for in-period events regardless of completion flag. Add targeted cadence-toggle UI assertions that switching Weekly/Monthly/Yearly updates card labels/helper units to the matching cadence each time (no stale monthly text after a weekly/yearly switch). Keep/extend workflow-safety assertions so cadence-summary fields remain out of list/create/delete and event CRUD contracts.</action>
  <verify>
    <automated>npm --prefix frontend test -- src/__tests__/item-detail-ledger.test.tsx --runInBand &amp;&amp; npm --prefix frontend test -- src/__tests__/items-workflows.test.tsx --runInBand &amp;&amp; npm --prefix frontend run typecheck</automated>
  </verify>
  <done>Frontend regressions lock event-driven row inclusion and confirm no non-summary workflow regressions.</done>
</task>

</tasks>

<verification>
Run targeted frontend tests and typecheck, then verify cadence switching still keeps cards in sync while row inclusion follows in-period events (completion ignored).
</verification>

<success_criteria>
Users see cadence cards and row lists that are synchronized and event-driven by active period, with no accidental introduction of deferred progress/event-history features.
</success_criteria>

<output>
After completion, create `.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-07-SUMMARY.md`
</output>
