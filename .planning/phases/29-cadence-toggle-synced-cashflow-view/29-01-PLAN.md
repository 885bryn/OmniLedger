---
phase: 29-cadence-toggle-synced-cashflow-view
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/items/item-detail-page.tsx
  - frontend/src/locales/en/common.json
  - frontend/src/locales/zh/common.json
autonomous: true
requirements: [VIEW-01, VIEW-03]
must_haves:
  truths:
    - User can see one shared segmented cadence control with Weekly, Monthly, and Yearly options directly above the summary cards.
    - User sees monthly as the default cadence each time an asset detail page is opened.
    - User sees card labels and helper copy that explicitly match the active cadence in the current locale.
  artifacts:
    - path: frontend/src/pages/items/item-detail-page.tsx
      provides: Cadence selector state, segmented control UI, and cadence-aware summary card label rendering
      contains: selectedCadence
    - path: frontend/src/locales/en/common.json
      provides: English cadence selector and cadence-explicit summary wording keys
      contains: cadence
    - path: frontend/src/locales/zh/common.json
      provides: Chinese cadence selector and cadence-explicit summary wording keys
      contains: cadence
  key_links:
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: frontend/src/locales/en/common.json
      via: translation keys for segmented control labels and cadence card titles
      pattern: items\.detail\.(cadence|summary)
    - from: frontend/src/pages/items/item-detail-page.tsx
      to: frontend/src/locales/zh/common.json
      via: shared translation key contract across en and zh
      pattern: items\.detail\.(cadence|summary)
---

<objective>
Add a localized segmented cadence toggle to the asset summary and wire cadence-explicit labels.

Purpose: Phase 29 starts by making cadence selection obvious and unambiguous before synchronized update behavior is added.
Output: Item detail summary UI with monthly-default cadence state plus en/zh locale keys for cadence selector text and cadence-explicit card labels.
</objective>

<execution_context>
@C:/Users/bryan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/bryan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-cadence-toggle-synced-cashflow-view/29-CONTEXT.md
@.planning/phases/28-cadence-normalized-totals/28-cadence-normalized-totals-01-SUMMARY.md
@frontend/src/pages/items/item-detail-page.tsx
@frontend/src/locales/en/common.json
@frontend/src/locales/zh/common.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add monthly-default segmented cadence selector in item detail summary</name>
  <files>frontend/src/pages/items/item-detail-page.tsx</files>
  <action>Introduce per-asset-session cadence state with allowed values `weekly`, `monthly`, and `yearly`, defaulting to `monthly` whenever a new asset detail route is opened. Render one shared segmented control directly above the obligations/income/net cards and keep control scope local to this page so leaving and re-entering resets to monthly. Extend the net-status summary typing to include `cadence_totals.recurring` values from the Phase 28 contract and create a single cadence resolution helper that every summary card reads (no card-level independent cadence logic).</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>The overview summary renders one three-option segmented control above all cashflow cards, and cadence state is monthly by default for each item detail entry.</done>
</task>

<task type="auto">
  <name>Task 2: Add locale-driven cadence labels and formula/unit wording</name>
  <files>frontend/src/pages/items/item-detail-page.tsx, frontend/src/locales/en/common.json, frontend/src/locales/zh/common.json</files>
  <action>Update summary card title keys so each card includes explicit full-word cadence text (Weekly/Monthly/Yearly in locale), not symbol-only shorthand. Keep net helper text cadence-explicit and localized in en/zh. Add locale keys for cadence selector labels and selected-state accessibility text. Preserve existing layout conventions and ensure fallback behavior remains stable when cadence metadata is missing from backend payload.</action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>All three summary cards and helper copy reflect the active cadence with localized full-word wording in both English and Chinese.</done>
</task>

</tasks>

<verification>
Typecheck the frontend and confirm the item detail overview now shows a monthly-default segmented cadence control above the three summary cards with locale-driven cadence wording.
</verification>

<success_criteria>
Users can switch among weekly, monthly, and yearly from a single visible control and immediately understand the active cadence from explicit localized labels and helper text.
</success_criteria>

<output>
After completion, create `.planning/phases/29-cadence-toggle-synced-cashflow-view/29-cadence-toggle-synced-cashflow-view-01-SUMMARY.md`
</output>
